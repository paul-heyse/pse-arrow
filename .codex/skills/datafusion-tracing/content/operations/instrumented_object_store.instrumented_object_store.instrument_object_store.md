# `instrumented_object_store::instrumented_object_store::instrument_object_store`

Full upstream contracts; raw type trees and source locators in [structured records](instrumented_object_store.instrumented_object_store.instrument_object_store.json).

<a id="op-4eafbb6f5c51434957c89e55"></a>
## instrument_object_store

`function` · `instrumented_object_store::instrumented_object_store::instrument_object_store` · instrumented-object-store 55.0.0
Reachability: `supported`.  Capture: hosted.

```rust
fn instrument_object_store(store: std::sync::Arc<dyn ObjectStore>, name: &str) -> std::sync::Arc<dyn ObjectStore>
```

Source: `src/instrumented_object_store.rs:36`. [Exact documentation build](https://docs.rs/crate/instrumented-object-store/55.0.0/json).

Instruments the provided `ObjectStore` with tracing.

<a id="op-10b0281459aa2e972c143f48"></a>
## instrument_object_store

`function` · `instrumented_object_store::instrumented_object_store::instrument_object_store` · instrumented-object-store 55.0.0
Reachability: `supported`.  Capture: private.

```rust
fn instrument_object_store(store: std::sync::Arc<dyn ObjectStore>, name: &str) -> std::sync::Arc<dyn ObjectStore>
```

Source: `src/instrumented_object_store.rs:36`. [Exact documentation build](../../build/acquired/rustdoc/instrumented-object-store@55.0.0.private.json.zst).

Instruments the provided `ObjectStore` with tracing.
