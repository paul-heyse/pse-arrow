# `tracing_core::callsite::private`

Crate `tracing-core` · 1 public items · structured records in [`model/tracing_core.callsite.private.json`](../model/tracing_core.callsite.private.json)

## Private

`struct` · `tracing_core::callsite::private::Private`

```rust
struct Private<T>
```

Don't call this function, it's private.

---
