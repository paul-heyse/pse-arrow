# `tracing_subscriber`

Crate `tracing-subscriber` · 1 public items · structured records in [`model/tracing_subscriber.json`](../model/tracing_subscriber.json)

## registry

`function` · `tracing_subscriber::registry`

```rust
fn registry() -> Registry
```

Returns a default [`Registry`].

---
