# `datafusion_tracing::utils`

Crate `datafusion-tracing` · 1 public items · structured records in [`model/datafusion_tracing.utils.json`](../model/datafusion_tracing.utils.json)

## DefaultDisplay

`struct` · `datafusion_tracing::utils::DefaultDisplay`

```rust
struct DefaultDisplay<'a>
```

**Implements**: `core::fmt::Display`

**via `core::fmt::Display`**

```rust
fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result
```

Helper struct for default display formatting of an `ExecutionPlan`.

---
