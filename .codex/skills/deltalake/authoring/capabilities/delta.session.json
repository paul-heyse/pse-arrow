{
  "id": "delta.session",
  "title": "Preserve session capabilities and configure Delta planning",
  "reviewed": "2026-09-18",
  "brief": "Use a Delta-configured session for Delta operations. A concrete SessionState and an arbitrary Session trait wrapper follow different resolution paths; choose fallback policy when caller configuration matters.",
  "task_aliases": [
    "custom session",
    "UDF lost",
    "planner MetricObserver",
    "runtime memory spill",
    "session fallback"
  ],
  "crates": [
    "deltalake-core"
  ],
  "input": [
    "SessionState",
    "Arc<dyn Session>",
    "RuntimeEnv",
    "UDF registry"
  ],
  "output": [
    "DeltaSessionContext",
    "resolved operation session"
  ],
  "properties": [
    "custom session",
    "UDF lost",
    "planner MetricObserver",
    "runtime memory spill",
    "session fallback"
  ],
  "operations": [
    "deltalake_core::delta_datafusion::session::create_session",
    "deltalake_core::delta_datafusion::session::SessionFallbackPolicy",
    "deltalake_core::delta_datafusion::session::DeltaRuntimeEnvBuilder::with_max_spill_size",
    "deltalake_core::delta_datafusion::session::create_session_state_with_spill_config",
    "deltalake_core::operations::write::WriteBuilder::with_session_state",
    "deltalake_core::operations::write::WriteBuilder::with_session_fallback_policy"
  ],
  "choices": [
    {
      "candidate": "create_session / DeltaSessionContext",
      "choose_when": "You want the supplied Delta planner and defaults",
      "tradeoff": "Add caller functions/runtime settings to this state"
    },
    {
      "candidate": "Concrete configured SessionState",
      "choose_when": "Your existing state includes required Delta planning support",
      "tradeoff": "Preserves the supplied state, including any incompatible planner"
    },
    {
      "candidate": "DeriveFromTrait",
      "choose_when": "A Session wrapper must contribute runtime/configuration/function registries",
      "tradeoff": "Derived state is a documented subset; arbitrary custom planner/catalog behavior is not fully cloned"
    },
    {
      "candidate": "RequireSessionState",
      "choose_when": "Silently replacing a trait wrapper would violate caller requirements",
      "tradeoff": "Rejects wrappers; make the desired concrete state explicit"
    }
  ],
  "claims": [
    {
      "id": "delta.session.1",
      "aspect": "default",
      "statement": "InternalDefaults is the default fallback for a non-SessionState wrapper. A caller UDF can disappear; DeriveFromTrait preserves the tested UDF while RequireSessionState rejects the wrapper.",
      "kind": "runtime_observation",
      "evidence": [
        "runtime"
      ],
      "assertion_test": "session_policies_preserve_discard_or_reject_wrapper_udf"
    },
    {
      "id": "delta.session.2",
      "aspect": "planner",
      "statement": "A plain SessionContext state passed to the logical-plan write probe fails to plan MetricObserver. The same plan with create_session().state() writes successfully.",
      "kind": "runtime_observation",
      "evidence": [
        "runtime"
      ],
      "assertion_test": "logical_plan_write_and_staged_writer_visibility"
    },
    {
      "id": "delta.session.3",
      "aspect": "resources",
      "statement": "create_session_state_with_spill_config(None,None) uses the default runtime. The named max_spill_size configures the FairSpillPool memory budget, while max_temp_directory_size bounds temporary disk; these are separate resources.",
      "kind": "source_observation",
      "evidence": [
        "upstream",
        "source"
      ]
    },
    {
      "id": "delta.session.4",
      "aspect": "resource boundary",
      "statement": "A configured 1024-byte FairSpillPool accepted a 512-byte reservation, rejected another 1024 bytes and released the reservation on drop. This tests pool configuration, not total operation/process RSS or automatic spilling.",
      "kind": "runtime_observation",
      "evidence": [
        "runtime"
      ],
      "assertion_test": "configured_memory_pool_enforces_reservation_budget"
    }
  ],
  "implementation": [
    "Choose policy based on required caller capabilities, then test with a distinguishing UDF/configuration rather than state type alone.",
    "Do not reuse an arbitrary session solely to avoid construction cost; verify planner, store mappings and runtime sharing semantics.",
    "Record runtime limits separately from batch/target-file-size options."
  ],
  "effects": [
    "configure planner and runtime",
    "share or derive session state"
  ],
  "errors": [
    "Resolution rejection and missing functions/planner failures occur before a successful commit; verify table state when diagnosing a failed operation."
  ],
  "unknowns": [
    "The probes establish UDF preservation, not equivalence of every Session implementation or process memory bounds."
  ],
  "evidence": [
    {
      "id": "upstream",
      "kind": "upstream_documentation",
      "scope": "Exact git-capture full contracts for the selected operations"
    },
    {
      "id": "source",
      "kind": "source_observation",
      "path": "skill_improvement/evidence/sources/delta-rs/crates/core/src/delta_datafusion/session.rs",
      "scope": "Pinned delta-rs implementation; full file retained with acquisition provenance"
    },
    {
      "id": "runtime",
      "kind": "runtime_observation",
      "path": "skill_improvement/evidence/implementation/probe-results.json",
      "scope": "Named local assertions only; see profile, fixtures and source digests",
      "tests": [
        "session_policies_preserve_discard_or_reject_wrapper_udf",
        "logical_plan_write_and_staged_writer_visibility",
        "configured_memory_pool_enforces_reservation_budget"
      ]
    }
  ]
}
