id: corpus-extension-impl
language: rust
severity: hint
message: Upstream code implements this extension point here.
note: |
  The most direct answer to "can I plug into this, and what does a real implementation look
  like". Run against content/corpus/examples and content/corpus/tests. An example is a
  complete compiling program; a test is real usage that upstream keeps green. Both are
  evidence, and they are kept in separate directories so you can tell which you are reading.
metadata:
  record: usage-impl
  reads: content/corpus
rule:
  kind: impl_item
  has:
    field: trait
    regex: '.'
