# Generated by build/queries.py from the pinned model. Do not edit by hand.
id: project-deprecated-api
language: rust
severity: hint
message: Deprecated at the pinned revision. A replacement exists.
note: |
  Deprecated code still compiles, so nothing forces a correction and a model trained before the
  deprecation keeps emitting it. These 3 identifiers are deprecated as of the pinned
  release, with the replacement the upstream note names:

  AWS_S3_ASSUME_ROLE_ARN -- since 0.20.0: Please use AWS_IAM_ROLE_ARN instead
    AWS_S3_ROLE_SESSION_NAME -- since 0.20.0: Please use AWS_IAM_ROLE_SESSION_NAME instead
    DeltaPhysicalCodec -- DeltaPhysicalCodec only supports the retired physical DeltaScan wrapper. Use DeltaLogicalCodec for table-provider serialization until a DeltaScanExec physical codec is available.

  Matched only as a qualified path rooted in a pinned crate, and only for names nothing
  undeprecated shares, because ast-grep resolves no imports. A use written as
  `use deltalake::x::name; name()` is therefore missed -- precision was chosen over recall here.
  Confirm against content/index/symbols.tsv before acting.
metadata:
  record: capability-gap
  capability: deprecation
  generated: true
rule:
  any:
    - kind: scoped_identifier
      regex: '^(buoyant_kernel|buoyant_kernel_engine|delta_kernel|delta_kernel_default_engine|deltalake|deltalake_aws|deltalake_azure|deltalake_catalog_glue|deltalake_catalog_unity|deltalake_core|deltalake_gcp|deltalake_hdfs|deltalake_lakefs|deltalake_mount|deltalake_opendal)\w*::.*(AWS_S3_ASSUME_ROLE_ARN|AWS_S3_ROLE_SESSION_NAME|DeltaPhysicalCodec)$'
    - kind: scoped_type_identifier
      regex: '^(buoyant_kernel|buoyant_kernel_engine|delta_kernel|delta_kernel_default_engine|deltalake|deltalake_aws|deltalake_azure|deltalake_catalog_glue|deltalake_catalog_unity|deltalake_core|deltalake_gcp|deltalake_hdfs|deltalake_lakefs|deltalake_mount|deltalake_opendal)\w*::.*(AWS_S3_ASSUME_ROLE_ARN|AWS_S3_ROLE_SESSION_NAME|DeltaPhysicalCodec)$'
