id: project-vacuum-default-retention
valid:
  - |
    async fn go() {
        let t = table.vacuum().with_retention_period(d).await;
    }
invalid:
  - |
    async fn go() {
        let t = table.vacuum().await;
    }
