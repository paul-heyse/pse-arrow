id: corpus-extension-impl
valid:
  - |
    impl DeltaTable {
        fn helper(&self) -> usize { 0 }
    }
invalid:
  - |
    impl LogStore for MyLogStore {
        fn name(&self) -> String { todo!() }
    }
