id: project-deltaops-entry-point
snapshots:
  ? |
    async fn go() {
        let t = DeltaOps(table).delete().await;
    }
  : labels:
    - source: DeltaOps
      style: primary
      start: 28
      end: 36
