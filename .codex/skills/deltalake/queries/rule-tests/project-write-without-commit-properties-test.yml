id: project-write-without-commit-properties
valid:
  - |
    async fn go() {
        let t = table.write(b).with_commit_properties(props).await;
    }
invalid:
  - |
    async fn go() {
        let t = table.write(b).await;
    }
