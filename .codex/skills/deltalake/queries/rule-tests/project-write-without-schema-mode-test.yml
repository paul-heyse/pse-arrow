id: project-write-without-schema-mode
valid:
  - |
    async fn go() {
        let t = table.write(batches).with_schema_mode(SchemaMode::Merge).await;
    }
invalid:
  - |
    async fn go() {
        let t = table.write(batches).await;
    }
