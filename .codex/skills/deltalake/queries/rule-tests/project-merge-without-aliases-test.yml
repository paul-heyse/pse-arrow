id: project-merge-without-aliases
valid:
  - |
    async fn go() {
        let t = table.merge(df, pred).with_source_alias("s").with_target_alias("t").await;
    }
invalid:
  - |
    async fn go() {
        let t = table.merge(df, pred).await;
    }
