# A model record: an object carrying a `path` member. Anchors a rule to one item entry rather
# than matching any nested object inside it.
id: json-record
language: json
rule:
  kind: object
  has:
    kind: pair
    has:
      field: key
      regex: '^"path"$'
