id: corpus-extension-impl
language: rust
severity: hint
message: An upstream example implements this extension point here.
note: |
  The most direct answer to "can I plug into this, and what does a real implementation look like".
  Each match is a complete compiling program, not a snippet. Run against content/corpus/examples.
metadata:
  record: example-impl
  reads: content/corpus/examples
rule:
  kind: impl_item
  has:
    field: trait
    regex: '.'
