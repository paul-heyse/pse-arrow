id: model-builder-methods
valid:
  - |
    {"name": "batch_size", "signature": "fn batch_size(&self) -> usize"}
  - |
    {"name": "with_schema", "signature": "fn with_schema(&self) -> SchemaRef"}
invalid:
  - |
    {"name": "with_batch_size", "signature": "fn with_batch_size(self, n: usize) -> Self"}
