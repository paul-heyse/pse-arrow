# `datafusion_physical_expr::equivalence::class`

Crate `datafusion-physical-expr` · 4 public items · structured records in [`model/datafusion_physical_expr.equivalence.class.json`](../model/datafusion_physical_expr.equivalence.class.json)

## AcrossPartitions

`enum` · `datafusion_physical_expr::equivalence::class::AcrossPartitions`

Also reachable as `datafusion::physical_expr::AcrossPartitions`, `datafusion_physical_expr::AcrossPartitions`, `datafusion_physical_expr::equivalence::AcrossPartitions`

```rust
enum AcrossPartitions
```

**Variants**: `Heterogeneous`, `Uniform`

**Implements**: `core::fmt::Display`

**Derives**: Clone, Debug, Default, Eq, PartialEq, StructuralPartialEq

**via `core::fmt::Display`**

```rust
fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result
```

[Full member, field, variant and typed contracts](../operations/datafusion_physical_expr.equivalence.class.AcrossPartitions.md).


Represents whether a constant expression's value is uniform or varies across
partitions. Has two variants:
- `Heterogeneous`: The constant expression may have different values for
  different partitions.
- `Uniform(Option<ScalarValue>)`: The constant expression has the same value
  across all partitions, or is `None` if the value is unknown.

---

## ConstExpr

`struct` · `datafusion_physical_expr::equivalence::class::ConstExpr`

Also reachable as `datafusion::physical_expr::ConstExpr`, `datafusion_physical_expr::ConstExpr`, `datafusion_physical_expr::equivalence::ConstExpr`

```rust
struct ConstExpr
```

**Fields**: `expr`, `across_partitions`

**Implements**: `core::convert::From`, `core::fmt::Display`

**Derives**: Clone, Debug, PartialEq

**Methods** (3)

```rust
fn collect_predicate_constants(input_eqs: &EquivalenceProperties, predicate: &Arc<dyn PhysicalExpr>) -> Vec<ConstExpr>
fn format_list(input: &[ConstExpr]) -> impl Display + '_
fn new(expr: Arc<dyn PhysicalExpr>, across_partitions: AcrossPartitions) -> Self
```

**via `core::convert::From`**

```rust
fn from(expr: Arc<dyn PhysicalExpr>) -> Self
```

**via `core::fmt::Display`**

```rust
fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result
```

[Full member, field, variant and typed contracts](../operations/datafusion_physical_expr.equivalence.class.ConstExpr.md).


A structure representing a expression known to be constant in a physical
execution plan.

The `ConstExpr` struct encapsulates an expression that is constant during
the execution of a query. For example if a filter like `A = 5` appears
earlier in the plan, `A` would become a constant in subsequent operations.

# Fields

- `expr`: Constant expression for a node in the physical plan.
- `across_partitions`: A boolean flag indicating whether the constant
  expression is the same across partitions. If set to `true`, the constant
  expression has same value for all partitions. If set to `false`, the
  constant expression may have different values for different partitions.

# Example

```rust
# use datafusion_physical_expr::ConstExpr;
# use datafusion_physical_expr::expressions::lit;
let col = lit(5);
// Create a constant expression from a physical expression:
let const_expr = ConstExpr::from(col);
```

---

## EquivalenceClass

`struct` · `datafusion_physical_expr::equivalence::class::EquivalenceClass`

Also reachable as `datafusion_physical_expr::equivalence::EquivalenceClass`

```rust
struct EquivalenceClass
```

**Implements**: `core::fmt::Display`, `core::iter::traits::collect::IntoIterator`, `core::ops::deref::Deref`

**Derives**: Clone, Debug, Default, Eq, PartialEq, StructuralPartialEq

**Methods** (7)

```rust
fn canonical_expr(&self) -> Option<&Arc<dyn PhysicalExpr>>
fn contains_any(&self, other: &Self) -> bool
fn extend(&mut self, other: Self)
fn is_trivial(&self) -> bool
fn new(exprs: impl IntoIterator<Item = Arc<dyn PhysicalExpr>>) -> Self
fn push(&mut self, expr: Arc<dyn PhysicalExpr>)
fn try_with_offset(&self, offset: isize) -> Result<Self>
```

**via `core::fmt::Display`**

```rust
fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result
```

**via `core::iter::traits::collect::IntoIterator`**

```rust
fn into_iter(self) -> Self::IntoIter
```

**via `core::ops::deref::Deref`**

```rust
fn deref(&self) -> &Self::Target
```

[Full member, field, variant and typed contracts](../operations/datafusion_physical_expr.equivalence.class.EquivalenceClass.md).


An `EquivalenceClass` is a set of [`Arc<dyn PhysicalExpr>`]s that are known
to have the same value for all tuples in a relation. These are generated by
equality predicates (e.g. `a = b`), typically equi-join conditions and
equality conditions in filters.

Two `EquivalenceClass`es are equal if they contains the same expressions in
without any ordering.

---

## EquivalenceGroup

`struct` · `datafusion_physical_expr::equivalence::class::EquivalenceGroup`

Also reachable as `datafusion_physical_expr::equivalence::EquivalenceGroup`

```rust
struct EquivalenceGroup
```

**Implements**: `core::convert::From`, `core::fmt::Display`, `core::iter::traits::collect::IntoIterator`, `core::ops::deref::Deref`

**Derives**: Clone, Debug, Default

**Methods** (17)

```rust
fn add_constant(&mut self, const_expr: ConstExpr)
fn add_equal_conditions(&mut self, left: Arc<dyn PhysicalExpr>, right: Arc<dyn PhysicalExpr>) -> bool
fn clear_per_partition_constants(&mut self) -> bool
fn exprs_equal(&self, left: &Arc<dyn PhysicalExpr>, right: &Arc<dyn PhysicalExpr>) -> bool
fn extend(&mut self, other: Self) -> bool
fn get_equivalence_class(&self, expr: &Arc<dyn PhysicalExpr>) -> Option<&EquivalenceClass>
fn is_expr_constant(&self, expr: &Arc<dyn PhysicalExpr>) -> Option<AcrossPartitions>
fn join(&self, right_equivalences: &Self, join_type: &JoinType, left_size: usize, on: &[(PhysicalExprRef, PhysicalExprRef)]) -> Result<Self>
fn new(classes: impl IntoIterator<Item = EquivalenceClass>) -> Self
fn normalize_expr(&self, expr: Arc<dyn PhysicalExpr>) -> Arc<dyn PhysicalExpr>
fn normalize_sort_expr(&self, sort_expr: PhysicalSortExpr) -> PhysicalSortExpr
fn normalize_sort_exprs<'a>(&'a self, sort_exprs: impl IntoIterator<Item = PhysicalSortExpr> + 'a) -> impl Iterator<Item = PhysicalSortExpr> + 'a
fn normalize_sort_requirement(&self, sort_requirement: PhysicalSortRequirement) -> PhysicalSortRequirement
fn normalize_sort_requirements<'a>(&'a self, sort_reqs: impl IntoIterator<Item = PhysicalSortRequirement> + 'a) -> impl Iterator<Item = PhysicalSortRequirement> + 'a
fn project(&self, mapping: &ProjectionMapping) -> Self
fn project_expr(&self, mapping: &ProjectionMapping, expr: &Arc<dyn PhysicalExpr>) -> Option<Arc<dyn PhysicalExpr>>
fn project_expressions<'a>(&'a self, mapping: &'a ProjectionMapping, expressions: impl IntoIterator<Item = &'a Arc<dyn PhysicalExpr>> + 'a) -> impl Iterator<Item = Option<Arc<dyn PhysicalExpr>>> + 'a
```

**via `core::convert::From`**

```rust
fn from(classes: Vec<EquivalenceClass>) -> Self
```

**via `core::fmt::Display`**

```rust
fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result
```

**via `core::iter::traits::collect::IntoIterator`**

```rust
fn into_iter(self) -> Self::IntoIter
```

**via `core::ops::deref::Deref`**

```rust
fn deref(&self) -> &Self::Target
```

[Full member, field, variant and typed contracts](../operations/datafusion_physical_expr.equivalence.class.EquivalenceGroup.md).


A collection of distinct `EquivalenceClass`es. This object supports fast
lookups of expressions and their equivalence classes.

---
