# `object_store::aws::client`

Crate `object_store` · 1 public items · structured records in [`model/object_store.aws.client.json`](../model/object_store.aws.client.json)

## RequestError

`enum` · `object_store::aws::client::RequestError`

```rust
enum RequestError
```

**Variants**: `Generic`, `Retry`

---
