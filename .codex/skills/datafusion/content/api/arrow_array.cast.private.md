# `arrow_array::cast::private`

Crate `arrow-array` · 1 public items · structured records in [`model/arrow_array.cast.private.json`](../model/arrow_array.cast.private.json)

## Sealed

`trait` · `arrow_array::cast::private::Sealed`

```rust
trait Sealed
```

---
