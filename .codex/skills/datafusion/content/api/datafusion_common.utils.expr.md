# `datafusion_common::utils::expr`

Crate `datafusion-common` · 1 public items · structured records in [`model/datafusion_common.utils.expr.json`](../model/datafusion_common.utils.expr.json)

## COUNT_STAR_EXPANSION

`constant` · `datafusion_common::utils::expr::COUNT_STAR_EXPANSION`

Also reachable as `datafusion_expr::utils::COUNT_STAR_EXPANSION`

```rust
const COUNT_STAR_EXPANSION: ScalarValue = _
```

The value to which `COUNT(*)` is expanded to in
`COUNT(<constant>)` expressions

---
