# `object_store::azure::credential::authority_hosts`

Crate `object_store` · 4 public items · structured records in [`model/object_store.azure.credential.authority_hosts.json`](../model/object_store.azure.credential.authority_hosts.json)

## AZURE_CHINA

`constant` · `object_store::azure::credential::authority_hosts::AZURE_CHINA`

Also reachable as `object_store::azure::authority_hosts::AZURE_CHINA`

```rust
const AZURE_CHINA: &str = "https://login.chinacloudapi.cn"
```

[Full member, field, variant and typed contracts](../operations/object_store.azure.credential.authority_hosts.AZURE_CHINA.md).


China-based Azure Authority Host

---

## AZURE_GERMANY

`constant` · `object_store::azure::credential::authority_hosts::AZURE_GERMANY`

Also reachable as `object_store::azure::authority_hosts::AZURE_GERMANY`

```rust
const AZURE_GERMANY: &str = "https://login.microsoftonline.de"
```

[Full member, field, variant and typed contracts](../operations/object_store.azure.credential.authority_hosts.AZURE_GERMANY.md).


Germany-based Azure Authority Host

---

## AZURE_GOVERNMENT

`constant` · `object_store::azure::credential::authority_hosts::AZURE_GOVERNMENT`

Also reachable as `object_store::azure::authority_hosts::AZURE_GOVERNMENT`

```rust
const AZURE_GOVERNMENT: &str = "https://login.microsoftonline.us"
```

[Full member, field, variant and typed contracts](../operations/object_store.azure.credential.authority_hosts.AZURE_GOVERNMENT.md).


US Government Azure Authority Host

---

## AZURE_PUBLIC_CLOUD

`constant` · `object_store::azure::credential::authority_hosts::AZURE_PUBLIC_CLOUD`

Also reachable as `object_store::azure::authority_hosts::AZURE_PUBLIC_CLOUD`

```rust
const AZURE_PUBLIC_CLOUD: &str = "https://login.microsoftonline.com"
```

[Full member, field, variant and typed contracts](../operations/object_store.azure.credential.authority_hosts.AZURE_PUBLIC_CLOUD.md).


Public Cloud Azure Authority Host

---
