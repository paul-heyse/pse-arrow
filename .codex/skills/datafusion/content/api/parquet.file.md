# `parquet::file`

Crate `parquet` · 1 public items · structured records in [`model/parquet.file.json`](../model/parquet.file.json)

## FOOTER_SIZE

`constant` · `parquet::file::FOOTER_SIZE`

```rust
const FOOTER_SIZE: usize = 8
```

The length of the parquet footer in bytes

---
