# `arrow_ipc::gen::Schema`

Crate `arrow-ipc` · 170 public items · structured records in [`model/arrow_ipc.gen.Schema.json`](../model/arrow_ipc.gen.Schema.json)

## ENUM_MAX_DATE_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_DATE_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_DATE_UNIT`, `arrow_ipc::ENUM_MAX_DATE_UNIT`

```rust
const ENUM_MAX_DATE_UNIT: i16 = 1
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_DATE_UNIT.md).


---

## ENUM_MAX_DICTIONARY_KIND

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_DICTIONARY_KIND`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_DICTIONARY_KIND`, `arrow_ipc::ENUM_MAX_DICTIONARY_KIND`

```rust
const ENUM_MAX_DICTIONARY_KIND: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_DICTIONARY_KIND.md).


---

## ENUM_MAX_ENDIANNESS

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_ENDIANNESS`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_ENDIANNESS`, `arrow_ipc::ENUM_MAX_ENDIANNESS`

```rust
const ENUM_MAX_ENDIANNESS: i16 = 1
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_ENDIANNESS.md).


---

## ENUM_MAX_FEATURE

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_FEATURE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_FEATURE`, `arrow_ipc::ENUM_MAX_FEATURE`

```rust
const ENUM_MAX_FEATURE: i64 = 2
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_FEATURE.md).


---

## ENUM_MAX_INTERVAL_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_INTERVAL_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_INTERVAL_UNIT`, `arrow_ipc::ENUM_MAX_INTERVAL_UNIT`

```rust
const ENUM_MAX_INTERVAL_UNIT: i16 = 2
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_INTERVAL_UNIT.md).


---

## ENUM_MAX_METADATA_VERSION

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_METADATA_VERSION`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_METADATA_VERSION`, `arrow_ipc::ENUM_MAX_METADATA_VERSION`

```rust
const ENUM_MAX_METADATA_VERSION: i16 = 4
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_METADATA_VERSION.md).


---

## ENUM_MAX_PRECISION

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_PRECISION`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_PRECISION`, `arrow_ipc::ENUM_MAX_PRECISION`

```rust
const ENUM_MAX_PRECISION: i16 = 2
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_PRECISION.md).


---

## ENUM_MAX_TIME_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_TIME_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_TIME_UNIT`, `arrow_ipc::ENUM_MAX_TIME_UNIT`

```rust
const ENUM_MAX_TIME_UNIT: i16 = 3
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_TIME_UNIT.md).


---

## ENUM_MAX_TYPE

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_TYPE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_TYPE`, `arrow_ipc::ENUM_MAX_TYPE`

```rust
const ENUM_MAX_TYPE: u8 = 26
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_TYPE.md).


---

## ENUM_MAX_UNION_MODE

`constant` · `arrow_ipc::gen::Schema::ENUM_MAX_UNION_MODE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MAX_UNION_MODE`, `arrow_ipc::ENUM_MAX_UNION_MODE`

```rust
const ENUM_MAX_UNION_MODE: i16 = 1
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MAX_UNION_MODE.md).


---

## ENUM_MIN_DATE_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_DATE_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_DATE_UNIT`, `arrow_ipc::ENUM_MIN_DATE_UNIT`

```rust
const ENUM_MIN_DATE_UNIT: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_DATE_UNIT.md).


---

## ENUM_MIN_DICTIONARY_KIND

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_DICTIONARY_KIND`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_DICTIONARY_KIND`, `arrow_ipc::ENUM_MIN_DICTIONARY_KIND`

```rust
const ENUM_MIN_DICTIONARY_KIND: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_DICTIONARY_KIND.md).


---

## ENUM_MIN_ENDIANNESS

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_ENDIANNESS`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_ENDIANNESS`, `arrow_ipc::ENUM_MIN_ENDIANNESS`

```rust
const ENUM_MIN_ENDIANNESS: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_ENDIANNESS.md).


---

## ENUM_MIN_FEATURE

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_FEATURE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_FEATURE`, `arrow_ipc::ENUM_MIN_FEATURE`

```rust
const ENUM_MIN_FEATURE: i64 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_FEATURE.md).


---

## ENUM_MIN_INTERVAL_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_INTERVAL_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_INTERVAL_UNIT`, `arrow_ipc::ENUM_MIN_INTERVAL_UNIT`

```rust
const ENUM_MIN_INTERVAL_UNIT: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_INTERVAL_UNIT.md).


---

## ENUM_MIN_METADATA_VERSION

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_METADATA_VERSION`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_METADATA_VERSION`, `arrow_ipc::ENUM_MIN_METADATA_VERSION`

```rust
const ENUM_MIN_METADATA_VERSION: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_METADATA_VERSION.md).


---

## ENUM_MIN_PRECISION

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_PRECISION`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_PRECISION`, `arrow_ipc::ENUM_MIN_PRECISION`

```rust
const ENUM_MIN_PRECISION: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_PRECISION.md).


---

## ENUM_MIN_TIME_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_TIME_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_TIME_UNIT`, `arrow_ipc::ENUM_MIN_TIME_UNIT`

```rust
const ENUM_MIN_TIME_UNIT: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_TIME_UNIT.md).


---

## ENUM_MIN_TYPE

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_TYPE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_TYPE`, `arrow_ipc::ENUM_MIN_TYPE`

```rust
const ENUM_MIN_TYPE: u8 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_TYPE.md).


---

## ENUM_MIN_UNION_MODE

`constant` · `arrow_ipc::gen::Schema::ENUM_MIN_UNION_MODE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_MIN_UNION_MODE`, `arrow_ipc::ENUM_MIN_UNION_MODE`

```rust
const ENUM_MIN_UNION_MODE: i16 = 0
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_MIN_UNION_MODE.md).


---

## ENUM_VALUES_DATE_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_DATE_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_DATE_UNIT`, `arrow_ipc::ENUM_VALUES_DATE_UNIT`

```rust
const ENUM_VALUES_DATE_UNIT: [DateUnit; 2] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_DATE_UNIT.md).


---

## ENUM_VALUES_DICTIONARY_KIND

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_DICTIONARY_KIND`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_DICTIONARY_KIND`, `arrow_ipc::ENUM_VALUES_DICTIONARY_KIND`

```rust
const ENUM_VALUES_DICTIONARY_KIND: [DictionaryKind; 1] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_DICTIONARY_KIND.md).


---

## ENUM_VALUES_ENDIANNESS

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_ENDIANNESS`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_ENDIANNESS`, `arrow_ipc::ENUM_VALUES_ENDIANNESS`

```rust
const ENUM_VALUES_ENDIANNESS: [Endianness; 2] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_ENDIANNESS.md).


---

## ENUM_VALUES_FEATURE

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_FEATURE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_FEATURE`, `arrow_ipc::ENUM_VALUES_FEATURE`

```rust
const ENUM_VALUES_FEATURE: [Feature; 3] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_FEATURE.md).


---

## ENUM_VALUES_INTERVAL_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_INTERVAL_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_INTERVAL_UNIT`, `arrow_ipc::ENUM_VALUES_INTERVAL_UNIT`

```rust
const ENUM_VALUES_INTERVAL_UNIT: [IntervalUnit; 3] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_INTERVAL_UNIT.md).


---

## ENUM_VALUES_METADATA_VERSION

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_METADATA_VERSION`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_METADATA_VERSION`, `arrow_ipc::ENUM_VALUES_METADATA_VERSION`

```rust
const ENUM_VALUES_METADATA_VERSION: [MetadataVersion; 5] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_METADATA_VERSION.md).


---

## ENUM_VALUES_PRECISION

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_PRECISION`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_PRECISION`, `arrow_ipc::ENUM_VALUES_PRECISION`

```rust
const ENUM_VALUES_PRECISION: [Precision; 3] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_PRECISION.md).


---

## ENUM_VALUES_TIME_UNIT

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_TIME_UNIT`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_TIME_UNIT`, `arrow_ipc::ENUM_VALUES_TIME_UNIT`

```rust
const ENUM_VALUES_TIME_UNIT: [TimeUnit; 4] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_TIME_UNIT.md).


---

## ENUM_VALUES_TYPE

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_TYPE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_TYPE`, `arrow_ipc::ENUM_VALUES_TYPE`

```rust
const ENUM_VALUES_TYPE: [Type; 27] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_TYPE.md).


---

## ENUM_VALUES_UNION_MODE

`constant` · `arrow_ipc::gen::Schema::ENUM_VALUES_UNION_MODE`

> **Deprecated** — since 2.0.0: Use associated constants instead. This will no longer be generated in 2021.

Also reachable as `arrow::ipc::ENUM_VALUES_UNION_MODE`, `arrow_ipc::ENUM_VALUES_UNION_MODE`

```rust
const ENUM_VALUES_UNION_MODE: [UnionMode; 2] = _
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ENUM_VALUES_UNION_MODE.md).


---

## BinaryOffset

`enum` · `arrow_ipc::gen::Schema::BinaryOffset`

Also reachable as `arrow::ipc::BinaryOffset`, `arrow_ipc::BinaryOffset`

```rust
enum BinaryOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryOffset.md).


---

## BinaryViewOffset

`enum` · `arrow_ipc::gen::Schema::BinaryViewOffset`

Also reachable as `arrow::ipc::BinaryViewOffset`, `arrow_ipc::BinaryViewOffset`

```rust
enum BinaryViewOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryViewOffset.md).


---

## BoolOffset

`enum` · `arrow_ipc::gen::Schema::BoolOffset`

Also reachable as `arrow::ipc::BoolOffset`, `arrow_ipc::BoolOffset`

```rust
enum BoolOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BoolOffset.md).


---

## DateOffset

`enum` · `arrow_ipc::gen::Schema::DateOffset`

Also reachable as `arrow::ipc::DateOffset`, `arrow_ipc::DateOffset`

```rust
enum DateOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DateOffset.md).


---

## DecimalOffset

`enum` · `arrow_ipc::gen::Schema::DecimalOffset`

Also reachable as `arrow::ipc::DecimalOffset`, `arrow_ipc::DecimalOffset`

```rust
enum DecimalOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DecimalOffset.md).


---

## DictionaryEncodingOffset

`enum` · `arrow_ipc::gen::Schema::DictionaryEncodingOffset`

Also reachable as `arrow::ipc::DictionaryEncodingOffset`, `arrow_ipc::DictionaryEncodingOffset`

```rust
enum DictionaryEncodingOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DictionaryEncodingOffset.md).


---

## DurationOffset

`enum` · `arrow_ipc::gen::Schema::DurationOffset`

Also reachable as `arrow::ipc::DurationOffset`, `arrow_ipc::DurationOffset`

```rust
enum DurationOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DurationOffset.md).


---

## FieldOffset

`enum` · `arrow_ipc::gen::Schema::FieldOffset`

Also reachable as `arrow::ipc::FieldOffset`, `arrow_ipc::FieldOffset`

```rust
enum FieldOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FieldOffset.md).


---

## FixedSizeBinaryOffset

`enum` · `arrow_ipc::gen::Schema::FixedSizeBinaryOffset`

Also reachable as `arrow::ipc::FixedSizeBinaryOffset`, `arrow_ipc::FixedSizeBinaryOffset`

```rust
enum FixedSizeBinaryOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeBinaryOffset.md).


---

## FixedSizeListOffset

`enum` · `arrow_ipc::gen::Schema::FixedSizeListOffset`

Also reachable as `arrow::ipc::FixedSizeListOffset`, `arrow_ipc::FixedSizeListOffset`

```rust
enum FixedSizeListOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeListOffset.md).


---

## FloatingPointOffset

`enum` · `arrow_ipc::gen::Schema::FloatingPointOffset`

Also reachable as `arrow::ipc::FloatingPointOffset`, `arrow_ipc::FloatingPointOffset`

```rust
enum FloatingPointOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FloatingPointOffset.md).


---

## IntOffset

`enum` · `arrow_ipc::gen::Schema::IntOffset`

Also reachable as `arrow::ipc::IntOffset`, `arrow_ipc::IntOffset`

```rust
enum IntOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntOffset.md).


---

## IntervalOffset

`enum` · `arrow_ipc::gen::Schema::IntervalOffset`

Also reachable as `arrow::ipc::IntervalOffset`, `arrow_ipc::IntervalOffset`

```rust
enum IntervalOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntervalOffset.md).


---

## KeyValueOffset

`enum` · `arrow_ipc::gen::Schema::KeyValueOffset`

Also reachable as `arrow::ipc::KeyValueOffset`, `arrow_ipc::KeyValueOffset`

```rust
enum KeyValueOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.KeyValueOffset.md).


---

## LargeBinaryOffset

`enum` · `arrow_ipc::gen::Schema::LargeBinaryOffset`

Also reachable as `arrow::ipc::LargeBinaryOffset`, `arrow_ipc::LargeBinaryOffset`

```rust
enum LargeBinaryOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeBinaryOffset.md).


---

## LargeListOffset

`enum` · `arrow_ipc::gen::Schema::LargeListOffset`

Also reachable as `arrow::ipc::LargeListOffset`, `arrow_ipc::LargeListOffset`

```rust
enum LargeListOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListOffset.md).


---

## LargeListViewOffset

`enum` · `arrow_ipc::gen::Schema::LargeListViewOffset`

Also reachable as `arrow::ipc::LargeListViewOffset`, `arrow_ipc::LargeListViewOffset`

```rust
enum LargeListViewOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListViewOffset.md).


---

## LargeUtf8Offset

`enum` · `arrow_ipc::gen::Schema::LargeUtf8Offset`

Also reachable as `arrow::ipc::LargeUtf8Offset`, `arrow_ipc::LargeUtf8Offset`

```rust
enum LargeUtf8Offset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeUtf8Offset.md).


---

## ListOffset

`enum` · `arrow_ipc::gen::Schema::ListOffset`

Also reachable as `arrow::ipc::ListOffset`, `arrow_ipc::ListOffset`

```rust
enum ListOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListOffset.md).


---

## ListViewOffset

`enum` · `arrow_ipc::gen::Schema::ListViewOffset`

Also reachable as `arrow::ipc::ListViewOffset`, `arrow_ipc::ListViewOffset`

```rust
enum ListViewOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListViewOffset.md).


---

## MapOffset

`enum` · `arrow_ipc::gen::Schema::MapOffset`

Also reachable as `arrow::ipc::MapOffset`, `arrow_ipc::MapOffset`

```rust
enum MapOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.MapOffset.md).


---

## NullOffset

`enum` · `arrow_ipc::gen::Schema::NullOffset`

Also reachable as `arrow::ipc::NullOffset`, `arrow_ipc::NullOffset`

```rust
enum NullOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.NullOffset.md).


---

## RunEndEncodedOffset

`enum` · `arrow_ipc::gen::Schema::RunEndEncodedOffset`

Also reachable as `arrow::ipc::RunEndEncodedOffset`, `arrow_ipc::RunEndEncodedOffset`

```rust
enum RunEndEncodedOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.RunEndEncodedOffset.md).


---

## SchemaOffset

`enum` · `arrow_ipc::gen::Schema::SchemaOffset`

Also reachable as `arrow::ipc::SchemaOffset`, `arrow_ipc::SchemaOffset`

```rust
enum SchemaOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.SchemaOffset.md).


---

## Struct_Offset

`enum` · `arrow_ipc::gen::Schema::Struct_Offset`

Also reachable as `arrow::ipc::Struct_Offset`, `arrow_ipc::Struct_Offset`

```rust
enum Struct_Offset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Struct_Offset.md).


---

## TimeOffset

`enum` · `arrow_ipc::gen::Schema::TimeOffset`

Also reachable as `arrow::ipc::TimeOffset`, `arrow_ipc::TimeOffset`

```rust
enum TimeOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimeOffset.md).


---

## TimestampOffset

`enum` · `arrow_ipc::gen::Schema::TimestampOffset`

Also reachable as `arrow::ipc::TimestampOffset`, `arrow_ipc::TimestampOffset`

```rust
enum TimestampOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimestampOffset.md).


---

## UnionOffset

`enum` · `arrow_ipc::gen::Schema::UnionOffset`

Also reachable as `arrow::ipc::UnionOffset`, `arrow_ipc::UnionOffset`

```rust
enum UnionOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.UnionOffset.md).


---

## Utf8Offset

`enum` · `arrow_ipc::gen::Schema::Utf8Offset`

Also reachable as `arrow::ipc::Utf8Offset`, `arrow_ipc::Utf8Offset`

```rust
enum Utf8Offset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8Offset.md).


---

## Utf8ViewOffset

`enum` · `arrow_ipc::gen::Schema::Utf8ViewOffset`

Also reachable as `arrow::ipc::Utf8ViewOffset`, `arrow_ipc::Utf8ViewOffset`

```rust
enum Utf8ViewOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8ViewOffset.md).


---

## finish_schema_buffer

`function` · `arrow_ipc::gen::Schema::finish_schema_buffer`

Also reachable as `arrow::ipc::finish_schema_buffer`, `arrow_ipc::finish_schema_buffer`

```rust
fn finish_schema_buffer<'a, 'b, A: flatbuffers::Allocator + 'a>(fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>, root: flatbuffers::WIPOffset<Schema<'a>>)
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.finish_schema_buffer.md).


---

## finish_size_prefixed_schema_buffer

`function` · `arrow_ipc::gen::Schema::finish_size_prefixed_schema_buffer`

Also reachable as `arrow::ipc::finish_size_prefixed_schema_buffer`, `arrow_ipc::finish_size_prefixed_schema_buffer`

```rust
fn finish_size_prefixed_schema_buffer<'a, 'b, A: flatbuffers::Allocator + 'a>(fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>, root: flatbuffers::WIPOffset<Schema<'a>>)
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.finish_size_prefixed_schema_buffer.md).


---

## root_as_schema

`function` · `arrow_ipc::gen::Schema::root_as_schema`

Also reachable as `arrow::ipc::root_as_schema`, `arrow_ipc::root_as_schema`

```rust
fn root_as_schema(buf: &[u8]) -> Result<Schema<'_>, flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.root_as_schema.md).


Verifies that a buffer of bytes contains a `Schema`
and returns it.
Note that verification is still experimental and may not
catch every error, or be maximally performant. For the
previous, unchecked, behavior use
`root_as_schema_unchecked`.

---

## root_as_schema_unchecked

`function` · `arrow_ipc::gen::Schema::root_as_schema_unchecked`

Also reachable as `arrow::ipc::root_as_schema_unchecked`, `arrow_ipc::root_as_schema_unchecked`

```rust
unsafe fn root_as_schema_unchecked(buf: &[u8]) -> Schema<'_>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.root_as_schema_unchecked.md).


Assumes, without verification, that a buffer of bytes contains a Schema and returns it.
# Safety
Callers must trust the given bytes do indeed contain a valid `Schema`.

---

## root_as_schema_with_opts

`function` · `arrow_ipc::gen::Schema::root_as_schema_with_opts`

Also reachable as `arrow::ipc::root_as_schema_with_opts`, `arrow_ipc::root_as_schema_with_opts`

```rust
fn root_as_schema_with_opts<'b, 'o>(opts: &'o flatbuffers::VerifierOptions, buf: &'b [u8]) -> Result<Schema<'b>, flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.root_as_schema_with_opts.md).


Verifies, with the given options, that a buffer of bytes
contains a `Schema` and returns it.
Note that verification is still experimental and may not
catch every error, or be maximally performant. For the
previous, unchecked, behavior use
`root_as_schema_unchecked`.

---

## size_prefixed_root_as_schema

`function` · `arrow_ipc::gen::Schema::size_prefixed_root_as_schema`

Also reachable as `arrow::ipc::size_prefixed_root_as_schema`, `arrow_ipc::size_prefixed_root_as_schema`

```rust
fn size_prefixed_root_as_schema(buf: &[u8]) -> Result<Schema<'_>, flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.size_prefixed_root_as_schema.md).


Verifies that a buffer of bytes contains a size prefixed
`Schema` and returns it.
Note that verification is still experimental and may not
catch every error, or be maximally performant. For the
previous, unchecked, behavior use
`size_prefixed_root_as_schema_unchecked`.

---

## size_prefixed_root_as_schema_unchecked

`function` · `arrow_ipc::gen::Schema::size_prefixed_root_as_schema_unchecked`

Also reachable as `arrow::ipc::size_prefixed_root_as_schema_unchecked`, `arrow_ipc::size_prefixed_root_as_schema_unchecked`

```rust
unsafe fn size_prefixed_root_as_schema_unchecked(buf: &[u8]) -> Schema<'_>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.size_prefixed_root_as_schema_unchecked.md).


Assumes, without verification, that a buffer of bytes contains a size prefixed Schema and returns it.
# Safety
Callers must trust the given bytes do indeed contain a valid size prefixed `Schema`.

---

## size_prefixed_root_as_schema_with_opts

`function` · `arrow_ipc::gen::Schema::size_prefixed_root_as_schema_with_opts`

Also reachable as `arrow::ipc::size_prefixed_root_as_schema_with_opts`, `arrow_ipc::size_prefixed_root_as_schema_with_opts`

```rust
fn size_prefixed_root_as_schema_with_opts<'b, 'o>(opts: &'o flatbuffers::VerifierOptions, buf: &'b [u8]) -> Result<Schema<'b>, flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.size_prefixed_root_as_schema_with_opts.md).


Verifies, with the given verifier options, that a buffer of
bytes contains a size prefixed `Schema` and returns
it. Note that verification is still experimental and may not
catch every error, or be maximally performant. For the
previous, unchecked, behavior use
`root_as_schema_unchecked`.

---

## Binary

`struct` · `arrow_ipc::gen::Schema::Binary`

Also reachable as `arrow::ipc::Binary`, `arrow_ipc::Binary`

```rust
struct Binary<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args BinaryArgs) -> flatbuffers::WIPOffset<Binary<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Binary.md).


Opaque binary data

---

## BinaryArgs

`struct` · `arrow_ipc::gen::Schema::BinaryArgs`

Also reachable as `arrow::ipc::BinaryArgs`, `arrow_ipc::BinaryArgs`

```rust
struct BinaryArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryArgs.md).


---

## BinaryBuilder

`struct` · `arrow_ipc::gen::Schema::BinaryBuilder`

Also reachable as `arrow::ipc::BinaryBuilder`, `arrow_ipc::BinaryBuilder`

```rust
struct BinaryBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<Binary<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> BinaryBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryBuilder.md).


---

## BinaryView

`struct` · `arrow_ipc::gen::Schema::BinaryView`

Also reachable as `arrow::ipc::BinaryView`, `arrow_ipc::BinaryView`

```rust
struct BinaryView<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args BinaryViewArgs) -> flatbuffers::WIPOffset<BinaryView<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryView.md).


Logically the same as Binary, but the internal representation uses a view
struct that contains the string length and either the string's entire data
inline (for small strings) or an inlined prefix, an index of another buffer,
and an offset pointing to a slice in that buffer (for non-small strings).

Since it uses a variable number of data buffers, each Field with this type
must have a corresponding entry in `variadicBufferCounts`.

---

## BinaryViewArgs

`struct` · `arrow_ipc::gen::Schema::BinaryViewArgs`

Also reachable as `arrow::ipc::BinaryViewArgs`, `arrow_ipc::BinaryViewArgs`

```rust
struct BinaryViewArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryViewArgs.md).


---

## BinaryViewBuilder

`struct` · `arrow_ipc::gen::Schema::BinaryViewBuilder`

Also reachable as `arrow::ipc::BinaryViewBuilder`, `arrow_ipc::BinaryViewBuilder`

```rust
struct BinaryViewBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<BinaryView<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> BinaryViewBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BinaryViewBuilder.md).


---

## Bool

`struct` · `arrow_ipc::gen::Schema::Bool`

Also reachable as `arrow::ipc::Bool`, `arrow_ipc::Bool`

```rust
struct Bool<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args BoolArgs) -> flatbuffers::WIPOffset<Bool<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Bool.md).


---

## BoolArgs

`struct` · `arrow_ipc::gen::Schema::BoolArgs`

Also reachable as `arrow::ipc::BoolArgs`, `arrow_ipc::BoolArgs`

```rust
struct BoolArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BoolArgs.md).


---

## BoolBuilder

`struct` · `arrow_ipc::gen::Schema::BoolBuilder`

Also reachable as `arrow::ipc::BoolBuilder`, `arrow_ipc::BoolBuilder`

```rust
struct BoolBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<Bool<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> BoolBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.BoolBuilder.md).


---

## Buffer

`struct` · `arrow_ipc::gen::Schema::Buffer`

Also reachable as `arrow::ipc::Buffer`, `arrow_ipc::Buffer`

```rust
struct Buffer
```

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, PartialEq, StructuralPartialEq

**Methods** (5)

```rust
fn length(&self) -> i64
fn new(offset: i64, length: i64) -> Self
fn offset(&self) -> i64
fn set_length(&mut self, x: i64)
fn set_offset(&mut self, x: i64)
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
fn alignment() -> flatbuffers::PushAlignment
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Buffer.md).


----------------------------------------------------------------------
A Buffer represents a single contiguous memory segment

---

## Date

`struct` · `arrow_ipc::gen::Schema::Date`

Also reachable as `arrow::ipc::Date`, `arrow_ipc::Date`

```rust
struct Date<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args DateArgs) -> flatbuffers::WIPOffset<Date<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn unit(&self) -> DateUnit
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Date.md).


Date is either a 32-bit or 64-bit signed integer type representing an
elapsed time since UNIX epoch (1970-01-01), stored in either of two units:

* Milliseconds (64 bits) indicating UNIX time elapsed since the epoch (no
  leap seconds), where the values are evenly divisible by 86400000
* Days (32 bits) since the UNIX epoch

---

## DateArgs

`struct` · `arrow_ipc::gen::Schema::DateArgs`

Also reachable as `arrow::ipc::DateArgs`, `arrow_ipc::DateArgs`

```rust
struct DateArgs
```

**Fields**: `unit`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DateArgs.md).


---

## DateBuilder

`struct` · `arrow_ipc::gen::Schema::DateBuilder`

Also reachable as `arrow::ipc::DateBuilder`, `arrow_ipc::DateBuilder`

```rust
struct DateBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_unit(&mut self, unit: DateUnit)
fn finish(self) -> flatbuffers::WIPOffset<Date<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> DateBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DateBuilder.md).


---

## DateUnit

`struct` · `arrow_ipc::gen::Schema::DateUnit`

Also reachable as `arrow::ipc::DateUnit`, `arrow_ipc::DateUnit`

```rust
struct DateUnit
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DateUnit.md).


---

## Decimal

`struct` · `arrow_ipc::gen::Schema::Decimal`

Also reachable as `arrow::ipc::Decimal`, `arrow_ipc::Decimal`

```rust
struct Decimal<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (5)

```rust
fn bitWidth(&self) -> i32
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args DecimalArgs) -> flatbuffers::WIPOffset<Decimal<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn precision(&self) -> i32
fn scale(&self) -> i32
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Decimal.md).


Exact decimal value represented as an integer value in two's
complement. Currently only 128-bit (16-byte) and 256-bit (32-byte) integers
are used. The representation uses the endianness indicated
in the Schema.

---

## DecimalArgs

`struct` · `arrow_ipc::gen::Schema::DecimalArgs`

Also reachable as `arrow::ipc::DecimalArgs`, `arrow_ipc::DecimalArgs`

```rust
struct DecimalArgs
```

**Fields**: `precision`, `scale`, `bitWidth`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DecimalArgs.md).


---

## DecimalBuilder

`struct` · `arrow_ipc::gen::Schema::DecimalBuilder`

Also reachable as `arrow::ipc::DecimalBuilder`, `arrow_ipc::DecimalBuilder`

```rust
struct DecimalBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (5)

```rust
fn add_bitWidth(&mut self, bitWidth: i32)
fn add_precision(&mut self, precision: i32)
fn add_scale(&mut self, scale: i32)
fn finish(self) -> flatbuffers::WIPOffset<Decimal<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> DecimalBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DecimalBuilder.md).


---

## DictionaryEncoding

`struct` · `arrow_ipc::gen::Schema::DictionaryEncoding`

Also reachable as `arrow::ipc::DictionaryEncoding`, `arrow_ipc::DictionaryEncoding`

```rust
struct DictionaryEncoding<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (6)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args DictionaryEncodingArgs<'args>) -> flatbuffers::WIPOffset<DictionaryEncoding<'bldr>>
fn dictionaryKind(&self) -> DictionaryKind
fn id(&self) -> i64
fn indexType(&self) -> Option<Int<'a>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn isOrdered(&self) -> bool
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DictionaryEncoding.md).


---

## DictionaryEncodingArgs

`struct` · `arrow_ipc::gen::Schema::DictionaryEncodingArgs`

Also reachable as `arrow::ipc::DictionaryEncodingArgs`, `arrow_ipc::DictionaryEncodingArgs`

```rust
struct DictionaryEncodingArgs<'a>
```

**Fields**: `id`, `indexType`, `isOrdered`, `dictionaryKind`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DictionaryEncodingArgs.md).


---

## DictionaryEncodingBuilder

`struct` · `arrow_ipc::gen::Schema::DictionaryEncodingBuilder`

Also reachable as `arrow::ipc::DictionaryEncodingBuilder`, `arrow_ipc::DictionaryEncodingBuilder`

```rust
struct DictionaryEncodingBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (6)

```rust
fn add_dictionaryKind(&mut self, dictionaryKind: DictionaryKind)
fn add_id(&mut self, id: i64)
fn add_indexType(&mut self, indexType: flatbuffers::WIPOffset<Int<'b>>)
fn add_isOrdered(&mut self, isOrdered: bool)
fn finish(self) -> flatbuffers::WIPOffset<DictionaryEncoding<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> DictionaryEncodingBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DictionaryEncodingBuilder.md).


---

## DictionaryKind

`struct` · `arrow_ipc::gen::Schema::DictionaryKind`

Also reachable as `arrow::ipc::DictionaryKind`, `arrow_ipc::DictionaryKind`

```rust
struct DictionaryKind
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DictionaryKind.md).


----------------------------------------------------------------------
Dictionary encoding metadata
Maintained for forwards compatibility, in the future
Dictionaries might be explicit maps between integers and values
allowing for non-contiguous index values

---

## Duration

`struct` · `arrow_ipc::gen::Schema::Duration`

Also reachable as `arrow::ipc::Duration`, `arrow_ipc::Duration`

```rust
struct Duration<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args DurationArgs) -> flatbuffers::WIPOffset<Duration<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn unit(&self) -> TimeUnit
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Duration.md).


---

## DurationArgs

`struct` · `arrow_ipc::gen::Schema::DurationArgs`

Also reachable as `arrow::ipc::DurationArgs`, `arrow_ipc::DurationArgs`

```rust
struct DurationArgs
```

**Fields**: `unit`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DurationArgs.md).


---

## DurationBuilder

`struct` · `arrow_ipc::gen::Schema::DurationBuilder`

Also reachable as `arrow::ipc::DurationBuilder`, `arrow_ipc::DurationBuilder`

```rust
struct DurationBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_unit(&mut self, unit: TimeUnit)
fn finish(self) -> flatbuffers::WIPOffset<Duration<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> DurationBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.DurationBuilder.md).


---

## Endianness

`struct` · `arrow_ipc::gen::Schema::Endianness`

Also reachable as `arrow::ipc::Endianness`, `arrow_ipc::Endianness`

```rust
struct Endianness
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (2)

```rust
fn equals_to_target_endianness(self) -> bool
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Endianness.md).


----------------------------------------------------------------------
Endianness of the platform producing the data

---

## Feature

`struct` · `arrow_ipc::gen::Schema::Feature`

Also reachable as `arrow::ipc::Feature`, `arrow_ipc::Feature`

```rust
struct Feature
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i64) -> Self
fn to_little_endian(self) -> i64
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Feature.md).


Represents Arrow Features that might not have full support
within implementations. This is intended to be used in
two scenarios:
 1.  A mechanism for readers of Arrow Streams
     and files to understand that the stream or file makes
     use of a feature that isn't supported or unknown to
     the implementation (and therefore can meet the Arrow
     forward compatibility guarantees).
 2.  A means of negotiating between a client and server
     what features a stream is allowed to use. The enums
     values here are intented to represent higher level
     features, additional details maybe negotiated
     with key-value pairs specific to the protocol.

Enums added to this list should be assigned power-of-two values
to facilitate exchanging and comparing bitmaps for supported
features.

---

## Field

`struct` · `arrow_ipc::gen::Schema::Field`

Also reachable as `arrow::ipc::Field`, `arrow_ipc::Field`

```rust
struct Field<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (35)

```rust
fn children(&self) -> Option<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<Field<'a>>>>
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args FieldArgs<'args>) -> flatbuffers::WIPOffset<Field<'bldr>>
fn custom_metadata(&self) -> Option<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<KeyValue<'a>>>>
fn dictionary(&self) -> Option<DictionaryEncoding<'a>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn name(&self) -> Option<&'a str>
fn nullable(&self) -> bool
fn type_(&self) -> Option<flatbuffers::Table<'a>>
fn type_as_binary(&self) -> Option<Binary<'a>>
fn type_as_binary_view(&self) -> Option<BinaryView<'a>>
fn type_as_bool(&self) -> Option<Bool<'a>>
fn type_as_date(&self) -> Option<Date<'a>>
fn type_as_decimal(&self) -> Option<Decimal<'a>>
fn type_as_duration(&self) -> Option<Duration<'a>>
fn type_as_fixed_size_binary(&self) -> Option<FixedSizeBinary<'a>>
fn type_as_fixed_size_list(&self) -> Option<FixedSizeList<'a>>
fn type_as_floating_point(&self) -> Option<FloatingPoint<'a>>
fn type_as_int(&self) -> Option<Int<'a>>
fn type_as_interval(&self) -> Option<Interval<'a>>
fn type_as_large_binary(&self) -> Option<LargeBinary<'a>>
fn type_as_large_list(&self) -> Option<LargeList<'a>>
fn type_as_large_list_view(&self) -> Option<LargeListView<'a>>
fn type_as_large_utf_8(&self) -> Option<LargeUtf8<'a>>
fn type_as_list(&self) -> Option<List<'a>>
fn type_as_list_view(&self) -> Option<ListView<'a>>
fn type_as_map(&self) -> Option<Map<'a>>
fn type_as_null(&self) -> Option<Null<'a>>
fn type_as_run_end_encoded(&self) -> Option<RunEndEncoded<'a>>
fn type_as_struct_(&self) -> Option<Struct_<'a>>
fn type_as_time(&self) -> Option<Time<'a>>
fn type_as_timestamp(&self) -> Option<Timestamp<'a>>
fn type_as_union(&self) -> Option<Union<'a>>
fn type_as_utf_8(&self) -> Option<Utf8<'a>>
fn type_as_utf_8_view(&self) -> Option<Utf8View<'a>>
fn type_type(&self) -> Type
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Field.md).


----------------------------------------------------------------------
A field represents a named column in a record / row batch or child of a
nested type.

---

## FieldArgs

`struct` · `arrow_ipc::gen::Schema::FieldArgs`

Also reachable as `arrow::ipc::FieldArgs`, `arrow_ipc::FieldArgs`

```rust
struct FieldArgs<'a>
```

**Fields**: `name`, `nullable`, `type_type`, `type_`, `dictionary`, `children`, `custom_metadata`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FieldArgs.md).


---

## FieldBuilder

`struct` · `arrow_ipc::gen::Schema::FieldBuilder`

Also reachable as `arrow::ipc::FieldBuilder`, `arrow_ipc::FieldBuilder`

```rust
struct FieldBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (9)

```rust
fn add_children(&mut self, children: flatbuffers::WIPOffset<flatbuffers::Vector<'b, flatbuffers::ForwardsUOffset<Field<'b>>>>)
fn add_custom_metadata(&mut self, custom_metadata: flatbuffers::WIPOffset<flatbuffers::Vector<'b, flatbuffers::ForwardsUOffset<KeyValue<'b>>>>)
fn add_dictionary(&mut self, dictionary: flatbuffers::WIPOffset<DictionaryEncoding<'b>>)
fn add_name(&mut self, name: flatbuffers::WIPOffset<&'b str>)
fn add_nullable(&mut self, nullable: bool)
fn add_type_(&mut self, type_: flatbuffers::WIPOffset<flatbuffers::UnionWIPOffset>)
fn add_type_type(&mut self, type_type: Type)
fn finish(self) -> flatbuffers::WIPOffset<Field<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> FieldBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FieldBuilder.md).


---

## FixedSizeBinary

`struct` · `arrow_ipc::gen::Schema::FixedSizeBinary`

Also reachable as `arrow::ipc::FixedSizeBinary`, `arrow_ipc::FixedSizeBinary`

```rust
struct FixedSizeBinary<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn byteWidth(&self) -> i32
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args FixedSizeBinaryArgs) -> flatbuffers::WIPOffset<FixedSizeBinary<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeBinary.md).


---

## FixedSizeBinaryArgs

`struct` · `arrow_ipc::gen::Schema::FixedSizeBinaryArgs`

Also reachable as `arrow::ipc::FixedSizeBinaryArgs`, `arrow_ipc::FixedSizeBinaryArgs`

```rust
struct FixedSizeBinaryArgs
```

**Fields**: `byteWidth`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeBinaryArgs.md).


---

## FixedSizeBinaryBuilder

`struct` · `arrow_ipc::gen::Schema::FixedSizeBinaryBuilder`

Also reachable as `arrow::ipc::FixedSizeBinaryBuilder`, `arrow_ipc::FixedSizeBinaryBuilder`

```rust
struct FixedSizeBinaryBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_byteWidth(&mut self, byteWidth: i32)
fn finish(self) -> flatbuffers::WIPOffset<FixedSizeBinary<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> FixedSizeBinaryBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeBinaryBuilder.md).


---

## FixedSizeList

`struct` · `arrow_ipc::gen::Schema::FixedSizeList`

Also reachable as `arrow::ipc::FixedSizeList`, `arrow_ipc::FixedSizeList`

```rust
struct FixedSizeList<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args FixedSizeListArgs) -> flatbuffers::WIPOffset<FixedSizeList<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn listSize(&self) -> i32
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeList.md).


---

## FixedSizeListArgs

`struct` · `arrow_ipc::gen::Schema::FixedSizeListArgs`

Also reachable as `arrow::ipc::FixedSizeListArgs`, `arrow_ipc::FixedSizeListArgs`

```rust
struct FixedSizeListArgs
```

**Fields**: `listSize`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeListArgs.md).


---

## FixedSizeListBuilder

`struct` · `arrow_ipc::gen::Schema::FixedSizeListBuilder`

Also reachable as `arrow::ipc::FixedSizeListBuilder`, `arrow_ipc::FixedSizeListBuilder`

```rust
struct FixedSizeListBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_listSize(&mut self, listSize: i32)
fn finish(self) -> flatbuffers::WIPOffset<FixedSizeList<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> FixedSizeListBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FixedSizeListBuilder.md).


---

## FloatingPoint

`struct` · `arrow_ipc::gen::Schema::FloatingPoint`

Also reachable as `arrow::ipc::FloatingPoint`, `arrow_ipc::FloatingPoint`

```rust
struct FloatingPoint<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args FloatingPointArgs) -> flatbuffers::WIPOffset<FloatingPoint<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn precision(&self) -> Precision
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FloatingPoint.md).


---

## FloatingPointArgs

`struct` · `arrow_ipc::gen::Schema::FloatingPointArgs`

Also reachable as `arrow::ipc::FloatingPointArgs`, `arrow_ipc::FloatingPointArgs`

```rust
struct FloatingPointArgs
```

**Fields**: `precision`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FloatingPointArgs.md).


---

## FloatingPointBuilder

`struct` · `arrow_ipc::gen::Schema::FloatingPointBuilder`

Also reachable as `arrow::ipc::FloatingPointBuilder`, `arrow_ipc::FloatingPointBuilder`

```rust
struct FloatingPointBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_precision(&mut self, precision: Precision)
fn finish(self) -> flatbuffers::WIPOffset<FloatingPoint<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> FloatingPointBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.FloatingPointBuilder.md).


---

## Int

`struct` · `arrow_ipc::gen::Schema::Int`

Also reachable as `arrow::ipc::Int`, `arrow_ipc::Int`

```rust
struct Int<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (4)

```rust
fn bitWidth(&self) -> i32
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args IntArgs) -> flatbuffers::WIPOffset<Int<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn is_signed(&self) -> bool
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Int.md).


---

## IntArgs

`struct` · `arrow_ipc::gen::Schema::IntArgs`

Also reachable as `arrow::ipc::IntArgs`, `arrow_ipc::IntArgs`

```rust
struct IntArgs
```

**Fields**: `bitWidth`, `is_signed`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntArgs.md).


---

## IntBuilder

`struct` · `arrow_ipc::gen::Schema::IntBuilder`

Also reachable as `arrow::ipc::IntBuilder`, `arrow_ipc::IntBuilder`

```rust
struct IntBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (4)

```rust
fn add_bitWidth(&mut self, bitWidth: i32)
fn add_is_signed(&mut self, is_signed: bool)
fn finish(self) -> flatbuffers::WIPOffset<Int<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> IntBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntBuilder.md).


---

## Interval

`struct` · `arrow_ipc::gen::Schema::Interval`

Also reachable as `arrow::ipc::Interval`, `arrow_ipc::Interval`

```rust
struct Interval<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args IntervalArgs) -> flatbuffers::WIPOffset<Interval<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn unit(&self) -> IntervalUnit
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Interval.md).


---

## IntervalArgs

`struct` · `arrow_ipc::gen::Schema::IntervalArgs`

Also reachable as `arrow::ipc::IntervalArgs`, `arrow_ipc::IntervalArgs`

```rust
struct IntervalArgs
```

**Fields**: `unit`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntervalArgs.md).


---

## IntervalBuilder

`struct` · `arrow_ipc::gen::Schema::IntervalBuilder`

Also reachable as `arrow::ipc::IntervalBuilder`, `arrow_ipc::IntervalBuilder`

```rust
struct IntervalBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_unit(&mut self, unit: IntervalUnit)
fn finish(self) -> flatbuffers::WIPOffset<Interval<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> IntervalBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntervalBuilder.md).


---

## IntervalUnit

`struct` · `arrow_ipc::gen::Schema::IntervalUnit`

Also reachable as `arrow::ipc::IntervalUnit`, `arrow_ipc::IntervalUnit`

```rust
struct IntervalUnit
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.IntervalUnit.md).


---

## KeyValue

`struct` · `arrow_ipc::gen::Schema::KeyValue`

Also reachable as `arrow::ipc::KeyValue`, `arrow_ipc::KeyValue`

```rust
struct KeyValue<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (4)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args KeyValueArgs<'args>) -> flatbuffers::WIPOffset<KeyValue<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn key(&self) -> Option<&'a str>
fn value(&self) -> Option<&'a str>
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.KeyValue.md).


----------------------------------------------------------------------
user defined key value pairs to add custom metadata to arrow
key namespacing is the responsibility of the user

---

## KeyValueArgs

`struct` · `arrow_ipc::gen::Schema::KeyValueArgs`

Also reachable as `arrow::ipc::KeyValueArgs`, `arrow_ipc::KeyValueArgs`

```rust
struct KeyValueArgs<'a>
```

**Fields**: `key`, `value`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.KeyValueArgs.md).


---

## KeyValueBuilder

`struct` · `arrow_ipc::gen::Schema::KeyValueBuilder`

Also reachable as `arrow::ipc::KeyValueBuilder`, `arrow_ipc::KeyValueBuilder`

```rust
struct KeyValueBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (4)

```rust
fn add_key(&mut self, key: flatbuffers::WIPOffset<&'b str>)
fn add_value(&mut self, value: flatbuffers::WIPOffset<&'b str>)
fn finish(self) -> flatbuffers::WIPOffset<KeyValue<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> KeyValueBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.KeyValueBuilder.md).


---

## LargeBinary

`struct` · `arrow_ipc::gen::Schema::LargeBinary`

Also reachable as `arrow::ipc::LargeBinary`, `arrow_ipc::LargeBinary`

```rust
struct LargeBinary<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args LargeBinaryArgs) -> flatbuffers::WIPOffset<LargeBinary<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeBinary.md).


Same as Binary, but with 64-bit offsets, allowing to represent
extremely large data values.

---

## LargeBinaryArgs

`struct` · `arrow_ipc::gen::Schema::LargeBinaryArgs`

Also reachable as `arrow::ipc::LargeBinaryArgs`, `arrow_ipc::LargeBinaryArgs`

```rust
struct LargeBinaryArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeBinaryArgs.md).


---

## LargeBinaryBuilder

`struct` · `arrow_ipc::gen::Schema::LargeBinaryBuilder`

Also reachable as `arrow::ipc::LargeBinaryBuilder`, `arrow_ipc::LargeBinaryBuilder`

```rust
struct LargeBinaryBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<LargeBinary<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> LargeBinaryBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeBinaryBuilder.md).


---

## LargeList

`struct` · `arrow_ipc::gen::Schema::LargeList`

Also reachable as `arrow::ipc::LargeList`, `arrow_ipc::LargeList`

```rust
struct LargeList<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args LargeListArgs) -> flatbuffers::WIPOffset<LargeList<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeList.md).


Same as List, but with 64-bit offsets, allowing to represent
extremely large data values.

---

## LargeListArgs

`struct` · `arrow_ipc::gen::Schema::LargeListArgs`

Also reachable as `arrow::ipc::LargeListArgs`, `arrow_ipc::LargeListArgs`

```rust
struct LargeListArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListArgs.md).


---

## LargeListBuilder

`struct` · `arrow_ipc::gen::Schema::LargeListBuilder`

Also reachable as `arrow::ipc::LargeListBuilder`, `arrow_ipc::LargeListBuilder`

```rust
struct LargeListBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<LargeList<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> LargeListBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListBuilder.md).


---

## LargeListView

`struct` · `arrow_ipc::gen::Schema::LargeListView`

Also reachable as `arrow::ipc::LargeListView`, `arrow_ipc::LargeListView`

```rust
struct LargeListView<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args LargeListViewArgs) -> flatbuffers::WIPOffset<LargeListView<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListView.md).


Same as ListView, but with 64-bit offsets and sizes, allowing to represent
extremely large data values.

---

## LargeListViewArgs

`struct` · `arrow_ipc::gen::Schema::LargeListViewArgs`

Also reachable as `arrow::ipc::LargeListViewArgs`, `arrow_ipc::LargeListViewArgs`

```rust
struct LargeListViewArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListViewArgs.md).


---

## LargeListViewBuilder

`struct` · `arrow_ipc::gen::Schema::LargeListViewBuilder`

Also reachable as `arrow::ipc::LargeListViewBuilder`, `arrow_ipc::LargeListViewBuilder`

```rust
struct LargeListViewBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<LargeListView<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> LargeListViewBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeListViewBuilder.md).


---

## LargeUtf8

`struct` · `arrow_ipc::gen::Schema::LargeUtf8`

Also reachable as `arrow::ipc::LargeUtf8`, `arrow_ipc::LargeUtf8`

```rust
struct LargeUtf8<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args LargeUtf8Args) -> flatbuffers::WIPOffset<LargeUtf8<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeUtf8.md).


Same as Utf8, but with 64-bit offsets, allowing to represent
extremely large data values.

---

## LargeUtf8Args

`struct` · `arrow_ipc::gen::Schema::LargeUtf8Args`

Also reachable as `arrow::ipc::LargeUtf8Args`, `arrow_ipc::LargeUtf8Args`

```rust
struct LargeUtf8Args
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeUtf8Args.md).


---

## LargeUtf8Builder

`struct` · `arrow_ipc::gen::Schema::LargeUtf8Builder`

Also reachable as `arrow::ipc::LargeUtf8Builder`, `arrow_ipc::LargeUtf8Builder`

```rust
struct LargeUtf8Builder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<LargeUtf8<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> LargeUtf8Builder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.LargeUtf8Builder.md).


---

## List

`struct` · `arrow_ipc::gen::Schema::List`

Also reachable as `arrow::ipc::List`, `arrow_ipc::List`

```rust
struct List<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args ListArgs) -> flatbuffers::WIPOffset<List<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.List.md).


---

## ListArgs

`struct` · `arrow_ipc::gen::Schema::ListArgs`

Also reachable as `arrow::ipc::ListArgs`, `arrow_ipc::ListArgs`

```rust
struct ListArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListArgs.md).


---

## ListBuilder

`struct` · `arrow_ipc::gen::Schema::ListBuilder`

Also reachable as `arrow::ipc::ListBuilder`, `arrow_ipc::ListBuilder`

```rust
struct ListBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<List<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> ListBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListBuilder.md).


---

## ListView

`struct` · `arrow_ipc::gen::Schema::ListView`

Also reachable as `arrow::ipc::ListView`, `arrow_ipc::ListView`

```rust
struct ListView<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args ListViewArgs) -> flatbuffers::WIPOffset<ListView<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListView.md).


Represents the same logical types that List can, but contains offsets and
sizes allowing for writes in any order and sharing of child values among
list values.

---

## ListViewArgs

`struct` · `arrow_ipc::gen::Schema::ListViewArgs`

Also reachable as `arrow::ipc::ListViewArgs`, `arrow_ipc::ListViewArgs`

```rust
struct ListViewArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListViewArgs.md).


---

## ListViewBuilder

`struct` · `arrow_ipc::gen::Schema::ListViewBuilder`

Also reachable as `arrow::ipc::ListViewBuilder`, `arrow_ipc::ListViewBuilder`

```rust
struct ListViewBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<ListView<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> ListViewBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.ListViewBuilder.md).


---

## Map

`struct` · `arrow_ipc::gen::Schema::Map`

Also reachable as `arrow::ipc::Map`, `arrow_ipc::Map`

```rust
struct Map<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (3)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args MapArgs) -> flatbuffers::WIPOffset<Map<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn keysSorted(&self) -> bool
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Map.md).


A Map is a logical nested type that is represented as

List<entries: Struct<key: K, value: V>>

In this layout, the keys and values are each respectively contiguous. We do
not constrain the key and value types, so the application is responsible
for ensuring that the keys are hashable and unique. Whether the keys are sorted
may be set in the metadata for this field.

In a field with Map type, the field has a child Struct field, which then
has two children: key type and the second the value type. The names of the
child fields may be respectively "entries", "key", and "value", but this is
not enforced.

Map
```text
  - child[0] entries: Struct
    - child[0] key: K
    - child[1] value: V
```
Neither the "entries" field nor the "key" field may be nullable.

The metadata is structured so that Arrow systems without special handling
for Map can make Map an alias for List. The "layout" attribute for the Map
field must have the same contents as a List.

---

## MapArgs

`struct` · `arrow_ipc::gen::Schema::MapArgs`

Also reachable as `arrow::ipc::MapArgs`, `arrow_ipc::MapArgs`

```rust
struct MapArgs
```

**Fields**: `keysSorted`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.MapArgs.md).


---

## MapBuilder

`struct` · `arrow_ipc::gen::Schema::MapBuilder`

Also reachable as `arrow::ipc::MapBuilder`, `arrow_ipc::MapBuilder`

```rust
struct MapBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (3)

```rust
fn add_keysSorted(&mut self, keysSorted: bool)
fn finish(self) -> flatbuffers::WIPOffset<Map<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> MapBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.MapBuilder.md).


---

## MetadataVersion

`struct` · `arrow_ipc::gen::Schema::MetadataVersion`

Also reachable as `arrow::ipc::MetadataVersion`, `arrow_ipc::MetadataVersion`

```rust
struct MetadataVersion
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.MetadataVersion.md).


---

## Null

`struct` · `arrow_ipc::gen::Schema::Null`

Also reachable as `arrow::ipc::Null`, `arrow_ipc::Null`

```rust
struct Null<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args NullArgs) -> flatbuffers::WIPOffset<Null<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Null.md).


These are stored in the flatbuffer in the Type union below

---

## NullArgs

`struct` · `arrow_ipc::gen::Schema::NullArgs`

Also reachable as `arrow::ipc::NullArgs`, `arrow_ipc::NullArgs`

```rust
struct NullArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.NullArgs.md).


---

## NullBuilder

`struct` · `arrow_ipc::gen::Schema::NullBuilder`

Also reachable as `arrow::ipc::NullBuilder`, `arrow_ipc::NullBuilder`

```rust
struct NullBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<Null<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> NullBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.NullBuilder.md).


---

## Precision

`struct` · `arrow_ipc::gen::Schema::Precision`

Also reachable as `arrow::ipc::Precision`, `arrow_ipc::Precision`

```rust
struct Precision
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Precision.md).


---

## RunEndEncoded

`struct` · `arrow_ipc::gen::Schema::RunEndEncoded`

Also reachable as `arrow::ipc::RunEndEncoded`, `arrow_ipc::RunEndEncoded`

```rust
struct RunEndEncoded<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args RunEndEncodedArgs) -> flatbuffers::WIPOffset<RunEndEncoded<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.RunEndEncoded.md).


Contains two child arrays, run_ends and values.
The run_ends child array must be a 16/32/64-bit integer array
which encodes the indices at which the run with the value in
each corresponding index in the values child array ends.
Like list/struct types, the value array can be of any type.

---

## RunEndEncodedArgs

`struct` · `arrow_ipc::gen::Schema::RunEndEncodedArgs`

Also reachable as `arrow::ipc::RunEndEncodedArgs`, `arrow_ipc::RunEndEncodedArgs`

```rust
struct RunEndEncodedArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.RunEndEncodedArgs.md).


---

## RunEndEncodedBuilder

`struct` · `arrow_ipc::gen::Schema::RunEndEncodedBuilder`

Also reachable as `arrow::ipc::RunEndEncodedBuilder`, `arrow_ipc::RunEndEncodedBuilder`

```rust
struct RunEndEncodedBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<RunEndEncoded<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> RunEndEncodedBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.RunEndEncodedBuilder.md).


---

## Schema

`struct` · `arrow_ipc::gen::Schema::Schema`

Also reachable as `arrow::ipc::Schema`, `arrow_ipc::Schema`

```rust
struct Schema<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (6)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args SchemaArgs<'args>) -> flatbuffers::WIPOffset<Schema<'bldr>>
fn custom_metadata(&self) -> Option<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<KeyValue<'a>>>>
fn endianness(&self) -> Endianness
fn features(&self) -> Option<flatbuffers::Vector<'a, Feature>>
fn fields(&self) -> Option<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<Field<'a>>>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Schema.md).


----------------------------------------------------------------------
A Schema describes the columns in a row batch

---

## SchemaArgs

`struct` · `arrow_ipc::gen::Schema::SchemaArgs`

Also reachable as `arrow::ipc::SchemaArgs`, `arrow_ipc::SchemaArgs`

```rust
struct SchemaArgs<'a>
```

**Fields**: `endianness`, `fields`, `custom_metadata`, `features`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.SchemaArgs.md).


---

## SchemaBuilder

`struct` · `arrow_ipc::gen::Schema::SchemaBuilder`

Also reachable as `arrow::ipc::SchemaBuilder`, `arrow_ipc::SchemaBuilder`

```rust
struct SchemaBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (6)

```rust
fn add_custom_metadata(&mut self, custom_metadata: flatbuffers::WIPOffset<flatbuffers::Vector<'b, flatbuffers::ForwardsUOffset<KeyValue<'b>>>>)
fn add_endianness(&mut self, endianness: Endianness)
fn add_features(&mut self, features: flatbuffers::WIPOffset<flatbuffers::Vector<'b, Feature>>)
fn add_fields(&mut self, fields: flatbuffers::WIPOffset<flatbuffers::Vector<'b, flatbuffers::ForwardsUOffset<Field<'b>>>>)
fn finish(self) -> flatbuffers::WIPOffset<Schema<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> SchemaBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.SchemaBuilder.md).


---

## Struct_

`struct` · `arrow_ipc::gen::Schema::Struct_`

Also reachable as `arrow::ipc::Struct_`, `arrow_ipc::Struct_`

```rust
struct Struct_<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args Struct_Args) -> flatbuffers::WIPOffset<Struct_<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Struct_.md).


A Struct_ in the flatbuffer metadata is the same as an Arrow Struct
(according to the physical memory layout). We used Struct_ here as
Struct is a reserved word in Flatbuffers

---

## Struct_Args

`struct` · `arrow_ipc::gen::Schema::Struct_Args`

Also reachable as `arrow::ipc::Struct_Args`, `arrow_ipc::Struct_Args`

```rust
struct Struct_Args
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Struct_Args.md).


---

## Struct_Builder

`struct` · `arrow_ipc::gen::Schema::Struct_Builder`

Also reachable as `arrow::ipc::Struct_Builder`, `arrow_ipc::Struct_Builder`

```rust
struct Struct_Builder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<Struct_<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> Struct_Builder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Struct_Builder.md).


---

## Time

`struct` · `arrow_ipc::gen::Schema::Time`

Also reachable as `arrow::ipc::Time`, `arrow_ipc::Time`

```rust
struct Time<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (4)

```rust
fn bitWidth(&self) -> i32
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args TimeArgs) -> flatbuffers::WIPOffset<Time<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn unit(&self) -> TimeUnit
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Time.md).


Time is either a 32-bit or 64-bit signed integer type representing an
elapsed time since midnight, stored in either of four units: seconds,
milliseconds, microseconds or nanoseconds.

The integer `bitWidth` depends on the `unit` and must be one of the following:
* SECOND and MILLISECOND: 32 bits
* MICROSECOND and NANOSECOND: 64 bits

The allowed values are between 0 (inclusive) and 86400 (=24*60*60) seconds
(exclusive), adjusted for the time unit (for example, up to 86400000
exclusive for the MILLISECOND unit).
This definition doesn't allow for leap seconds. Time values from
measurements with leap seconds will need to be corrected when ingesting
into Arrow (for example by replacing the value 86400 with 86399).

---

## TimeArgs

`struct` · `arrow_ipc::gen::Schema::TimeArgs`

Also reachable as `arrow::ipc::TimeArgs`, `arrow_ipc::TimeArgs`

```rust
struct TimeArgs
```

**Fields**: `unit`, `bitWidth`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimeArgs.md).


---

## TimeBuilder

`struct` · `arrow_ipc::gen::Schema::TimeBuilder`

Also reachable as `arrow::ipc::TimeBuilder`, `arrow_ipc::TimeBuilder`

```rust
struct TimeBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (4)

```rust
fn add_bitWidth(&mut self, bitWidth: i32)
fn add_unit(&mut self, unit: TimeUnit)
fn finish(self) -> flatbuffers::WIPOffset<Time<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> TimeBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimeBuilder.md).


---

## TimeUnit

`struct` · `arrow_ipc::gen::Schema::TimeUnit`

Also reachable as `arrow::ipc::TimeUnit`, `arrow_ipc::TimeUnit`

```rust
struct TimeUnit
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimeUnit.md).


---

## Timestamp

`struct` · `arrow_ipc::gen::Schema::Timestamp`

Also reachable as `arrow::ipc::Timestamp`, `arrow_ipc::Timestamp`

```rust
struct Timestamp<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (4)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args TimestampArgs<'args>) -> flatbuffers::WIPOffset<Timestamp<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn timezone(&self) -> Option<&'a str>
fn unit(&self) -> TimeUnit
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Timestamp.md).


Timestamp is a 64-bit signed integer representing an elapsed time since a
fixed epoch, stored in either of four units: seconds, milliseconds,
microseconds or nanoseconds, and is optionally annotated with a timezone.

Timestamp values do not include any leap seconds (in other words, all
days are considered 86400 seconds long).

Timestamps with a non-empty timezone
------------------------------------

If a Timestamp column has a non-empty timezone value, its epoch is
1970-01-01 00:00:00 (January 1st 1970, midnight) in the *UTC* timezone
(the Unix epoch), regardless of the Timestamp's own timezone.

Therefore, timestamp values with a non-empty timezone correspond to
physical points in time together with some additional information about
how the data was obtained and/or how to display it (the timezone).

  For example, the timestamp value 0 with the timezone string "Europe/Paris"
  corresponds to "January 1st 1970, 00h00" in the UTC timezone, but the
  application may prefer to display it as "January 1st 1970, 01h00" in
  the Europe/Paris timezone (which is the same physical point in time).

One consequence is that timestamp values with a non-empty timezone
can be compared and ordered directly, since they all share the same
well-known point of reference (the Unix epoch).

Timestamps with an unset / empty timezone
-----------------------------------------

If a Timestamp column has no timezone value, its epoch is
1970-01-01 00:00:00 (January 1st 1970, midnight) in an *unknown* timezone.

Therefore, timestamp values without a timezone cannot be meaningfully
interpreted as physical points in time, but only as calendar / clock
indications ("wall clock time") in an unspecified timezone.

  For example, the timestamp value 0 with an empty timezone string
  corresponds to "January 1st 1970, 00h00" in an unknown timezone: there
  is not enough information to interpret it as a well-defined physical
  point in time.

One consequence is that timestamp values without a timezone cannot
be reliably compared or ordered, since they may have different points of
reference.  In particular, it is *not* possible to interpret an unset
or empty timezone as the same as "UTC".

Conversion between timezones
----------------------------

If a Timestamp column has a non-empty timezone, changing the timezone
to a different non-empty value is a metadata-only operation:
the timestamp values need not change as their point of reference remains
the same (the Unix epoch).

However, if a Timestamp column has no timezone value, changing it to a
non-empty value requires to think about the desired semantics.
One possibility is to assume that the original timestamp values are
relative to the epoch of the timezone being set; timestamp values should
then adjusted to the Unix epoch (for example, changing the timezone from
empty to "Europe/Paris" would require converting the timestamp values
from "Europe/Paris" to "UTC", which seems counter-intuitive but is
nevertheless correct).

Guidelines for encoding data from external libraries
----------------------------------------------------

Date & time libraries often have multiple different data types for temporal
data. In order to ease interoperability between different implementations the
Arrow project has some recommendations for encoding these types into a Timestamp
column.

An "instant" represents a physical point in time that has no relevant timezone
(for example, astronomical data). To encode an instant, use a Timestamp with
the timezone string set to "UTC", and make sure the Timestamp values
are relative to the UTC epoch (January 1st 1970, midnight).

A "zoned date-time" represents a physical point in time annotated with an
informative timezone (for example, the timezone in which the data was
recorded).  To encode a zoned date-time, use a Timestamp with the timezone
string set to the name of the timezone, and make sure the Timestamp values
are relative to the UTC epoch (January 1st 1970, midnight).

 (There is some ambiguity between an instant and a zoned date-time with the
  UTC timezone.  Both of these are stored the same in Arrow.  Typically,
  this distinction does not matter.  If it does, then an application should
  use custom metadata or an extension type to distinguish between the two cases.)

An "offset date-time" represents a physical point in time combined with an
explicit offset from UTC.  To encode an offset date-time, use a Timestamp
with the timezone string set to the numeric timezone offset string
(e.g. "+03:00"), and make sure the Timestamp values are relative to
the UTC epoch (January 1st 1970, midnight).

A "naive date-time" (also called "local date-time" in some libraries)
represents a wall clock time combined with a calendar date, but with
no indication of how to map this information to a physical point in time.
Naive date-times must be handled with care because of this missing
information, and also because daylight saving time (DST) may make
some values ambiguous or nonexistent. A naive date-time may be
stored as a struct with Date and Time fields. However, it may also be
encoded into a Timestamp column with an empty timezone. The timestamp
values should be computed "as if" the timezone of the date-time values
was UTC; for example, the naive date-time "January 1st 1970, 00h00" would
be encoded as timestamp value 0.

---

## TimestampArgs

`struct` · `arrow_ipc::gen::Schema::TimestampArgs`

Also reachable as `arrow::ipc::TimestampArgs`, `arrow_ipc::TimestampArgs`

```rust
struct TimestampArgs<'a>
```

**Fields**: `unit`, `timezone`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimestampArgs.md).


---

## TimestampBuilder

`struct` · `arrow_ipc::gen::Schema::TimestampBuilder`

Also reachable as `arrow::ipc::TimestampBuilder`, `arrow_ipc::TimestampBuilder`

```rust
struct TimestampBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (4)

```rust
fn add_timezone(&mut self, timezone: flatbuffers::WIPOffset<&'b str>)
fn add_unit(&mut self, unit: TimeUnit)
fn finish(self) -> flatbuffers::WIPOffset<Timestamp<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> TimestampBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TimestampBuilder.md).


---

## Type

`struct` · `arrow_ipc::gen::Schema::Type`

Also reachable as `arrow::ipc::Type`, `arrow_ipc::Type`

```rust
struct Type
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: u8) -> Self
fn to_little_endian(self) -> u8
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Type.md).


----------------------------------------------------------------------
Top-level Type value, enabling extensible type-specific metadata. We can
add new logical types to Type without breaking backwards compatibility

---

## TypeUnionTableOffset

`struct` · `arrow_ipc::gen::Schema::TypeUnionTableOffset`

Also reachable as `arrow::ipc::TypeUnionTableOffset`, `arrow_ipc::TypeUnionTableOffset`

```rust
struct TypeUnionTableOffset
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.TypeUnionTableOffset.md).


---

## Union

`struct` · `arrow_ipc::gen::Schema::Union`

Also reachable as `arrow::ipc::Union`, `arrow_ipc::Union`

```rust
struct Union<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (4)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, args: &'args UnionArgs<'args>) -> flatbuffers::WIPOffset<Union<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
fn mode(&self) -> UnionMode
fn typeIds(&self) -> Option<flatbuffers::Vector<'a, i32>>
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Union.md).


A union is a complex type with children in Field
By default ids in the type vector refer to the offsets in the children
optionally typeIds provides an indirection between the child offset and the type id
for each child `typeIds[offset]` is the id used in the type vector

---

## UnionArgs

`struct` · `arrow_ipc::gen::Schema::UnionArgs`

Also reachable as `arrow::ipc::UnionArgs`, `arrow_ipc::UnionArgs`

```rust
struct UnionArgs<'a>
```

**Fields**: `mode`, `typeIds`

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.UnionArgs.md).


---

## UnionBuilder

`struct` · `arrow_ipc::gen::Schema::UnionBuilder`

Also reachable as `arrow::ipc::UnionBuilder`, `arrow_ipc::UnionBuilder`

```rust
struct UnionBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (4)

```rust
fn add_mode(&mut self, mode: UnionMode)
fn add_typeIds(&mut self, typeIds: flatbuffers::WIPOffset<flatbuffers::Vector<'b, i32>>)
fn finish(self) -> flatbuffers::WIPOffset<Union<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> UnionBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.UnionBuilder.md).


---

## UnionMode

`struct` · `arrow_ipc::gen::Schema::UnionMode`

Also reachable as `arrow::ipc::UnionMode`, `arrow_ipc::UnionMode`

```rust
struct UnionMode
```

**Implements**: `flatbuffers::endian_scalar::EndianScalar`, `flatbuffers::follow::Follow`, `flatbuffers::push::Push`, `flatbuffers::verifier::SimpleToVerifyInSlice`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd, StructuralPartialEq

**Methods** (1)

```rust
fn variant_name(self) -> Option<&'static str>
```

**via `flatbuffers::endian_scalar::EndianScalar`**

```rust
fn from_little_endian(v: i16) -> Self
fn to_little_endian(self) -> i16
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::push::Push`**

```rust
unsafe fn push(&self, dst: &mut [u8], _written_len: usize)
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.UnionMode.md).


---

## Utf8

`struct` · `arrow_ipc::gen::Schema::Utf8`

Also reachable as `arrow::ipc::Utf8`, `arrow_ipc::Utf8`

```rust
struct Utf8<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args Utf8Args) -> flatbuffers::WIPOffset<Utf8<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8.md).


Unicode with UTF-8 encoding

---

## Utf8Args

`struct` · `arrow_ipc::gen::Schema::Utf8Args`

Also reachable as `arrow::ipc::Utf8Args`, `arrow_ipc::Utf8Args`

```rust
struct Utf8Args
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8Args.md).


---

## Utf8Builder

`struct` · `arrow_ipc::gen::Schema::Utf8Builder`

Also reachable as `arrow::ipc::Utf8Builder`, `arrow_ipc::Utf8Builder`

```rust
struct Utf8Builder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<Utf8<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> Utf8Builder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8Builder.md).


---

## Utf8View

`struct` · `arrow_ipc::gen::Schema::Utf8View`

Also reachable as `arrow::ipc::Utf8View`, `arrow_ipc::Utf8View`

```rust
struct Utf8View<'a>
```

**Fields**: `_tab`

**Implements**: `flatbuffers::follow::Follow`, `flatbuffers::verifier::Verifiable`

**Derives**: Clone, Copy, Debug, PartialEq, StructuralPartialEq

**Methods** (2)

```rust
fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr, A: flatbuffers::Allocator + 'bldr>(_fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr, A>, _args: &'args Utf8ViewArgs) -> flatbuffers::WIPOffset<Utf8View<'bldr>>
unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self
```

**via `flatbuffers::follow::Follow`**

```rust
unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner
```

**via `flatbuffers::verifier::Verifiable`**

```rust
fn run_verifier(v: &mut flatbuffers::Verifier<'_, '_>, pos: usize) -> Result<(), flatbuffers::InvalidFlatbuffer>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8View.md).


Logically the same as Utf8, but the internal representation uses a view
struct that contains the string length and either the string's entire data
inline (for small strings) or an inlined prefix, an index of another buffer,
and an offset pointing to a slice in that buffer (for non-small strings).

Since it uses a variable number of data buffers, each Field with this type
must have a corresponding entry in `variadicBufferCounts`.

---

## Utf8ViewArgs

`struct` · `arrow_ipc::gen::Schema::Utf8ViewArgs`

Also reachable as `arrow::ipc::Utf8ViewArgs`, `arrow_ipc::Utf8ViewArgs`

```rust
struct Utf8ViewArgs
```

**Derives**: Default

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8ViewArgs.md).


---

## Utf8ViewBuilder

`struct` · `arrow_ipc::gen::Schema::Utf8ViewBuilder`

Also reachable as `arrow::ipc::Utf8ViewBuilder`, `arrow_ipc::Utf8ViewBuilder`

```rust
struct Utf8ViewBuilder<'a: 'b, 'b, A: flatbuffers::Allocator + 'a>
```

**Methods** (2)

```rust
fn finish(self) -> flatbuffers::WIPOffset<Utf8View<'a>>
fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a, A>) -> Utf8ViewBuilder<'a, 'b, A>
```

[Full member, field, variant and typed contracts](../operations/arrow_ipc.gen.Schema.Utf8ViewBuilder.md).


---
