# `arrow::error`

Crate `arrow` · 1 public items · structured records in [`model/arrow.error.json`](../model/arrow.error.json)

## Result

`type_alias` · `arrow::error::Result`

```rust
type Result<T> = std::result::Result<T, ArrowError>
```

A specialized `Result` type for Arrow operations.

---
