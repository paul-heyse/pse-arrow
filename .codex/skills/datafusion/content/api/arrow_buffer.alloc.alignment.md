# `arrow_buffer::alloc::alignment`

Crate `arrow-buffer` · 1 public items · structured records in [`model/arrow_buffer.alloc.alignment.json`](../model/arrow_buffer.alloc.alignment.json)

## ALIGNMENT

`constant` · `arrow_buffer::alloc::alignment::ALIGNMENT`

Also reachable as `arrow::alloc::ALIGNMENT`, `arrow_buffer::alloc::ALIGNMENT`

```rust
const ALIGNMENT: usize = _
```

[Full member, field, variant and typed contracts](../operations/arrow_buffer.alloc.alignment.ALIGNMENT.md).


Cache and allocation multiple alignment size

---
