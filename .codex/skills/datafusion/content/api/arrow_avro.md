# `arrow_avro`

Crate `arrow-avro` · 1 public items · structured records in [`model/arrow_avro.json`](../model/arrow_avro.json)

## AvroFieldExt

`trait` · `arrow_avro::AvroFieldExt`

Also reachable as `datafusion::arrow_avro::AvroFieldExt`, `datafusion_datasource_avro::arrow_avro::AvroFieldExt`

```rust
trait AvroFieldExt
```

**Methods** (1)

```rust
fn with_utf8view(&self) -> Self
```

[Full member, field, variant and typed contracts](../operations/arrow_avro.AvroFieldExt.md).


Extension trait for AvroField to add Utf8View support

This trait adds methods for working with Utf8View support to the AvroField struct.

---
