# `datafusion_functions::crypto::expr_fn`

Crate `datafusion-functions` · 6 public items · structured records in [`model/datafusion_functions.crypto.expr_fn.json`](../model/datafusion_functions.crypto.expr_fn.json)

## digest

`function` · `datafusion_functions::crypto::expr_fn::digest`

Also reachable as `datafusion::prelude::digest`, `datafusion_functions::expr_fn::digest`

```rust
fn digest(input_arg1: datafusion_expr::Expr, input_arg2: datafusion_expr::Expr) -> datafusion_expr::Expr
```

[Full member, field, variant and typed contracts](../operations/datafusion_functions.crypto.expr_fn.digest.md).


Computes the binary hash of an expression using the specified algorithm.

---

## md5

`function` · `datafusion_functions::crypto::expr_fn::md5`

Also reachable as `datafusion::prelude::md5`, `datafusion_functions::expr_fn::md5`

```rust
fn md5(input_arg: datafusion_expr::Expr) -> datafusion_expr::Expr
```

[Full member, field, variant and typed contracts](../operations/datafusion_functions.crypto.expr_fn.md5.md).


Computes an MD5 128-bit checksum for a string expression.

---

## sha224

`function` · `datafusion_functions::crypto::expr_fn::sha224`

Also reachable as `datafusion::prelude::sha224`, `datafusion_functions::expr_fn::sha224`

```rust
fn sha224(input_arg1: datafusion_expr::Expr) -> datafusion_expr::Expr
```

[Full member, field, variant and typed contracts](../operations/datafusion_functions.crypto.expr_fn.sha224.md).


Computes the SHA-224 hash of a binary string.

---

## sha256

`function` · `datafusion_functions::crypto::expr_fn::sha256`

Also reachable as `datafusion::prelude::sha256`, `datafusion_functions::expr_fn::sha256`

```rust
fn sha256(input_arg1: datafusion_expr::Expr) -> datafusion_expr::Expr
```

[Full member, field, variant and typed contracts](../operations/datafusion_functions.crypto.expr_fn.sha256.md).


Computes the SHA-256 hash of a binary string.

---

## sha384

`function` · `datafusion_functions::crypto::expr_fn::sha384`

Also reachable as `datafusion::prelude::sha384`, `datafusion_functions::expr_fn::sha384`

```rust
fn sha384(input_arg1: datafusion_expr::Expr) -> datafusion_expr::Expr
```

[Full member, field, variant and typed contracts](../operations/datafusion_functions.crypto.expr_fn.sha384.md).


Computes the SHA-384 hash of a binary string.

---

## sha512

`function` · `datafusion_functions::crypto::expr_fn::sha512`

Also reachable as `datafusion::prelude::sha512`, `datafusion_functions::expr_fn::sha512`

```rust
fn sha512(input_arg1: datafusion_expr::Expr) -> datafusion_expr::Expr
```

[Full member, field, variant and typed contracts](../operations/datafusion_functions.crypto.expr_fn.sha512.md).


Computes the SHA-512 hash of a binary string.

---
