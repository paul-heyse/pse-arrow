# `object_store::client::s3`

Crate `object_store` · 3 public items · structured records in [`model/object_store.client.s3.json`](../model/object_store.client.s3.json)

## ListContents

`struct` · `object_store::client::s3::ListContents`

```rust
struct ListContents
```

**Fields**: `key`, `size`, `last_modified`, `e_tag`

[Full member, field, variant and typed contracts](../operations/object_store.client.s3.ListContents.md).


---

## ListPrefix

`struct` · `object_store::client::s3::ListPrefix`

```rust
struct ListPrefix
```

**Fields**: `prefix`

[Full member, field, variant and typed contracts](../operations/object_store.client.s3.ListPrefix.md).


---

## ListResponse

`struct` · `object_store::client::s3::ListResponse`

```rust
struct ListResponse
```

**Fields**: `contents`, `common_prefixes`, `next_continuation_token`

[Full member, field, variant and typed contracts](../operations/object_store.client.s3.ListResponse.md).


---
