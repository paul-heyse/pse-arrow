# Task routes

Reviewed depth is selective. Search the full operation index for unreviewed tasks.

| Task vocabulary | Decision brief | Input → output |
|---|---|---|
| parse numeric strings; cast bad values to null; reject invalid cast; strict conversion; safe cast | [Choose conversion failure and output-schema policies explicitly](../capabilities/arrow.cast.md) | Array, DataType, CastOptions → ArrayRef, ArrowError |
| same mask many arrays; filter predicate reuse; filter once; selection bitmap setup | [Reuse selection preparation when the workload warrants it](../capabilities/arrow.filter-reuse.md) | BooleanArray, Array, RecordBatch → FilterPredicate, ArrayRef, RecordBatch |
| compare composite keys; multi column sorting; dictionary row encoding; durable key bytes; row decode | [Encode composite keys for local comparisons](../capabilities/arrow.row-keys.md) | ArrayRef, SortField, SortOptions → Rows, OwnedRow, ArrayRef |
| align schema; extension metadata; non nullable field; zero column row count; physical expression adapter; schema evolution | [Distinguish batch validity, relational schema and metadata propagation](../capabilities/arrow.schema.md) | Schema, Field, ArrayRef, DFSchema → RecordBatch, DFSchema, PhysicalExpr |
| filter rows; Boolean mask; gather indices; reorder duplicate positions; slice contiguous range; project columns; zero columns | [Choose a row or column selection representation](../capabilities/arrow.select.md) | RecordBatch, Array, BooleanArray, integer indices → RecordBatch, ArrayRef |
| aggregate merge state; groups accumulator; sliding window retract; window frame peers; partial aggregation; count distinct | [Reuse aggregate/window functions and characterize state before extensions](../capabilities/df.aggregate-window.md) | ArrayRef, group indices, window partition, state arrays → ScalarValue, state arrays, ArrayRef |
| first non null value; coalesce fallback; nullable default; coalesce direct invoke | [Use coalesce through its planning contract](../capabilities/df.coalesce.md) | Expr, Field, DataType → Expr, ColumnarValue |
| consume incrementally; large query result; stream memory budget; blocking sort; cache repeat execution; cancel stream | [Choose stream, materialization, cache and resource boundaries](../capabilities/df.consume.md) | DataFrame, RuntimeEnv → SendableRecordBatchStream, RecordBatch, DataFrame |
| build expression; incompatible types; simplify direct expression; coalesce constructor; type coercion | [Separate expression construction, coercion and evaluation](../capabilities/df.expressions.md) | Expr, DFSchema, ScalarUDF → Expr, PhysicalExpr, ColumnarValue |
| built in scalar functions; nested list array map; SQL aliases; table function; return field volatility; custom UDF alternative | [Discover built-ins in the actual session before writing a UDF](../capabilities/df.functions.md) | Expr, ColumnarValue, SessionState → Expr, ColumnarValue, TableProvider |
| serialize plan; Substrait; protobuf custom codec; Arrow IPC Flight; FFI ownership | [Match the boundary to data, plan or ABI interchange](../capabilities/df.interchange.md) | LogicalPlan, ExecutionPlan, RecordBatch, FFI provider → bytes, plan, stream, FFI handles |
| custom provider scan; exact inexact predicate; min max pruning; limit projection filter column; false negatives | [Advertise only sound filter, projection and limit pushdown](../capabilities/df.pushdown.md) | Expr, projection indices, limit, Session → ExecutionPlan, ScanResult |
| join duplicates; null keys join; union all distinct; set bag operations; sort limit top k; unnest list | [Choose relational cardinality, null-key and ordering semantics](../capabilities/df.relations.md) | DataFrame, Expr → DataFrame, LogicalPlan |
| materialized batches table; partitioned files; Parquet object storage; provider view cache; register table | [Reuse a provider before implementing a new source](../capabilities/df.source.md) | RecordBatch, files, LogicalPlan, ObjectStore → TableProvider, DataFrame, ExecutionPlan |
| repeat query; reuse view cache; write Parquet files; object store registry; metadata cache; insert overwrite | [Separate source reuse, plan reuse, materialization and writes](../capabilities/df.storage-reuse.md) | DataFrame, ObjectStore, TableProvider → files, DataFrame, ExecutionPlan |
| Parquet row selection; skip row groups; page index; predicate decode; retained row coordinates | [Compose row-group pruning, row selection and decode filters](../capabilities/parquet.selection.md) | Parquet bytes, row groups, RowSelection, RowFilter → RecordBatch, reader |
