# `sqlparser::keywords::UNTIL`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.UNTIL.json).

<a id="op-335ad7764d96a804bc225818"></a>
## UNTIL

`constant` · `sqlparser::keywords::UNTIL` · sqlparser 0.62.0

```rust
const UNTIL: &'static str = "UNTIL"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `UNTIL` SQL keyword.
