# `sqlparser::keywords::HIVEVAR`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.HIVEVAR.json).

<a id="op-be1177ef35c4f996506afe73"></a>
## HIVEVAR

`constant` · `sqlparser::keywords::HIVEVAR` · sqlparser 0.62.0

```rust
const HIVEVAR: &'static str = "HIVEVAR"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `HIVEVAR` SQL keyword.
