# `sqlparser::keywords::WHERE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.WHERE.json).

<a id="op-737de3ae2a0db7b89a1e7b0c"></a>
## WHERE

`constant` · `sqlparser::keywords::WHERE` · sqlparser 0.62.0

```rust
const WHERE: &'static str = "WHERE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `WHERE` SQL keyword.
