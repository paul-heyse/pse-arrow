# `sqlparser::keywords::GZIP`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.GZIP.json).

<a id="op-f6273edb4c7bf35b8d863352"></a>
## GZIP

`constant` · `sqlparser::keywords::GZIP` · sqlparser 0.62.0

```rust
const GZIP: &'static str = "GZIP"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `GZIP` SQL keyword.
