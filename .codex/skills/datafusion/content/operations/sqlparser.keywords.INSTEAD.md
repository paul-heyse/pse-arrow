# `sqlparser::keywords::INSTEAD`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.INSTEAD.json).

<a id="op-e664b16a0405c176bfc689f6"></a>
## INSTEAD

`constant` · `sqlparser::keywords::INSTEAD` · sqlparser 0.62.0

```rust
const INSTEAD: &'static str = "INSTEAD"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `INSTEAD` SQL keyword.
