# `sqlparser::keywords::UESCAPE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.UESCAPE.json).

<a id="op-b2bab3de008eb87aaff15557"></a>
## UESCAPE

`constant` · `sqlparser::keywords::UESCAPE` · sqlparser 0.62.0

```rust
const UESCAPE: &'static str = "UESCAPE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `UESCAPE` SQL keyword.
