# `sqlparser::keywords::CONTINUE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CONTINUE.json).

<a id="op-5f9c224b0badeebbcfe9e78b"></a>
## CONTINUE

`constant` · `sqlparser::keywords::CONTINUE` · sqlparser 0.62.0

```rust
const CONTINUE: &'static str = "CONTINUE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CONTINUE` SQL keyword.
