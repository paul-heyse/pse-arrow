# `sqlparser::keywords::JOIN`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.JOIN.json).

<a id="op-b35a88df623a6f6223f5da54"></a>
## JOIN

`constant` · `sqlparser::keywords::JOIN` · sqlparser 0.62.0

```rust
const JOIN: &'static str = "JOIN"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `JOIN` SQL keyword.
