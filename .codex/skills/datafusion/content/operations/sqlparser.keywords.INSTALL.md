# `sqlparser::keywords::INSTALL`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.INSTALL.json).

<a id="op-4a9137bdc3032ce723cb3792"></a>
## INSTALL

`constant` · `sqlparser::keywords::INSTALL` · sqlparser 0.62.0

```rust
const INSTALL: &'static str = "INSTALL"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `INSTALL` SQL keyword.
