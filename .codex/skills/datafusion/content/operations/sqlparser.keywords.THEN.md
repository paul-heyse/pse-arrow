# `sqlparser::keywords::THEN`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.THEN.json).

<a id="op-9f7919ffa8897c308a999efe"></a>
## THEN

`constant` · `sqlparser::keywords::THEN` · sqlparser 0.62.0

```rust
const THEN: &'static str = "THEN"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `THEN` SQL keyword.
