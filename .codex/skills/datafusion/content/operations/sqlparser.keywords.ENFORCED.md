# `sqlparser::keywords::ENFORCED`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.ENFORCED.json).

<a id="op-36d1d99ac362e9592c15363c"></a>
## ENFORCED

`constant` · `sqlparser::keywords::ENFORCED` · sqlparser 0.62.0

```rust
const ENFORCED: &'static str = "ENFORCED"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `ENFORCED` SQL keyword.
