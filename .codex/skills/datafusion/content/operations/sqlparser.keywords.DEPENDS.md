# `sqlparser::keywords::DEPENDS`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.DEPENDS.json).

<a id="op-08327d4cad5a122388af8faf"></a>
## DEPENDS

`constant` · `sqlparser::keywords::DEPENDS` · sqlparser 0.62.0

```rust
const DEPENDS: &'static str = "DEPENDS"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `DEPENDS` SQL keyword.
