# `sqlparser::keywords::ASSERT`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.ASSERT.json).

<a id="op-9b94e34b5fae16cb10094be3"></a>
## ASSERT

`constant` · `sqlparser::keywords::ASSERT` · sqlparser 0.62.0

```rust
const ASSERT: &'static str = "ASSERT"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `ASSERT` SQL keyword.
