# `parquet::schema::types::TypePtr`

Full upstream contracts; raw type trees and source locators in [structured records](parquet.schema.types.TypePtr.json).

<a id="op-1685bf8b2a3ab0d4f7c0b98c"></a>
## TypePtr

`type_alias` · `parquet::schema::types::TypePtr` · parquet 59.3.0

```rust
type TypePtr = std::sync::Arc<Type>
```

Source: `src/schema/types.rs:36`. [Exact documentation build](https://docs.rs/crate/parquet/59.3.0/json).

Type alias for `Arc<Type>`.
