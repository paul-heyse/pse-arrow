# `sqlparser::keywords::IDENTITY`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.IDENTITY.json).

<a id="op-f7689df2c02ea1cf0b5b393d"></a>
## IDENTITY

`constant` · `sqlparser::keywords::IDENTITY` · sqlparser 0.62.0

```rust
const IDENTITY: &'static str = "IDENTITY"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `IDENTITY` SQL keyword.
