# `sqlparser::keywords::PARALLEL`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.PARALLEL.json).

<a id="op-06ec0d00d27b94fd84b12a4e"></a>
## PARALLEL

`constant` · `sqlparser::keywords::PARALLEL` · sqlparser 0.62.0

```rust
const PARALLEL: &'static str = "PARALLEL"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `PARALLEL` SQL keyword.
