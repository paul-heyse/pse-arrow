# `sqlparser::keywords::ESCAPED`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.ESCAPED.json).

<a id="op-52584b823a7b474757c3dbe8"></a>
## ESCAPED

`constant` · `sqlparser::keywords::ESCAPED` · sqlparser 0.62.0

```rust
const ESCAPED: &'static str = "ESCAPED"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `ESCAPED` SQL keyword.
