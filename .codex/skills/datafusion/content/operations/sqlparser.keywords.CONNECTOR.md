# `sqlparser::keywords::CONNECTOR`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CONNECTOR.json).

<a id="op-558574fc9e0d744df3662a78"></a>
## CONNECTOR

`constant` · `sqlparser::keywords::CONNECTOR` · sqlparser 0.62.0

```rust
const CONNECTOR: &'static str = "CONNECTOR"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CONNECTOR` SQL keyword.
