# `sqlparser::keywords::ELSE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.ELSE.json).

<a id="op-180dfd2064815958881eb6c4"></a>
## ELSE

`constant` · `sqlparser::keywords::ELSE` · sqlparser 0.62.0

```rust
const ELSE: &'static str = "ELSE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `ELSE` SQL keyword.
