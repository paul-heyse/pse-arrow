# `sqlparser::keywords::CONCURRENTLY`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CONCURRENTLY.json).

<a id="op-084bdbbbf47f9aadea35f3a8"></a>
## CONCURRENTLY

`constant` · `sqlparser::keywords::CONCURRENTLY` · sqlparser 0.62.0

```rust
const CONCURRENTLY: &'static str = "CONCURRENTLY"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CONCURRENTLY` SQL keyword.
