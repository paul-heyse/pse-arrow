# `arrow_array::temporal_conversions::date32_to_datetime`

Full upstream contracts; raw type trees and source locators in [structured records](arrow_array.temporal_conversions.date32_to_datetime.json).

<a id="op-24d31e3f013b7bda865a7cf4"></a>
## date32_to_datetime

`function` · `arrow_array::temporal_conversions::date32_to_datetime` · arrow-array 59.3.0

```rust
fn date32_to_datetime(v: i32) -> Option<chrono::NaiveDateTime>
```

Source: `src/temporal_conversions.rs:55`. [Exact documentation build](https://docs.rs/crate/arrow-array/59.3.0/json).

converts a `i32` representing a `date32` to [`NaiveDateTime`]

Unresolved upstream links (retained, not inferred): ``NaiveDateTime``.
