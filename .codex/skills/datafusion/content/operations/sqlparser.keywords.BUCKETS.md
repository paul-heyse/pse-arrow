# `sqlparser::keywords::BUCKETS`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.BUCKETS.json).

<a id="op-6c32abd2ee400bac08b4ae4a"></a>
## BUCKETS

`constant` · `sqlparser::keywords::BUCKETS` · sqlparser 0.62.0

```rust
const BUCKETS: &'static str = "BUCKETS"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `BUCKETS` SQL keyword.
