# `sqlparser::keywords::LIKE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.LIKE.json).

<a id="op-f9771f4b5cf7064810bbb071"></a>
## LIKE

`constant` · `sqlparser::keywords::LIKE` · sqlparser 0.62.0

```rust
const LIKE: &'static str = "LIKE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `LIKE` SQL keyword.
