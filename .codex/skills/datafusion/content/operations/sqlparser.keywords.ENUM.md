# `sqlparser::keywords::ENUM`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.ENUM.json).

<a id="op-82839d4a5e646ebe364110fc"></a>
## ENUM

`constant` · `sqlparser::keywords::ENUM` · sqlparser 0.62.0

```rust
const ENUM: &'static str = "ENUM"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `ENUM` SQL keyword.
