# `sqlparser::keywords::RUN`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.RUN.json).

<a id="op-af5dde979827b5997ec2bbca"></a>
## RUN

`constant` · `sqlparser::keywords::RUN` · sqlparser 0.62.0

```rust
const RUN: &'static str = "RUN"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `RUN` SQL keyword.
