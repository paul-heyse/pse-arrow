# `sqlparser::keywords::INNER`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.INNER.json).

<a id="op-ff49987b1337aa984fb7bc64"></a>
## INNER

`constant` · `sqlparser::keywords::INNER` · sqlparser 0.62.0

```rust
const INNER: &'static str = "INNER"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `INNER` SQL keyword.
