# `sqlparser::keywords::AND`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.AND.json).

<a id="op-7ca1eb09840c959f0c5f380a"></a>
## AND

`constant` · `sqlparser::keywords::AND` · sqlparser 0.62.0

```rust
const AND: &'static str = "AND"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `AND` SQL keyword.
