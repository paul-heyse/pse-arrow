# `sqlparser::keywords::RENAME`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.RENAME.json).

<a id="op-db21694ac49ab932bc4dc074"></a>
## RENAME

`constant` · `sqlparser::keywords::RENAME` · sqlparser 0.62.0

```rust
const RENAME: &'static str = "RENAME"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `RENAME` SQL keyword.
