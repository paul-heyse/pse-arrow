# `sqlparser::keywords::APPLY`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.APPLY.json).

<a id="op-f38db3d1fa91f0be319a655c"></a>
## APPLY

`constant` · `sqlparser::keywords::APPLY` · sqlparser 0.62.0

```rust
const APPLY: &'static str = "APPLY"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `APPLY` SQL keyword.
