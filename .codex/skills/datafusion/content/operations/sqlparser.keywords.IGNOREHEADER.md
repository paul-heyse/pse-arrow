# `sqlparser::keywords::IGNOREHEADER`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.IGNOREHEADER.json).

<a id="op-b72fa215778a5b05f58409e2"></a>
## IGNOREHEADER

`constant` · `sqlparser::keywords::IGNOREHEADER` · sqlparser 0.62.0

```rust
const IGNOREHEADER: &'static str = "IGNOREHEADER"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `IGNOREHEADER` SQL keyword.
