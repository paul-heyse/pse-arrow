# `sqlparser::keywords::IF`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.IF.json).

<a id="op-33ce7889bc903b10ed02e857"></a>
## IF

`constant` · `sqlparser::keywords::IF` · sqlparser 0.62.0

```rust
const IF: &'static str = "IF"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `IF` SQL keyword.
