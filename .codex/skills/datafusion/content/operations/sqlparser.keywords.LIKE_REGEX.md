# `sqlparser::keywords::LIKE_REGEX`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.LIKE_REGEX.json).

<a id="op-b97e21856e10e7c7542889d3"></a>
## LIKE_REGEX

`constant` · `sqlparser::keywords::LIKE_REGEX` · sqlparser 0.62.0

```rust
const LIKE_REGEX: &'static str = "LIKE_REGEX"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `LIKE_REGEX` SQL keyword.
