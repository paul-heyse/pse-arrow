# `sqlparser::keywords::CLONE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CLONE.json).

<a id="op-4590c4522fab04f60dad3500"></a>
## CLONE

`constant` · `sqlparser::keywords::CLONE` · sqlparser 0.62.0

```rust
const CLONE: &'static str = "CLONE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CLONE` SQL keyword.
