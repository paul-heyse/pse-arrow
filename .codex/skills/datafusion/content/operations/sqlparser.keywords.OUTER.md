# `sqlparser::keywords::OUTER`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.OUTER.json).

<a id="op-7dcb34b6a718004dccbd9bdd"></a>
## OUTER

`constant` · `sqlparser::keywords::OUTER` · sqlparser 0.62.0

```rust
const OUTER: &'static str = "OUTER"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `OUTER` SQL keyword.
