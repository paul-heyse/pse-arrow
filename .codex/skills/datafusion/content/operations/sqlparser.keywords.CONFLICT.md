# `sqlparser::keywords::CONFLICT`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CONFLICT.json).

<a id="op-5ebda9852fb1fbf14aba0cd6"></a>
## CONFLICT

`constant` · `sqlparser::keywords::CONFLICT` · sqlparser 0.62.0

```rust
const CONFLICT: &'static str = "CONFLICT"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CONFLICT` SQL keyword.
