# `sqlparser::keywords::ROLLBACK`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.ROLLBACK.json).

<a id="op-ebff78faff3699872d4988ce"></a>
## ROLLBACK

`constant` · `sqlparser::keywords::ROLLBACK` · sqlparser 0.62.0

```rust
const ROLLBACK: &'static str = "ROLLBACK"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `ROLLBACK` SQL keyword.
