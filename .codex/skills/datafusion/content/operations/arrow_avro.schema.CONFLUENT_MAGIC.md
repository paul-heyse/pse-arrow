# `arrow_avro::schema::CONFLUENT_MAGIC`

Full upstream contracts; raw type trees and source locators in [structured records](arrow_avro.schema.CONFLUENT_MAGIC.json).

<a id="op-6bd348e72b3caa9e8a44b0fe"></a>
## CONFLUENT_MAGIC

`constant` · `arrow_avro::schema::CONFLUENT_MAGIC` · arrow-avro 59.3.0

```rust
const CONFLUENT_MAGIC: [u8; 1] = _
```

Source: `src/schema.rs:40`. [Exact documentation build](https://docs.rs/crate/arrow-avro/59.3.0/json).

The Confluent "magic" byte (`0x00`)
