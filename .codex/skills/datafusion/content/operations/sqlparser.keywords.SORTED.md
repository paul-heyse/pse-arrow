# `sqlparser::keywords::SORTED`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.SORTED.json).

<a id="op-9b0edbdec669d2a993b83441"></a>
## SORTED

`constant` · `sqlparser::keywords::SORTED` · sqlparser 0.62.0

```rust
const SORTED: &'static str = "SORTED"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `SORTED` SQL keyword.
