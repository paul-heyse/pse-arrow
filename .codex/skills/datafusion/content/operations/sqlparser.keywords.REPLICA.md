# `sqlparser::keywords::REPLICA`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.REPLICA.json).

<a id="op-5737c52cd38412634374d391"></a>
## REPLICA

`constant` · `sqlparser::keywords::REPLICA` · sqlparser 0.62.0

```rust
const REPLICA: &'static str = "REPLICA"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `REPLICA` SQL keyword.
