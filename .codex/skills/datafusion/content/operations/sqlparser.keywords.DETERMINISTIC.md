# `sqlparser::keywords::DETERMINISTIC`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.DETERMINISTIC.json).

<a id="op-30a93127673b556b3a2138df"></a>
## DETERMINISTIC

`constant` · `sqlparser::keywords::DETERMINISTIC` · sqlparser 0.62.0

```rust
const DETERMINISTIC: &'static str = "DETERMINISTIC"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `DETERMINISTIC` SQL keyword.
