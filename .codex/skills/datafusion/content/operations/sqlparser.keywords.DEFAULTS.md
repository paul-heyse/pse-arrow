# `sqlparser::keywords::DEFAULTS`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.DEFAULTS.json).

<a id="op-db4f2dd1cd4e96e885589429"></a>
## DEFAULTS

`constant` · `sqlparser::keywords::DEFAULTS` · sqlparser 0.62.0

```rust
const DEFAULTS: &'static str = "DEFAULTS"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `DEFAULTS` SQL keyword.
