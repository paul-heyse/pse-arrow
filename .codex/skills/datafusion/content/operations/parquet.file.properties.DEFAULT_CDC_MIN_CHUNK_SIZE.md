# `parquet::file::properties::DEFAULT_CDC_MIN_CHUNK_SIZE`

Full upstream contracts; raw type trees and source locators in [structured records](parquet.file.properties.DEFAULT_CDC_MIN_CHUNK_SIZE.json).

<a id="op-80566ed3875ca312857a6a21"></a>
## DEFAULT_CDC_MIN_CHUNK_SIZE

`constant` · `parquet::file::properties::DEFAULT_CDC_MIN_CHUNK_SIZE` · parquet 59.3.0

```rust
const DEFAULT_CDC_MIN_CHUNK_SIZE: usize = _
```

Source: `src/file/properties.rs:76`. [Exact documentation build](https://docs.rs/crate/parquet/59.3.0/json).

Default minimum chunk size for content-defined chunking: 256 KiB.
