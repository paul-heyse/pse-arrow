# `sqlparser::keywords::RETURNING`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.RETURNING.json).

<a id="op-934b40a7daa3ddfeda3c11b4"></a>
## RETURNING

`constant` · `sqlparser::keywords::RETURNING` · sqlparser 0.62.0

```rust
const RETURNING: &'static str = "RETURNING"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `RETURNING` SQL keyword.
