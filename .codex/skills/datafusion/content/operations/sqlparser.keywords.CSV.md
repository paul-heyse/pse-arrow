# `sqlparser::keywords::CSV`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CSV.json).

<a id="op-27d03149be42231a2136a36e"></a>
## CSV

`constant` · `sqlparser::keywords::CSV` · sqlparser 0.62.0

```rust
const CSV: &'static str = "CSV"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CSV` SQL keyword.
