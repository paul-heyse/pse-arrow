# `sqlparser::keywords::EVERY`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.EVERY.json).

<a id="op-d05119e21577401d78da5a21"></a>
## EVERY

`constant` · `sqlparser::keywords::EVERY` · sqlparser 0.62.0

```rust
const EVERY: &'static str = "EVERY"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `EVERY` SQL keyword.
