# `sqlparser::keywords::READ_ONLY`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.READ_ONLY.json).

<a id="op-15a2c953042dd471ae575a57"></a>
## READ_ONLY

`constant` · `sqlparser::keywords::READ_ONLY` · sqlparser 0.62.0

```rust
const READ_ONLY: &'static str = "READ_ONLY"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `READ_ONLY` SQL keyword.
