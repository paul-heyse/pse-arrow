# `sqlparser::keywords::BOTH`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.BOTH.json).

<a id="op-47317aef4b8fd66fc7f94bcc"></a>
## BOTH

`constant` · `sqlparser::keywords::BOTH` · sqlparser 0.62.0

```rust
const BOTH: &'static str = "BOTH"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `BOTH` SQL keyword.
