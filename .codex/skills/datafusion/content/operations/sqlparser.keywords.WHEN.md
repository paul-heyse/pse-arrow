# `sqlparser::keywords::WHEN`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.WHEN.json).

<a id="op-95c243544ad97494ccc9f36a"></a>
## WHEN

`constant` · `sqlparser::keywords::WHEN` · sqlparser 0.62.0

```rust
const WHEN: &'static str = "WHEN"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `WHEN` SQL keyword.
