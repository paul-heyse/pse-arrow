# `sqlparser::keywords::XOR`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.XOR.json).

<a id="op-c9066cd784b290c2092fdb77"></a>
## XOR

`constant` · `sqlparser::keywords::XOR` · sqlparser 0.62.0

```rust
const XOR: &'static str = "XOR"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `XOR` SQL keyword.
