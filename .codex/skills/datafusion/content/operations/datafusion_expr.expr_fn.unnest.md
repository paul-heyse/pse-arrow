# `datafusion_expr::expr_fn::unnest`

Full upstream contracts; raw type trees and source locators in [structured records](datafusion_expr.expr_fn.unnest.json).

<a id="op-d35c751efc0df98bf3747257"></a>
## unnest

`function` · `datafusion_expr::expr_fn::unnest` · datafusion-expr 55.1.0

```rust
fn unnest(expr: Expr) -> Expr
```

Source: `src/expr_fn.rs:390`. [Exact documentation build](https://docs.rs/crate/datafusion-expr/55.1.0/json).

Create a Unnest expression with default (non-outer) semantics.
