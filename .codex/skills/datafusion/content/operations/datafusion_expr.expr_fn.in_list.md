# `datafusion_expr::expr_fn::in_list`

Full upstream contracts; raw type trees and source locators in [structured records](datafusion_expr.expr_fn.in_list.json).

<a id="op-aad49959472c97df32c454a4"></a>
## in_list

`function` · `datafusion_expr::expr_fn::in_list` · datafusion-expr 55.1.0

```rust
fn in_list(expr: Expr, list: Vec<Expr>, negated: bool) -> Expr
```

Source: `src/expr_fn.rs:246`. [Exact documentation build](https://docs.rs/crate/datafusion-expr/55.1.0/json).

Create an in_list expression
