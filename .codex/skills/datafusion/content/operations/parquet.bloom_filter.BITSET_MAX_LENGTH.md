# `parquet::bloom_filter::BITSET_MAX_LENGTH`

Full upstream contracts; raw type trees and source locators in [structured records](parquet.bloom_filter.BITSET_MAX_LENGTH.json).

<a id="op-10db3716e4217119663fd14c"></a>
## BITSET_MAX_LENGTH

`constant` · `parquet::bloom_filter::BITSET_MAX_LENGTH` · parquet 59.3.0

```rust
const BITSET_MAX_LENGTH: usize = _
```

Source: `src/bloom_filter/mod.rs:360`. [Exact documentation build](https://docs.rs/crate/parquet/59.3.0/json).

The maximum number of bytes for a bloom filter bitset.
