# `sqlparser::keywords::CONSTRAINT`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.CONSTRAINT.json).

<a id="op-24aaa5f418ca4b691dfd101b"></a>
## CONSTRAINT

`constant` · `sqlparser::keywords::CONSTRAINT` · sqlparser 0.62.0

```rust
const CONSTRAINT: &'static str = "CONSTRAINT"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `CONSTRAINT` SQL keyword.
