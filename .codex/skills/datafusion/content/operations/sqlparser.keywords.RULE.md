# `sqlparser::keywords::RULE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.RULE.json).

<a id="op-ed9f6068b21b240f9cdb64fb"></a>
## RULE

`constant` · `sqlparser::keywords::RULE` · sqlparser 0.62.0

```rust
const RULE: &'static str = "RULE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `RULE` SQL keyword.
