# `sqlparser::keywords::UUID`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.UUID.json).

<a id="op-1f3c33283170262a5d93a996"></a>
## UUID

`constant` · `sqlparser::keywords::UUID` · sqlparser 0.62.0

```rust
const UUID: &'static str = "UUID"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `UUID` SQL keyword.
