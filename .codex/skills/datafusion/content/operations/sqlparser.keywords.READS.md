# `sqlparser::keywords::READS`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.READS.json).

<a id="op-716f888d747547d72b4337a7"></a>
## READS

`constant` · `sqlparser::keywords::READS` · sqlparser 0.62.0

```rust
const READS: &'static str = "READS"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `READS` SQL keyword.
