# `parquet::bloom_filter::BITSET_MIN_LENGTH`

Full upstream contracts; raw type trees and source locators in [structured records](parquet.bloom_filter.BITSET_MIN_LENGTH.json).

<a id="op-65b75db588b6e018cdc7bd85"></a>
## BITSET_MIN_LENGTH

`constant` · `parquet::bloom_filter::BITSET_MIN_LENGTH` · parquet 59.3.0

```rust
const BITSET_MIN_LENGTH: usize = 32
```

Source: `src/bloom_filter/mod.rs:358`. [Exact documentation build](https://docs.rs/crate/parquet/59.3.0/json).

The minimum number of bytes for a bloom filter bitset.
