# `sqlparser::keywords::RETURN`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.RETURN.json).

<a id="op-f189fcde647aa4a7472c464f"></a>
## RETURN

`constant` · `sqlparser::keywords::RETURN` · sqlparser 0.62.0

```rust
const RETURN: &'static str = "RETURN"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `RETURN` SQL keyword.
