# `sqlparser::keywords::SELECT`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.SELECT.json).

<a id="op-ef63b57dd1bccc15607a9a47"></a>
## SELECT

`constant` · `sqlparser::keywords::SELECT` · sqlparser 0.62.0

```rust
const SELECT: &'static str = "SELECT"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `SELECT` SQL keyword.
