# `sqlparser::keywords::DECLARE`

Full upstream contracts; raw type trees and source locators in [structured records](sqlparser.keywords.DECLARE.json).

<a id="op-c7f78e0a06ecae773f1eda20"></a>
## DECLARE

`constant` · `sqlparser::keywords::DECLARE` · sqlparser 0.62.0

```rust
const DECLARE: &'static str = "DECLARE"
```

Source: `src/keywords.rs:91`. [Exact documentation build](https://docs.rs/crate/sqlparser/0.62.0/json).

The `DECLARE` SQL keyword.
