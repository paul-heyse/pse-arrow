{
  "module": "parquet::arrow::arrow_reader::selection::selector",
  "crate": "parquet",
  "api": "api/parquet.arrow.arrow_reader.selection.selector.md",
  "items": [
    {
      "path": "parquet::arrow::arrow_reader::selection::selector::RowSelector",
      "name": "RowSelector",
      "kind": "struct",
      "crate": "parquet",
      "signature": "struct RowSelector",
      "summary": "[`RowSelection`] is a collection of [`RowSelector`] used to skip rows when scanning a parquet file",
      "doc": "api/parquet.arrow.arrow_reader.selection.selector.md#rowselector",
      "aliases": [
        "parquet::arrow::arrow_reader::RowSelector"
      ],
      "fields": [
        "row_count",
        "skip"
      ],
      "methods": [
        {
          "name": "select",
          "signature": "fn select(row_count: usize) -> Self",
          "summary": "Select `row_count` rows"
        },
        {
          "name": "skip",
          "signature": "fn skip(row_count: usize) -> Self",
          "summary": "Skip `row_count` rows"
        }
      ]
    }
  ]
}
