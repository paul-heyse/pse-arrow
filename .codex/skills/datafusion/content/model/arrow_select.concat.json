{
  "module": "arrow_select::concat",
  "crate": "arrow-select",
  "api": "api/arrow_select.concat.md",
  "items": [
    {
      "path": "arrow_select::concat::concat",
      "name": "concat",
      "kind": "function",
      "crate": "arrow-select",
      "signature": "fn concat(arrays: &[&dyn Array]) -> Result<ArrayRef, arrow_schema::ArrowError>",
      "summary": "Concatenate multiple [Array] of the same type into a single [ArrayRef].",
      "doc": "api/arrow_select.concat.md#concat",
      "aliases": [
        "arrow::compute::concat",
        "arrow::compute::kernels::concat::concat"
      ]
    },
    {
      "path": "arrow_select::concat::concat_batches",
      "name": "concat_batches",
      "kind": "function",
      "crate": "arrow-select",
      "signature": "fn concat_batches<'a>(schema: &arrow_schema::SchemaRef, input_batches: impl IntoIterator<Item = &'a RecordBatch>) -> Result<RecordBatch, arrow_schema::ArrowError>",
      "summary": "Concatenates `batches` together into a single [`RecordBatch`].",
      "doc": "api/arrow_select.concat.md#concat_batches",
      "aliases": [
        "arrow::compute::concat_batches",
        "arrow::compute::kernels::concat::concat_batches"
      ]
    }
  ]
}
