{
  "module": "datafusion_physical_optimizer::join_selection",
  "crate": "datafusion-physical-optimizer",
  "api": "api/datafusion_physical_optimizer.join_selection.md",
  "items": [
    {
      "path": "datafusion_physical_optimizer::join_selection::hash_join_swap_subrule",
      "name": "hash_join_swap_subrule",
      "kind": "function",
      "crate": "datafusion-physical-optimizer",
      "signature": "fn hash_join_swap_subrule(input: std::sync::Arc<dyn ExecutionPlan>, _config_options: &datafusion_common::config::ConfigOptions) -> datafusion_common::error::Result<std::sync::Arc<dyn ExecutionPlan>>",
      "summary": "This subrule will swap build/probe sides of a hash join depending on whether one of its inputs may produce an infinite stream of records. The rule ensures that the left (build) side of the hash join always operates on an input stream that…",
      "doc": "api/datafusion_physical_optimizer.join_selection.md#hash_join_swap_subrule"
    },
    {
      "path": "datafusion_physical_optimizer::join_selection::JoinSelection",
      "name": "JoinSelection",
      "kind": "struct",
      "crate": "datafusion-physical-optimizer",
      "signature": "struct JoinSelection",
      "summary": "The [`JoinSelection`] rule tries to modify a given plan so that it can accommodate infinite sources and optimize joins in the plan according to available statistical information, if there is any.",
      "doc": "api/datafusion_physical_optimizer.join_selection.md#joinselection",
      "implements": [
        "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
      ],
      "methods": [
        {
          "name": "name",
          "signature": "fn name(&self) -> &str",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        },
        {
          "name": "new",
          "signature": "fn new() -> Self"
        },
        {
          "name": "optimize",
          "signature": "fn optimize(&self, plan: Arc<dyn ExecutionPlan>, config: &ConfigOptions) -> Result<Arc<dyn ExecutionPlan>>",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        },
        {
          "name": "optimize_with_context",
          "signature": "fn optimize_with_context(&self, plan: Arc<dyn ExecutionPlan>, context: &dyn PhysicalOptimizerContext) -> Result<Arc<dyn ExecutionPlan>>",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        },
        {
          "name": "schema_check",
          "signature": "fn schema_check(&self) -> bool",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        }
      ]
    },
    {
      "path": "datafusion_physical_optimizer::join_selection::PipelineFixerSubrule",
      "name": "PipelineFixerSubrule",
      "kind": "type_alias",
      "crate": "datafusion-physical-optimizer",
      "signature": "type PipelineFixerSubrule = dyn Fn(std::sync::Arc<dyn ExecutionPlan>, &datafusion_common::config::ConfigOptions) -> datafusion_common::error::Result<std::sync::Arc<dyn ExecutionPlan>>",
      "summary": "Pipeline-fixing join selection subrule.",
      "doc": "api/datafusion_physical_optimizer.join_selection.md#pipelinefixersubrule"
    }
  ]
}
