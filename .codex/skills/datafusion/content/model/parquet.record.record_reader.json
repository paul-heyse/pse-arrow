{
  "module": "parquet::record::record_reader",
  "crate": "parquet",
  "api": "api/parquet.record.record_reader.md",
  "items": [
    {
      "path": "parquet::record::record_reader::RecordReader",
      "name": "RecordReader",
      "kind": "trait",
      "crate": "parquet",
      "signature": "trait RecordReader<T>",
      "summary": "Read up to `num_records` records from `row_group_reader` into `self`.",
      "doc": "api/parquet.record.record_reader.md#recordreader",
      "aliases": [
        "parquet::record::RecordReader"
      ],
      "required_methods": [
        "read_from_row_group"
      ],
      "provided_methods": [],
      "methods": [
        {
          "name": "read_from_row_group",
          "signature": "fn read_from_row_group(&mut self, row_group_reader: &mut dyn RowGroupReader, num_records: usize) -> Result<(), ParquetError>",
          "summary": "Read up to `num_records` records from `row_group_reader` into `self`."
        }
      ]
    }
  ]
}
