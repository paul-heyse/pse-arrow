{
  "module": "datafusion_physical_optimizer::limit_pushdown",
  "crate": "datafusion-physical-optimizer",
  "api": "api/datafusion_physical_optimizer.limit_pushdown.md",
  "items": [
    {
      "path": "datafusion_physical_optimizer::limit_pushdown::pushdown_limit_helper",
      "name": "pushdown_limit_helper",
      "kind": "function",
      "crate": "datafusion-physical-optimizer",
      "signature": "fn pushdown_limit_helper(pushdown_plan: std::sync::Arc<dyn ExecutionPlan>, global_state: GlobalRequirements) -> datafusion_common::error::Result<(datafusion_common::tree_node::Transformed<std::sync::Arc<dyn ExecutionPlan>>, GlobalRequirements)>",
      "summary": "This function is the main helper function of the `LimitPushDown` rule. The helper takes an `ExecutionPlan` and a global (algorithm) state which is an instance of `GlobalRequirements` and modifies these parameters while checking if the limi…",
      "doc": "api/datafusion_physical_optimizer.limit_pushdown.md#pushdown_limit_helper"
    },
    {
      "path": "datafusion_physical_optimizer::limit_pushdown::GlobalRequirements",
      "name": "GlobalRequirements",
      "kind": "struct",
      "crate": "datafusion-physical-optimizer",
      "signature": "struct GlobalRequirements",
      "summary": "This is a \"data class\" we use within the [`LimitPushdown`] rule to push down limits in the plan. GlobalRequirements are hold as a rule-wide state and holds the fetch and skip information. The struct also has a field named satisfied which m…",
      "doc": "api/datafusion_physical_optimizer.limit_pushdown.md#globalrequirements",
      "methods": []
    },
    {
      "path": "datafusion_physical_optimizer::limit_pushdown::LimitPushdown",
      "name": "LimitPushdown",
      "kind": "struct",
      "crate": "datafusion-physical-optimizer",
      "signature": "struct LimitPushdown",
      "summary": "This rule inspects [`ExecutionPlan`]'s and pushes down the fetch limit from the parent to the child if applicable.",
      "doc": "api/datafusion_physical_optimizer.limit_pushdown.md#limitpushdown",
      "implements": [
        "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
      ],
      "methods": [
        {
          "name": "name",
          "signature": "fn name(&self) -> &str",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        },
        {
          "name": "new",
          "signature": "fn new() -> Self"
        },
        {
          "name": "optimize",
          "signature": "fn optimize(&self, plan: Arc<dyn ExecutionPlan>, _config: &ConfigOptions) -> Result<Arc<dyn ExecutionPlan>>",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        },
        {
          "name": "schema_check",
          "signature": "fn schema_check(&self) -> bool",
          "via_trait": "datafusion_session::physical_optimizer::PhysicalOptimizerRule"
        }
      ]
    }
  ]
}
