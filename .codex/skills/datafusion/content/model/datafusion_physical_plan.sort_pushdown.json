{
  "module": "datafusion_physical_plan::sort_pushdown",
  "crate": "datafusion-physical-plan",
  "api": "api/datafusion_physical_plan.sort_pushdown.md",
  "items": [
    {
      "path": "datafusion_physical_plan::sort_pushdown::SortOrderPushdownResult",
      "name": "SortOrderPushdownResult",
      "kind": "enum",
      "crate": "datafusion-physical-plan",
      "signature": "enum SortOrderPushdownResult<T>",
      "summary": "Result of attempting to push down sort ordering to a node.",
      "doc": "api/datafusion_physical_plan.sort_pushdown.md#sortorderpushdownresult",
      "aliases": [
        "datafusion::physical_plan::SortOrderPushdownResult",
        "datafusion_physical_plan::SortOrderPushdownResult"
      ],
      "variants": [
        "Exact",
        "Inexact",
        "Unsupported"
      ],
      "methods": [
        {
          "name": "into_inexact",
          "signature": "fn into_inexact(self) -> Self",
          "summary": "Convert this result to `Inexact`, downgrading `Exact` if present."
        },
        {
          "name": "into_inner",
          "signature": "fn into_inner(self) -> Option<T>",
          "summary": "Extract the inner value if present"
        },
        {
          "name": "map",
          "signature": "fn map<U, F: FnOnce(T) -> U>(self, f: F) -> SortOrderPushdownResult<U>",
          "summary": "Map the inner value to a different type while preserving the variant."
        },
        {
          "name": "try_map",
          "signature": "fn try_map<U, E, F: FnOnce(T) -> Result<U, E>>(self, f: F) -> Result<SortOrderPushdownResult<U>, E>",
          "summary": "Try to map the inner value, returning an error if the function fails."
        }
      ]
    }
  ]
}
