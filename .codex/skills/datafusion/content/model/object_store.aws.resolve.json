{
  "module": "object_store::aws::resolve",
  "crate": "object_store",
  "api": "api/object_store.aws.resolve.md",
  "items": [
    {
      "path": "object_store::aws::resolve::resolve_bucket_region",
      "name": "resolve_bucket_region",
      "kind": "function",
      "crate": "object_store",
      "signature": "async fn resolve_bucket_region(bucket: &str, client_options: &ClientOptions) -> Result<String>",
      "summary": "Get the bucket region using the [HeadBucket API]. This will fail if the bucket does not exist.",
      "doc": "api/object_store.aws.resolve.md#resolve_bucket_region",
      "aliases": [
        "object_store::aws::resolve_bucket_region"
      ]
    }
  ]
}
