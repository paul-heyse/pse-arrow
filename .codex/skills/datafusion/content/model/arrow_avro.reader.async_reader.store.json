{
  "module": "arrow_avro::reader::async_reader::store",
  "crate": "arrow-avro",
  "api": "api/arrow_avro.reader.async_reader.store.md",
  "items": [
    {
      "path": "arrow_avro::reader::async_reader::store::AvroObjectReader",
      "name": "AvroObjectReader",
      "kind": "struct",
      "crate": "arrow-avro",
      "signature": "struct AvroObjectReader",
      "summary": "An implementation of an AsyncFileReader using the [`ObjectStore`] API.",
      "doc": "api/arrow_avro.reader.async_reader.store.md#avroobjectreader",
      "deprecated": "since 59.2.0: Implement `AsyncFileReader` directly instead; see the example on the `AsyncFileReader` trait documentation and `arrow-avro/examples/object_store.rs`. Use `SpawnedReader` to perform I/O on a dedicated runtime. See also https://github.com/apache/arrow-rs/issues/10308",
      "aliases": [
        "arrow_avro::reader::AvroObjectReader",
        "arrow_avro::reader::async_reader::AvroObjectReader"
      ],
      "implements": [
        "arrow_avro::reader::async_reader::async_file_reader::AsyncFileReader"
      ],
      "methods": [
        {
          "name": "get_byte_ranges",
          "signature": "fn get_byte_ranges(&mut self, ranges: Vec<Range<u64>>) -> BoxFuture<'_, Result<Vec<Bytes>, AvroError>> where Self: Send",
          "via_trait": "arrow_avro::reader::async_reader::async_file_reader::AsyncFileReader"
        },
        {
          "name": "get_bytes",
          "signature": "fn get_bytes(&mut self, range: Range<u64>) -> BoxFuture<'_, Result<Bytes, AvroError>>",
          "via_trait": "arrow_avro::reader::async_reader::async_file_reader::AsyncFileReader"
        },
        {
          "name": "new",
          "signature": "fn new(store: Arc<dyn ObjectStore>, path: Path) -> Self",
          "summary": "Creates a new [`Self`] from a store implementation and file location."
        },
        {
          "name": "with_runtime",
          "signature": "fn with_runtime(self, handle: Handle) -> Self",
          "summary": "Perform IO on the provided tokio runtime"
        }
      ]
    }
  ]
}
