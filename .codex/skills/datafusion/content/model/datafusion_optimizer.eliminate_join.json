{
  "module": "datafusion_optimizer::eliminate_join",
  "crate": "datafusion-optimizer",
  "api": "api/datafusion_optimizer.eliminate_join.md",
  "items": [
    {
      "path": "datafusion_optimizer::eliminate_join::EliminateJoin",
      "name": "EliminateJoin",
      "kind": "struct",
      "crate": "datafusion-optimizer",
      "signature": "struct EliminateJoin",
      "summary": "Rewrites an inner join to a semi join when one input only filters the other, removes an outer join whose non-preserved side is unused and cannot multiply the preserved side's rows, and replaces an always-false inner join with an empty rela…",
      "doc": "api/datafusion_optimizer.eliminate_join.md#eliminatejoin",
      "implements": [
        "datafusion_optimizer::optimizer::OptimizerRule"
      ],
      "methods": [
        {
          "name": "name",
          "signature": "fn name(&self) -> &str",
          "via_trait": "datafusion_optimizer::optimizer::OptimizerRule"
        },
        {
          "name": "new",
          "signature": "fn new() -> Self"
        },
        {
          "name": "rewrite",
          "signature": "fn rewrite(&self, plan: LogicalPlan, _config: &dyn OptimizerConfig) -> Result<Transformed<LogicalPlan>>",
          "via_trait": "datafusion_optimizer::optimizer::OptimizerRule"
        }
      ]
    }
  ]
}
