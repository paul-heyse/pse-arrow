{
  "module": "object_store::aws::credential",
  "crate": "object_store",
  "api": "api/object_store.aws.credential.md",
  "items": [
    {
      "path": "object_store::aws::credential::AwsAuthorizer",
      "name": "AwsAuthorizer",
      "kind": "struct",
      "crate": "object_store",
      "signature": "struct AwsAuthorizer<'a>",
      "summary": "Authorize a [`HttpRequest`] with an [`AwsCredential`] using [AWS SigV4]",
      "doc": "api/object_store.aws.credential.md#awsauthorizer",
      "aliases": [
        "object_store::aws::AwsAuthorizer"
      ],
      "methods": [
        {
          "name": "authorize",
          "signature": "fn authorize(&self, request: &mut HttpRequest, pre_calculated_digest: Option<&[u8]>)",
          "summary": "Authorize `request` with an optional pre-calculated SHA256 digest by attaching the relevant [AWS SigV4] headers"
        },
        {
          "name": "new",
          "signature": "fn new(credential: &'a AwsCredential, service: &'a str, region: &'a str) -> Self",
          "summary": "Create a new [`AwsAuthorizer`]"
        },
        {
          "name": "with_request_payer",
          "signature": "fn with_request_payer(self, request_payer: bool) -> Self",
          "summary": "Set whether to include requester pays headers"
        },
        {
          "name": "with_sign_payload",
          "signature": "fn with_sign_payload(self, signed: bool) -> Self",
          "summary": "Controls whether this [`AwsAuthorizer`] will attempt to sign the request payload, the default is `true`"
        }
      ]
    },
    {
      "path": "object_store::aws::credential::AwsCredential",
      "name": "AwsCredential",
      "kind": "struct",
      "crate": "object_store",
      "signature": "struct AwsCredential",
      "summary": "A set of AWS security credentials",
      "doc": "api/object_store.aws.credential.md#awscredential",
      "aliases": [
        "object_store::aws::AwsCredential"
      ],
      "fields": [
        "key_id",
        "secret_key",
        "token"
      ],
      "methods": []
    }
  ]
}
