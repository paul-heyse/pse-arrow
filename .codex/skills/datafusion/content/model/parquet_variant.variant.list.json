{
  "module": "parquet_variant::variant::list",
  "crate": "parquet-variant",
  "api": "api/parquet_variant.variant.list.md",
  "items": [
    {
      "path": "parquet_variant::variant::list::VariantList",
      "name": "VariantList",
      "kind": "struct",
      "crate": "parquet-variant",
      "signature": "struct VariantList<'m, 'v>",
      "summary": "[`Variant`] Array.",
      "doc": "api/parquet_variant.variant.list.md#variantlist",
      "fields": [
        "metadata",
        "value"
      ],
      "methods": [
        {
          "name": "get",
          "signature": "fn get(&self, index: usize) -> Option<Variant<'m, 'v>>",
          "summary": "Returns element by index in `0..self.len()`, if any. May panic if this list is [invalid]."
        },
        {
          "name": "is_empty",
          "signature": "fn is_empty(&self) -> bool",
          "summary": "Is the array of zero length"
        },
        {
          "name": "is_fully_validated",
          "signature": "fn is_fully_validated(&self) -> bool",
          "summary": "True if this instance is fully [validated] for panic-free infallible accesses."
        },
        {
          "name": "iter",
          "signature": "fn iter(&self) -> impl Iterator<Item = Variant<'m, 'v>> + '_",
          "summary": "Iterates over the values of this list. When working with [unvalidated] input, consider [`Self::iter_try`] to avoid panics due to invalid data."
        },
        {
          "name": "iter_try",
          "signature": "fn iter_try(&self) -> impl Iterator<Item = Result<Variant<'m, 'v>, ArrowError>> + '_",
          "summary": "Fallible iteration over the elements of this list."
        },
        {
          "name": "len",
          "signature": "fn len(&self) -> usize",
          "summary": "Return the length of this array"
        },
        {
          "name": "new",
          "signature": "fn new(metadata: VariantMetadata<'m>, value: &'v [u8]) -> Self"
        },
        {
          "name": "try_get",
          "signature": "fn try_get(&self, index: usize) -> Result<Variant<'m, 'v>, ArrowError>",
          "summary": "Fallible version of `get`. Returns element by index, capturing validation errors"
        },
        {
          "name": "try_new",
          "signature": "fn try_new(metadata: VariantMetadata<'m>, value: &'v [u8]) -> Result<Self, ArrowError>",
          "summary": "Attempts to interpret `value` as a variant array value."
        },
        {
          "name": "with_full_validation",
          "signature": "fn with_full_validation(self) -> Result<Self, ArrowError>",
          "summary": "Performs a full [validation] of this variant array and returns the result."
        }
      ]
    }
  ]
}
