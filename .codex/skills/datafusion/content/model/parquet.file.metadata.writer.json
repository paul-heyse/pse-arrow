{
  "module": "parquet::file::metadata::writer",
  "crate": "parquet",
  "api": "api/parquet.file.metadata.writer.md",
  "items": [
    {
      "path": "parquet::file::metadata::writer::ParquetMetaDataWriter",
      "name": "ParquetMetaDataWriter",
      "kind": "struct",
      "crate": "parquet",
      "signature": "struct ParquetMetaDataWriter<'a, W: Write>",
      "summary": "Writes [`ParquetMetaData`] to a byte stream",
      "doc": "api/parquet.file.metadata.writer.md#parquetmetadatawriter",
      "aliases": [
        "parquet::file::metadata::ParquetMetaDataWriter"
      ],
      "methods": [
        {
          "name": "finish",
          "signature": "fn finish(self) -> Result<()>",
          "summary": "Write the metadata to the buffer"
        },
        {
          "name": "new",
          "signature": "fn new(buf: W, metadata: &'a ParquetMetaData) -> Self",
          "summary": "Create a new `ParquetMetaDataWriter` to write to `buf`"
        },
        {
          "name": "new_with_tracked",
          "signature": "fn new_with_tracked(buf: TrackedWrite<W>, metadata: &'a ParquetMetaData) -> Self",
          "summary": "Create a new ParquetMetaDataWriter to write to `buf`"
        },
        {
          "name": "with_write_path_in_schema",
          "signature": "fn with_write_path_in_schema(self, val: bool) -> Self",
          "summary": "Set whether or not to write the `path_in_schema` field in the Thrift `ColumnMetaData` struct."
        }
      ]
    }
  ]
}
