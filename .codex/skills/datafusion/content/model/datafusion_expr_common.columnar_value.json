{
  "module": "datafusion_expr_common::columnar_value",
  "crate": "datafusion-expr-common",
  "api": "api/datafusion_expr_common.columnar_value.md",
  "items": [
    {
      "path": "datafusion_expr_common::columnar_value::ColumnarValue",
      "name": "ColumnarValue",
      "kind": "enum",
      "crate": "datafusion-expr-common",
      "signature": "enum ColumnarValue",
      "summary": "The result of evaluating an expression.",
      "doc": "api/datafusion_expr_common.columnar_value.md#columnarvalue",
      "aliases": [
        "datafusion::logical_expr::ColumnarValue",
        "datafusion::physical_plan::ColumnarValue",
        "datafusion_expr::ColumnarValue",
        "datafusion_physical_plan::ColumnarValue",
        "datafusion_physical_plan::execution_plan::ColumnarValue"
      ],
      "variants": [
        "Array",
        "Scalar"
      ],
      "implements": [
        "core::convert::From",
        "core::convert::TryFrom",
        "core::fmt::Display"
      ],
      "methods": [
        {
          "name": "cast_to",
          "signature": "fn cast_to(&self, cast_type: &DataType, cast_options: Option<&CastOptions<'static>>) -> Result<ColumnarValue>",
          "summary": "Cast this [ColumnarValue] to the specified `DataType`"
        },
        {
          "name": "create_null_array",
          "signature": "fn create_null_array(num_rows: usize) -> Self",
          "summary": "Null columnar values are implemented as a null array in order to pass batch num_rows"
        },
        {
          "name": "data_type",
          "signature": "fn data_type(&self) -> DataType"
        },
        {
          "name": "fmt",
          "signature": "fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result",
          "via_trait": "core::fmt::Display"
        },
        {
          "name": "from",
          "signature": "fn from(value: ScalarValue) -> Self",
          "via_trait": "core::convert::From"
        },
        {
          "name": "into_array",
          "signature": "fn into_array(self, num_rows: usize) -> Result<ArrayRef>",
          "summary": "Convert any [`Self::Scalar`] into an Arrow [`ArrayRef`] with the specified number of rows  by repeating the same scalar multiple times, which is not as efficient as handling the scalar directly. [`Self::Array`] will just be returned as is."
        },
        {
          "name": "into_array_of_size",
          "signature": "fn into_array_of_size(self, num_rows: usize) -> Result<ArrayRef>",
          "summary": "Convert a columnar value into an Arrow [`ArrayRef`] with the specified number of rows. [`Self::Scalar`] is converted by repeating the same scalar multiple times which is not as efficient as handling the scalar directly. This validates that…"
        },
        {
          "name": "to_array",
          "signature": "fn to_array(&self, num_rows: usize) -> Result<ArrayRef>",
          "summary": "Convert any [`Self::Scalar`] into an Arrow [`ArrayRef`] with the specified number of rows  by repeating the same scalar multiple times, which is not as efficient as handling the scalar directly. [`Self::Array`] will just be returned as is."
        },
        {
          "name": "to_array_of_size",
          "signature": "fn to_array_of_size(&self, num_rows: usize) -> Result<ArrayRef>",
          "summary": "Convert a columnar value into an Arrow [`ArrayRef`] with the specified number of rows. [`Self::Scalar`] is converted by repeating the same scalar multiple times which is not as efficient as handling the scalar directly. This validates that…"
        },
        {
          "name": "values_to_arrays",
          "signature": "fn values_to_arrays(args: &[ColumnarValue]) -> Result<Vec<ArrayRef>>",
          "summary": "Converts  [`ColumnarValue`]s to [`ArrayRef`]s with the same length."
        }
      ]
    }
  ]
}
