{
  "module": "datafusion_physical_plan::topk",
  "crate": "datafusion-physical-plan",
  "api": "api/datafusion_physical_plan.topk.md",
  "items": [
    {
      "path": "datafusion_physical_plan::topk::TopK",
      "name": "TopK",
      "kind": "struct",
      "crate": "datafusion-physical-plan",
      "signature": "struct TopK",
      "summary": "TopK",
      "doc": "api/datafusion_physical_plan.topk.md#topk",
      "aliases": [
        "datafusion::physical_plan::TopK",
        "datafusion_physical_plan::TopK"
      ],
      "methods": [
        {
          "name": "emit",
          "signature": "fn emit(self) -> Result<SendableRecordBatchStream>",
          "summary": "Returns the top k results broken into `batch_size` [`RecordBatch`]es, consuming the heap"
        },
        {
          "name": "insert_batch",
          "signature": "fn insert_batch(&mut self, batch: RecordBatch) -> Result<()>",
          "summary": "Insert `batch`, remembering if any of its values are among the top k seen so far."
        },
        {
          "name": "try_new",
          "signature": "fn try_new(partition_id: usize, schema: SchemaRef, common_sort_prefix: Vec<PhysicalSortExpr>, expr: LexOrdering, k: usize, batch_size: usize, runtime: Arc<RuntimeEnv>, metrics: &ExecutionPlanMetricsSet, filter: Arc<RwLock<TopKDynamicFilters>>) -> Result<Self>",
          "summary": "Create a new [`TopK`] that stores the top `k` values, as defined by the sort expressions in `expr`."
        }
      ]
    },
    {
      "path": "datafusion_physical_plan::topk::TopKDynamicFilters",
      "name": "TopKDynamicFilters",
      "kind": "struct",
      "crate": "datafusion-physical-plan",
      "signature": "struct TopKDynamicFilters",
      "summary": "For more background, please also see the [Dynamic Filters: Passing Information Between Operators During Execution for 25x Faster Queries blog]",
      "doc": "api/datafusion_physical_plan.topk.md#topkdynamicfilters"
    }
  ]
}
