{
  "module": "datafusion_physical_plan::aggregates::order",
  "crate": "datafusion-physical-plan",
  "api": "api/datafusion_physical_plan.aggregates.order.md",
  "items": [
    {
      "path": "datafusion_physical_plan::aggregates::order::GroupOrdering",
      "name": "GroupOrdering",
      "kind": "enum",
      "crate": "datafusion-physical-plan",
      "signature": "enum GroupOrdering",
      "summary": "Ordering information for each group in the hash table",
      "doc": "api/datafusion_physical_plan.aggregates.order.md#groupordering",
      "variants": [
        "None",
        "Partial",
        "Full"
      ],
      "methods": [
        {
          "name": "emit_to",
          "signature": "fn emit_to(&self) -> Option<EmitTo>",
          "summary": "Returns how many groups can be emitted while respecting the current ordering guarantees, or `None` if no data can be emitted."
        },
        {
          "name": "input_done",
          "signature": "fn input_done(&mut self)",
          "summary": "Updates the state to indicate that the input is complete."
        },
        {
          "name": "new_groups",
          "signature": "fn new_groups(&mut self, batch_group_values: &[ArrayRef], group_indices: &[usize], total_num_groups: usize) -> Result<()>",
          "summary": "Called when new groups are added in a batch."
        },
        {
          "name": "oom_emit_to",
          "signature": "fn oom_emit_to(&self, n: usize) -> Option<EmitTo>",
          "summary": "Returns the emit strategy to use under memory pressure (OOM)."
        },
        {
          "name": "remove_groups",
          "signature": "fn remove_groups(&mut self, n: usize)",
          "summary": "Removes the first `n` groups from the internal state, shifting all existing indexes down by `n`."
        },
        {
          "name": "reset",
          "signature": "fn reset(&mut self)",
          "summary": "Resets the ordering state while preserving the configured ordering mode."
        },
        {
          "name": "size",
          "signature": "fn size(&self) -> usize",
          "summary": "Returns the size of memory used by the ordering state, in bytes."
        },
        {
          "name": "try_new",
          "signature": "fn try_new(mode: &InputOrderMode) -> Result<Self>",
          "summary": "Create a `GroupOrdering` for the specified ordering"
        }
      ]
    }
  ]
}
