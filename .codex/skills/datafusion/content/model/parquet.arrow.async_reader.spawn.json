{
  "module": "parquet::arrow::async_reader::spawn",
  "crate": "parquet",
  "api": "api/parquet.arrow.async_reader.spawn.md",
  "items": [
    {
      "path": "parquet::arrow::async_reader::spawn::SpawnedReader",
      "name": "SpawnedReader",
      "kind": "struct",
      "crate": "parquet",
      "signature": "struct SpawnedReader<R>",
      "summary": "An [`AsyncFileReader`] that performs I/O on a separate tokio runtime.",
      "doc": "api/parquet.arrow.async_reader.spawn.md#spawnedreader",
      "aliases": [
        "parquet::arrow::async_reader::SpawnedReader"
      ],
      "implements": [
        "parquet::arrow::async_reader::AsyncFileReader"
      ],
      "methods": [
        {
          "name": "get_byte_ranges",
          "signature": "fn get_byte_ranges(&mut self, ranges: Vec<Range<u64>>) -> BoxFuture<'_, Result<Vec<Bytes>>>",
          "via_trait": "parquet::arrow::async_reader::AsyncFileReader"
        },
        {
          "name": "get_bytes",
          "signature": "fn get_bytes(&mut self, range: Range<u64>) -> BoxFuture<'_, Result<Bytes>>",
          "via_trait": "parquet::arrow::async_reader::AsyncFileReader"
        },
        {
          "name": "get_metadata",
          "signature": "fn get_metadata<'a>(&'a mut self, options: Option<&'a ArrowReaderOptions>) -> BoxFuture<'a, Result<Arc<ParquetMetaData>>>",
          "via_trait": "parquet::arrow::async_reader::AsyncFileReader"
        },
        {
          "name": "into_inner",
          "signature": "fn into_inner(self) -> R",
          "summary": "Returns the inner reader"
        },
        {
          "name": "new",
          "signature": "fn new(inner: R, handle: Handle) -> Self",
          "summary": "Creates a new [`SpawnedReader`] that performs the I/O of `inner` on `handle`"
        }
      ]
    }
  ]
}
