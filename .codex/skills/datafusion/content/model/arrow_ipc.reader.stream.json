{
  "module": "arrow_ipc::reader::stream",
  "crate": "arrow-ipc",
  "api": "api/arrow_ipc.reader.stream.md",
  "items": [
    {
      "path": "arrow_ipc::reader::stream::StreamDecoder",
      "name": "StreamDecoder",
      "kind": "struct",
      "crate": "arrow-ipc",
      "signature": "struct StreamDecoder",
      "summary": "A low-level interface for reading [`RecordBatch`] data from a stream of bytes",
      "doc": "api/arrow_ipc.reader.stream.md#streamdecoder",
      "methods": [
        {
          "name": "decode",
          "signature": "fn decode(&mut self, buffer: &mut Buffer) -> Result<Option<RecordBatch>, ArrowError>",
          "summary": "Try to read the next [`RecordBatch`] from the provided [`Buffer`]"
        },
        {
          "name": "finish",
          "signature": "fn finish(&mut self) -> Result<(), ArrowError>",
          "summary": "Signal the end of stream"
        },
        {
          "name": "new",
          "signature": "fn new() -> Self",
          "summary": "Create a new [`StreamDecoder`]"
        },
        {
          "name": "schema",
          "signature": "fn schema(&self) -> Option<SchemaRef>",
          "summary": "Return the schema if decoded, else None."
        },
        {
          "name": "with_require_alignment",
          "signature": "fn with_require_alignment(self, require_alignment: bool) -> Self",
          "summary": "Specifies whether or not array data in input buffers is required to be properly aligned."
        },
        {
          "name": "with_skip_validation",
          "signature": "unsafe fn with_skip_validation(self, skip_validation: bool) -> Self",
          "summary": "Specifies if validation should be skipped when reading data (defaults to `false`)"
        }
      ]
    }
  ]
}
