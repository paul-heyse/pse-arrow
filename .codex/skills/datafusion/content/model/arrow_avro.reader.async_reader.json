{
  "module": "arrow_avro::reader::async_reader",
  "crate": "arrow-avro",
  "api": "api/arrow_avro.reader.async_reader.md",
  "items": [
    {
      "path": "arrow_avro::reader::async_reader::AsyncAvroFileReader",
      "name": "AsyncAvroFileReader",
      "kind": "struct",
      "crate": "arrow-avro",
      "signature": "struct AsyncAvroFileReader<R>",
      "summary": "An asynchronous Avro file reader that implements `Stream<Item = Result<RecordBatch, ArrowError>>`. This uses an [`AsyncFileReader`] to fetch data ranges as needed, starting with fetching the header, then reading all the blocks in the provi…",
      "doc": "api/arrow_avro.reader.async_reader.md#asyncavrofilereader",
      "aliases": [
        "arrow_avro::reader::AsyncAvroFileReader"
      ],
      "implements": [
        "futures_core::stream::Stream"
      ],
      "methods": [
        {
          "name": "builder",
          "signature": "fn builder(reader: R, file_size: u64, batch_size: usize) -> ReaderBuilder<R>",
          "summary": "Returns a builder for a new [`Self`], allowing some optional parameters."
        },
        {
          "name": "poll_next",
          "signature": "fn poll_next(Pin<&mut self>, cx: &mut Context<'_>) -> Poll<Option<Self::Item>>",
          "via_trait": "futures_core::stream::Stream"
        },
        {
          "name": "schema",
          "signature": "fn schema(&self) -> SchemaRef",
          "summary": "Returns the Arrow schema for batches produced by this reader."
        }
      ]
    }
  ]
}
