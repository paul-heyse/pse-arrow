{
  "module": "parquet::file::metadata::push_decoder",
  "crate": "parquet",
  "api": "api/parquet.file.metadata.push_decoder.md",
  "items": [
    {
      "path": "parquet::file::metadata::push_decoder::ParquetMetaDataPushDecoder",
      "name": "ParquetMetaDataPushDecoder",
      "kind": "struct",
      "crate": "parquet",
      "signature": "struct ParquetMetaDataPushDecoder",
      "summary": "A push decoder for [`ParquetMetaData`].",
      "doc": "api/parquet.file.metadata.push_decoder.md#parquetmetadatapushdecoder",
      "aliases": [
        "parquet::file::metadata::ParquetMetaDataPushDecoder"
      ],
      "methods": [
        {
          "name": "clear_all_ranges",
          "signature": "fn clear_all_ranges(&mut self)",
          "summary": "Clear any staged byte ranges currently buffered for future decode work."
        },
        {
          "name": "push_range",
          "signature": "fn push_range(&mut self, range: Range<u64>, buffer: Bytes) -> Result<()>",
          "summary": "Pushes a single range of data into the decoder's buffer."
        },
        {
          "name": "push_ranges",
          "signature": "fn push_ranges(&mut self, ranges: Vec<Range<u64>>, buffers: Vec<Bytes>) -> Result<()>",
          "summary": "Push the data into the decoder's buffer."
        },
        {
          "name": "try_decode",
          "signature": "fn try_decode(&mut self) -> Result<DecodeResult<ParquetMetaData>>",
          "summary": "Try to decode the metadata from the pushed data, returning the decoded metadata or an error if not enough data is available."
        },
        {
          "name": "try_new",
          "signature": "fn try_new(file_len: u64) -> Result<Self>",
          "summary": "Create a new `ParquetMetaDataPushDecoder` with the given file length."
        },
        {
          "name": "try_new_with_metadata",
          "signature": "fn try_new_with_metadata(file_len: u64, metadata: ParquetMetaData) -> Result<Self>",
          "summary": "Create a decoder with the given `ParquetMetaData` already known."
        },
        {
          "name": "with_column_index_policy",
          "signature": "fn with_column_index_policy(self, column_index_policy: PageIndexPolicy) -> Self",
          "summary": "Set the policy for reading the ColumnIndex (part of the PageIndex)"
        },
        {
          "name": "with_file_decryption_properties",
          "signature": "fn with_file_decryption_properties(self, file_decryption_properties: Option<std::sync::Arc<FileDecryptionProperties>>) -> Self",
          "summary": "Provide decryption properties for decoding encrypted Parquet files"
        },
        {
          "name": "with_metadata_options",
          "signature": "fn with_metadata_options(self, options: Option<Arc<ParquetMetaDataOptions>>) -> Self",
          "summary": "Set the options to use when decoding the Parquet metadata."
        },
        {
          "name": "with_offset_index_policy",
          "signature": "fn with_offset_index_policy(self, offset_index_policy: PageIndexPolicy) -> Self",
          "summary": "Set the policy for reading the OffsetIndex (part of the PageIndex)"
        },
        {
          "name": "with_page_index_policy",
          "signature": "fn with_page_index_policy(self, page_index_policy: PageIndexPolicy) -> Self",
          "summary": "Enable or disable reading the page index structures described in \"[Parquet page index] Layout to Support Page Skipping\"."
        }
      ]
    }
  ]
}
