{
  "module": "parquet::arrow",
  "crate": "parquet",
  "api": "api/parquet.arrow.md",
  "items": [
    {
      "path": "parquet::arrow::ARROW_SCHEMA_META_KEY",
      "name": "ARROW_SCHEMA_META_KEY",
      "kind": "constant",
      "crate": "parquet",
      "signature": "const ARROW_SCHEMA_META_KEY: &str = \"ARROW:schema\"",
      "summary": "Schema metadata key used to store serialized Arrow schema",
      "doc": "api/parquet.arrow.md#arrow_schema_meta_key"
    },
    {
      "path": "parquet::arrow::PARQUET_FIELD_ID_META_KEY",
      "name": "PARQUET_FIELD_ID_META_KEY",
      "kind": "constant",
      "crate": "parquet",
      "signature": "const PARQUET_FIELD_ID_META_KEY: &str = \"PARQUET:field_id\"",
      "summary": "The value of this metadata key, if present on [`Field::metadata`], will be used to populate [`BasicTypeInfo::id`]",
      "doc": "api/parquet.arrow.md#parquet_field_id_meta_key"
    },
    {
      "path": "parquet::arrow::parquet_column",
      "name": "parquet_column",
      "kind": "function",
      "crate": "parquet",
      "signature": "fn parquet_column<'a>(parquet_schema: &schema::types::SchemaDescriptor, arrow_schema: &'a arrow_schema::Schema, name: &str) -> Option<(usize, &'a arrow_schema::FieldRef)>",
      "summary": "Lookups up the parquet column by name",
      "doc": "api/parquet.arrow.md#parquet_column"
    },
    {
      "path": "parquet::arrow::ProjectionMask",
      "name": "ProjectionMask",
      "kind": "struct",
      "crate": "parquet",
      "signature": "struct ProjectionMask",
      "summary": "A [`ProjectionMask`] identifies a set of columns within a potentially nested schema to project",
      "doc": "api/parquet.arrow.md#projectionmask",
      "methods": [
        {
          "name": "all",
          "signature": "fn all() -> Self",
          "summary": "Create a [`ProjectionMask`] which selects all columns"
        },
        {
          "name": "columns",
          "signature": "fn columns<'a>(schema: &SchemaDescriptor, names: impl IntoIterator<Item = &'a str>) -> Self",
          "summary": "Create a [`ProjectionMask`] which selects only the named columns"
        },
        {
          "name": "intersect",
          "signature": "fn intersect(&mut self, other: &Self)",
          "summary": "Intersect two projection masks"
        },
        {
          "name": "leaf_included",
          "signature": "fn leaf_included(&self, leaf_idx: usize) -> bool",
          "summary": "Returns true if the leaf column `leaf_idx` is included by the mask"
        },
        {
          "name": "leaves",
          "signature": "fn leaves(schema: &SchemaDescriptor, indices: impl IntoIterator<Item = usize>) -> Self",
          "summary": "Create a [`ProjectionMask`] which selects only the specified leaf columns"
        },
        {
          "name": "none",
          "signature": "fn none(len: usize) -> Self",
          "summary": "Create a [`ProjectionMask`] which selects no columns"
        },
        {
          "name": "roots",
          "signature": "fn roots(schema: &SchemaDescriptor, indices: impl IntoIterator<Item = usize>) -> Self",
          "summary": "Create a [`ProjectionMask`] which selects only the specified root columns"
        },
        {
          "name": "union",
          "signature": "fn union(&mut self, other: &Self)",
          "summary": "Union two projection masks"
        }
      ]
    }
  ]
}
