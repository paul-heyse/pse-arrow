{
  "module": "parquet::schema::parser",
  "crate": "parquet",
  "api": "api/parquet.schema.parser.md",
  "items": [
    {
      "path": "parquet::schema::parser::parse_message_type",
      "name": "parse_message_type",
      "kind": "function",
      "crate": "parquet",
      "signature": "fn parse_message_type(message_type: &str) -> errors::Result<schema::types::Type>",
      "summary": "Parses message type as string into a Parquet [`Type`] which, for example, could be used to extract individual columns. Returns Parquet general error when parsing or validation fails.",
      "doc": "api/parquet.schema.parser.md#parse_message_type"
    }
  ]
}
