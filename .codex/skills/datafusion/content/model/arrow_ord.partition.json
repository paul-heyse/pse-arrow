{
  "module": "arrow_ord::partition",
  "crate": "arrow-ord",
  "api": "api/arrow_ord.partition.md",
  "items": [
    {
      "path": "arrow_ord::partition::partition",
      "name": "partition",
      "kind": "function",
      "crate": "arrow-ord",
      "signature": "fn partition(columns: &[arrow_array::ArrayRef]) -> Result<Partitions, arrow_schema::ArrowError>",
      "summary": "Given a list of lexicographically sorted columns, computes the [`Partitions`], where a partition consists of the set of consecutive rows with equal values",
      "doc": "api/arrow_ord.partition.md#partition",
      "aliases": [
        "arrow::compute::kernels::partition::partition",
        "arrow::compute::partition"
      ]
    },
    {
      "path": "arrow_ord::partition::Partitions",
      "name": "Partitions",
      "kind": "struct",
      "crate": "arrow-ord",
      "signature": "struct Partitions",
      "summary": "A computed set of partitions, see [`partition`]",
      "doc": "api/arrow_ord.partition.md#partitions",
      "aliases": [
        "arrow::compute::Partitions",
        "arrow::compute::kernels::partition::Partitions"
      ],
      "methods": [
        {
          "name": "is_empty",
          "signature": "fn is_empty(&self) -> bool",
          "summary": "Returns true if this contains no partitions"
        },
        {
          "name": "len",
          "signature": "fn len(&self) -> usize",
          "summary": "Returns the number of partitions"
        },
        {
          "name": "ranges",
          "signature": "fn ranges(&self) -> Vec<Range<usize>>",
          "summary": "Returns the range of each partition"
        }
      ]
    }
  ]
}
