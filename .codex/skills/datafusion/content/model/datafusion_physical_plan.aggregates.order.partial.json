{
  "module": "datafusion_physical_plan::aggregates::order::partial",
  "crate": "datafusion-physical-plan",
  "api": "api/datafusion_physical_plan.aggregates.order.partial.md",
  "items": [
    {
      "path": "datafusion_physical_plan::aggregates::order::partial::GroupOrderingPartial",
      "name": "GroupOrderingPartial",
      "kind": "struct",
      "crate": "datafusion-physical-plan",
      "signature": "struct GroupOrderingPartial",
      "summary": "Tracks grouping state when the data is ordered by some subset of the group keys.",
      "doc": "api/datafusion_physical_plan.aggregates.order.partial.md#grouporderingpartial",
      "aliases": [
        "datafusion_physical_plan::aggregates::order::GroupOrderingPartial"
      ],
      "methods": [
        {
          "name": "emit_to",
          "signature": "fn emit_to(&self) -> Option<EmitTo>",
          "summary": "How many groups be emitted, or None if no data can be emitted"
        },
        {
          "name": "input_done",
          "signature": "fn input_done(&mut self)",
          "summary": "Note that the input is complete so any outstanding groups are done as well"
        },
        {
          "name": "new_groups",
          "signature": "fn new_groups(&mut self, batch_group_values: &[ArrayRef], group_indices: &[usize], total_num_groups: usize) -> Result<()>",
          "summary": "Called when new groups are added in a batch. See documentation on [`super::GroupOrdering::new_groups`]"
        },
        {
          "name": "remove_groups",
          "signature": "fn remove_groups(&mut self, n: usize)",
          "summary": "remove the first n groups from the internal state, shifting all existing indexes down by `n`"
        },
        {
          "name": "reset",
          "signature": "fn reset(&mut self)",
          "summary": "Starts tracking a new ordered input segment with the same sort-key columns."
        },
        {
          "name": "try_new",
          "signature": "fn try_new(order_indices: Vec<usize>) -> Result<Self>",
          "summary": "TODO: Remove unnecessary `input_schema` parameter."
        }
      ]
    }
  ]
}
