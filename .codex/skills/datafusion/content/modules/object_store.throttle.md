# `object_store::throttle`

Full upstream contracts; raw type trees and source locators in [structured records](object_store.throttle.json).

<a id="op-24627def065b8610bf6f0f29"></a>
## throttle

`module` · `object_store::throttle` · object_store 0.13.2

```rust
mod throttle
```

Source: `src/throttle.rs:18`. [Exact documentation build](https://docs.rs/crate/object_store/0.13.2/json).

A throttling object store wrapper
