# `object_store::client`

Full upstream contracts; raw type trees and source locators in [structured records](object_store.client.json).

<a id="op-988ade6019fb87cb45c4e7ff"></a>
## client

`module` · `object_store::client` · object_store 0.13.2

```rust
mod client
```

Source: `src/client/mod.rs:18`. [Exact documentation build](https://docs.rs/crate/object_store/0.13.2/json).

Generic utilities for [`reqwest`] based [`ObjectStore`] implementations

[`ObjectStore`]: crate::ObjectStore

Unresolved upstream links (retained, not inferred): ``reqwest``.
