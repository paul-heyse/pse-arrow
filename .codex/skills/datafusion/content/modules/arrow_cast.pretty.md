# `arrow_cast::pretty`

Full upstream contracts; raw type trees and source locators in [structured records](arrow_cast.pretty.json).

<a id="op-f83724b3b9fd6b9155cf350d"></a>
## pretty

`module` · `arrow_cast::pretty` · arrow-cast 59.3.0

```rust
mod pretty
```

Source: `src/pretty.rs:18`. [Exact documentation build](https://docs.rs/crate/arrow-cast/59.3.0/json).

Utilities for pretty printing [`RecordBatch`]es and [`Array`]s.

Note this module is not available unless `feature = "prettyprint"` is enabled.

[`RecordBatch`]: arrow_array::RecordBatch
[`Array`]: arrow_array::Array
