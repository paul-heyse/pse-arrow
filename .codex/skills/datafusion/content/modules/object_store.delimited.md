# `object_store::delimited`

Full upstream contracts; raw type trees and source locators in [structured records](object_store.delimited.json).

<a id="op-3bd237256505abb66de595e2"></a>
## delimited

`module` · `object_store::delimited` · object_store 0.13.2

```rust
mod delimited
```

Source: `src/delimited.rs:18`. [Exact documentation build](https://docs.rs/crate/object_store/0.13.2/json).

Utility for streaming newline delimited files from object storage
