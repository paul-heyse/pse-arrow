# `object_store::buffered`

Full upstream contracts; raw type trees and source locators in [structured records](object_store.buffered.json).

<a id="op-c51a1582651e4519a6b3a9df"></a>
## buffered

`module` · `object_store::buffered` · object_store 0.13.2

```rust
mod buffered
```

Source: `src/buffered.rs:18`. [Exact documentation build](https://docs.rs/crate/object_store/0.13.2/json).

Utilities for performing tokio-style buffered IO
