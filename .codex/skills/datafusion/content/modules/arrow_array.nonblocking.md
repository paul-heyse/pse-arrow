# `arrow_array::nonblocking`

Full upstream contracts; raw type trees and source locators in [structured records](arrow_array.nonblocking.json).

<a id="op-30fc627f05f95a61d87a0ca1"></a>
## nonblocking

`module` · `arrow_array::nonblocking` · arrow-array 59.3.0

```rust
mod nonblocking
```

Source: `src/nonblocking.rs:18`. [Exact documentation build](https://docs.rs/crate/arrow-array/59.3.0/json).

Trait definitions specific to using Arrow with async.
