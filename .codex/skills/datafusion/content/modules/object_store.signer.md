# `object_store::signer`

Full upstream contracts; raw type trees and source locators in [structured records](object_store.signer.json).

<a id="op-9ecd63f04dec36ab26d2fb2f"></a>
## signer

`module` · `object_store::signer` · object_store 0.13.2

```rust
mod signer
```

Source: `src/signer.rs:18`. [Exact documentation build](https://docs.rs/crate/object_store/0.13.2/json).

Abstraction of signed URL generation for those object store implementations that support it
