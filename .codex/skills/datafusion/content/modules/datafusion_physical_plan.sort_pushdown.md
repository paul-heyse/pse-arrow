# `datafusion_physical_plan::sort_pushdown`

Full upstream contracts; raw type trees and source locators in [structured records](datafusion_physical_plan.sort_pushdown.json).

<a id="op-8a8154b4835bc7f892f88e54"></a>
## sort_pushdown

`module` · `datafusion_physical_plan::sort_pushdown` · datafusion-physical-plan 55.1.0

```rust
mod sort_pushdown
```

Source: `src/sort_pushdown.rs:18`. [Exact documentation build](https://docs.rs/crate/datafusion-physical-plan/55.1.0/json).

Sort pushdown types for physical execution plans.

This module provides types used for pushing sort ordering requirements
down through the execution plan tree to data sources.
