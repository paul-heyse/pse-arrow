# `object_store::chunked`

Full upstream contracts; raw type trees and source locators in [structured records](object_store.chunked.json).

<a id="op-72f8bff2f6ac8cc1cf8973b0"></a>
## chunked

`module` · `object_store::chunked` · object_store 0.13.2

```rust
mod chunked
```

Source: `src/chunked.rs:18`. [Exact documentation build](https://docs.rs/crate/object_store/0.13.2/json).

A [`ChunkedStore`](../operations/object_store.chunked.ChunkedStore.md#op-b2eebc76c44478d71e96fcb7) that can be used to test streaming behaviour
