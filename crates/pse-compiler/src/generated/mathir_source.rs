// SPDX-License-Identifier: MIT OR Apache-2.0
// Copyright (c) 2026 Paul Heyse
// @generated by pse-schema; do not edit

//! Exact generated Arrow views feed the existing mathematical algorithm callbacks.
use pse_mathir::{
    DomainRef, GuardRef, MathIrError, NodeId, Opcode, TemplateValueKind, ValueRef,
};
use pse_quantity::{
    BoundIndexId, ConversionId, DomainId, InvariantId, OperationId, QuantityTypeId,
    ReductionKind, UnitId, WeightNormalization, infer::BuiltInRule,
};
use crate::mathir_relations::malformed;
#[expect(
    clippy::needless_pass_by_value,
    reason = "map_err consumes the relation error at the mathematical boundary"
)]
fn relation_error(error: pse_relations::RelationError) -> MathIrError {
    malformed(error.to_string())
}
fn enumeration<T>(value: &str, parse: fn(&str) -> Option<T>) -> Result<T, MathIrError> {
    parse(value).ok_or_else(|| malformed("unknown mathematical enum member"))
}
fn guard(
    node: Option<u64>,
    source: Option<pse_ids::SemanticId>,
    predicate: Option<u64>,
) -> Result<Option<GuardRef>, MathIrError> {
    if node.is_none() && source.is_none() && predicate.is_none() {
        return Ok(None);
    }
    GuardRef::from_columns(node.map(NodeId), source, predicate).map(Some)
}
/// Replay one exact generated family into the domain algorithm, without a Cell model.
pub(crate) fn replay(
    key: pse_schema::model::RelationKey,
    batch: &pse_relations::columnar::FieldCheckedBatch,
    sink: &mut dyn pse_mathir::relations::MathRelationSink,
) -> Result<(), MathIrError> {
    match (key.namespace.as_str(), key.name) {
        ("compiled", "kernel_bindings") => {
            let view = pse_relations::generated::r#compiled::r#kernel_bindings::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let parameters = row
                    .parameter_bindings
                    .into_iter()
                    .map(|parameter| (
                        parameter.name,
                        parameter.symbol_id,
                        parameter.value,
                        parameter.unit_id.map(UnitId::from_id),
                    ))
                    .collect::<Vec<_>>();
                let inputs = row
                    .input_bindings
                    .into_iter()
                    .map(|input| (input.name, NodeId(input.node_id)))
                    .collect::<Vec<_>>();
                sink.kernel_binding(
                    row.binding_id,
                    row.kernel_id,
                    row.scope_instance_id,
                    &parameters,
                    &inputs,
                )?;
            }
        }
        ("inferred", "kernel_bindings") => {
            let view = pse_relations::generated::r#inferred::r#kernel_bindings::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let parameters = row
                    .parameter_bindings
                    .into_iter()
                    .map(|parameter| (
                        parameter.name,
                        parameter.symbol_id,
                        parameter.value,
                        parameter.unit_id.map(UnitId::from_id),
                    ))
                    .collect::<Vec<_>>();
                let inputs = row
                    .input_bindings
                    .into_iter()
                    .map(|input| (input.name, NodeId(input.node_id)))
                    .collect::<Vec<_>>();
                sink.kernel_binding(
                    row.binding_id,
                    row.kernel_id,
                    row.scope_instance_id,
                    &parameters,
                    &inputs,
                )?;
            }
        }
        ("compiled", "math_affine") => {
            let view = pse_relations::generated::r#compiled::r#math_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("inferred", "math_affine") => {
            let view = pse_relations::generated::r#inferred::r#math_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("normalized", "template_expr_affine") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("normalized", "instance_expr_affine") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("normalized", "display_expr_affine") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("normalized", "contribution_expr_affine") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("normalized", "guard_expr_affine") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_affine::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let terms = row
                    .terms
                    .into_iter()
                    .map(|term| (term.coefficient, NodeId(term.child_node_id)))
                    .collect::<Vec<_>>();
                sink.affine(
                    NodeId(row.node_id),
                    row.constant,
                    row.constant_quantity_type_id.map(QuantityTypeId::from_id),
                    row.constant_unit_id.map(UnitId::from_id),
                    &terms,
                )?;
            }
        }
        ("compiled", "math_broadcasts") => {
            let view = pse_relations::generated::r#compiled::r#math_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::Actual(DomainId::from_id(row.r#domain_id)),
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("inferred", "math_broadcasts") => {
            let view = pse_relations::generated::r#inferred::r#math_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::Actual(DomainId::from_id(row.r#domain_id)),
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("normalized", "template_expr_broadcasts") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("normalized", "instance_expr_broadcasts") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("normalized", "display_expr_broadcasts") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("normalized", "contribution_expr_broadcasts") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("normalized", "guard_expr_broadcasts") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_broadcasts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.broadcast(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                )?;
            }
        }
        ("compiled", "math_complementarity") => {
            let view = pse_relations::generated::r#compiled::r#math_complementarity::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_complementarity") => {
            let view = pse_relations::generated::r#inferred::r#math_complementarity::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_conditionals") => {
            let view = pse_relations::generated::r#compiled::r#math_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(Some(row.r#guard_node_id), None, None)?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("inferred", "math_conditionals") => {
            let view = pse_relations::generated::r#inferred::r#math_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(Some(row.r#guard_node_id), None, None)?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("normalized", "template_expr_conditionals") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(
                            row.r#guard_node_id,
                            row.r#guard_source_id,
                            row.r#guard_predicate_id,
                        )?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("normalized", "instance_expr_conditionals") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(
                            row.r#guard_node_id,
                            row.r#guard_source_id,
                            row.r#guard_predicate_id,
                        )?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("normalized", "display_expr_conditionals") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(
                            row.r#guard_node_id,
                            row.r#guard_source_id,
                            row.r#guard_predicate_id,
                        )?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("normalized", "contribution_expr_conditionals") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(
                            row.r#guard_node_id,
                            row.r#guard_source_id,
                            row.r#guard_predicate_id,
                        )?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("normalized", "guard_expr_conditionals") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_conditionals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.conditional(
                    NodeId(row.node_id),
                    guard(
                            row.r#guard_node_id,
                            row.r#guard_source_id,
                            row.r#guard_predicate_id,
                        )?
                        .ok_or_else(|| malformed("conditional guard absent"))?,
                )?;
            }
        }
        ("compiled", "math_dae_links") => {
            let view = pse_relations::generated::r#compiled::r#math_dae_links::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_dae_links") => {
            let view = pse_relations::generated::r#inferred::r#math_dae_links::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_derivatives") => {
            let view = pse_relations::generated::r#compiled::r#math_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::Actual(DomainId::from_id(row.r#wrt_domain_id)),
                    row.order,
                )?;
            }
        }
        ("inferred", "math_derivatives") => {
            let view = pse_relations::generated::r#inferred::r#math_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::Actual(DomainId::from_id(row.r#wrt_domain_id)),
                    row.order,
                )?;
            }
        }
        ("normalized", "template_expr_derivatives") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#wrt_domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    row.order,
                )?;
            }
        }
        ("normalized", "instance_expr_derivatives") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#wrt_domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    row.order,
                )?;
            }
        }
        ("normalized", "display_expr_derivatives") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#wrt_domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    row.order,
                )?;
            }
        }
        ("normalized", "contribution_expr_derivatives") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#wrt_domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    row.order,
                )?;
            }
        }
        ("normalized", "guard_expr_derivatives") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_derivatives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.derivative(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#wrt_domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    row.order,
                )?;
            }
        }
        ("compiled", "math_equations") => {
            let view = pse_relations::generated::r#compiled::r#math_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_equations") => {
            let view = pse_relations::generated::r#inferred::r#math_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_expr_args") => {
            let view = pse_relations::generated::r#compiled::r#math_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("inferred", "math_expr_args") => {
            let view = pse_relations::generated::r#inferred::r#math_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("normalized", "template_expr_args") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("normalized", "instance_expr_args") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("normalized", "display_expr_args") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("normalized", "contribution_expr_args") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("normalized", "guard_expr_args") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_args::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_arg(
                    NodeId(row.parent_node_id),
                    row.argument_ordinal,
                    NodeId(row.child_node_id),
                )?;
            }
        }
        ("compiled", "math_expr_nodes") => {
            let view = pse_relations::generated::r#compiled::r#math_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("inferred", "math_expr_nodes") => {
            let view = pse_relations::generated::r#inferred::r#math_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "template_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "instance_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "display_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "contribution_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "guard_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.expr_node(
                    NodeId(row.node_id),
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("compiled", "math_float_constants") => {
            let view = pse_relations::generated::r#compiled::r#math_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("inferred", "math_float_constants") => {
            let view = pse_relations::generated::r#inferred::r#math_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("normalized", "template_expr_float_constants") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("normalized", "instance_expr_float_constants") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("normalized", "display_expr_float_constants") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("normalized", "contribution_expr_float_constants") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("normalized", "guard_expr_float_constants") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_float_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.float_constant(
                    NodeId(row.node_id),
                    row.value,
                    UnitId::from_id(row.unit_id),
                )?;
            }
        }
        ("compiled", "math_free_indices") => {
            let view = pse_relations::generated::r#compiled::r#math_free_indices::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.free_index(
                    row.indexed_equation_id,
                    BoundIndexId::from_id(row.bound_index_id),
                    DomainId::from_id(row.domain_id),
                    row.position,
                )?;
            }
        }
        ("inferred", "math_free_indices") => {
            let view = pse_relations::generated::r#inferred::r#math_free_indices::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.free_index(
                    row.indexed_equation_id,
                    BoundIndexId::from_id(row.bound_index_id),
                    DomainId::from_id(row.domain_id),
                    row.position,
                )?;
            }
        }
        ("compiled", "math_gathers") => {
            let view = pse_relations::generated::r#compiled::r#math_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let coordinates = row
                    .coordinate_map
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("inferred", "math_gathers") => {
            let view = pse_relations::generated::r#inferred::r#math_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let coordinates = row
                    .coordinate_map
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("normalized", "template_expr_gathers") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.gather_state.as_str() == "pending" {
                    if row.coordinate_map.is_some() {
                        return Err(
                            malformed("pending gather carries resolved coordinates"),
                        );
                    }
                    let indices = row
                        .index_nodes
                        .ok_or_else(|| malformed("pending gather indices absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_gather(NodeId(row.node_id), row.group_id, &indices)?;
                    continue;
                }
                if row.index_nodes.is_some() {
                    return Err(malformed("resolved gather carries pending indices"));
                }
                let coordinates = row
                    .coordinate_map
                    .ok_or_else(|| malformed("resolved gather coordinates absent"))?
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("normalized", "instance_expr_gathers") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.gather_state.as_str() == "pending" {
                    if row.coordinate_map.is_some() {
                        return Err(
                            malformed("pending gather carries resolved coordinates"),
                        );
                    }
                    let indices = row
                        .index_nodes
                        .ok_or_else(|| malformed("pending gather indices absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_gather(NodeId(row.node_id), row.group_id, &indices)?;
                    continue;
                }
                if row.index_nodes.is_some() {
                    return Err(malformed("resolved gather carries pending indices"));
                }
                let coordinates = row
                    .coordinate_map
                    .ok_or_else(|| malformed("resolved gather coordinates absent"))?
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("normalized", "display_expr_gathers") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.gather_state.as_str() == "pending" {
                    if row.coordinate_map.is_some() {
                        return Err(
                            malformed("pending gather carries resolved coordinates"),
                        );
                    }
                    let indices = row
                        .index_nodes
                        .ok_or_else(|| malformed("pending gather indices absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_gather(NodeId(row.node_id), row.group_id, &indices)?;
                    continue;
                }
                if row.index_nodes.is_some() {
                    return Err(malformed("resolved gather carries pending indices"));
                }
                let coordinates = row
                    .coordinate_map
                    .ok_or_else(|| malformed("resolved gather coordinates absent"))?
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("normalized", "contribution_expr_gathers") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.gather_state.as_str() == "pending" {
                    if row.coordinate_map.is_some() {
                        return Err(
                            malformed("pending gather carries resolved coordinates"),
                        );
                    }
                    let indices = row
                        .index_nodes
                        .ok_or_else(|| malformed("pending gather indices absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_gather(NodeId(row.node_id), row.group_id, &indices)?;
                    continue;
                }
                if row.index_nodes.is_some() {
                    return Err(malformed("resolved gather carries pending indices"));
                }
                let coordinates = row
                    .coordinate_map
                    .ok_or_else(|| malformed("resolved gather coordinates absent"))?
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("normalized", "guard_expr_gathers") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_gathers::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.gather_state.as_str() == "pending" {
                    if row.coordinate_map.is_some() {
                        return Err(
                            malformed("pending gather carries resolved coordinates"),
                        );
                    }
                    let indices = row
                        .index_nodes
                        .ok_or_else(|| malformed("pending gather indices absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_gather(NodeId(row.node_id), row.group_id, &indices)?;
                    continue;
                }
                if row.index_nodes.is_some() {
                    return Err(malformed("resolved gather carries pending indices"));
                }
                let coordinates = row
                    .coordinate_map
                    .ok_or_else(|| malformed("resolved gather coordinates absent"))?
                    .into_iter()
                    .map(|coordinate| (
                        BoundIndexId::from_id(coordinate.bound_index_id),
                        coordinate.position,
                    ))
                    .collect::<Vec<_>>();
                sink.gather(NodeId(row.node_id), row.group_id, &coordinates)?;
            }
        }
        ("compiled", "math_implicit_refs") => {
            let view = pse_relations::generated::r#compiled::r#math_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("inferred", "math_implicit_refs") => {
            let view = pse_relations::generated::r#inferred::r#math_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("normalized", "template_expr_implicit_refs") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("normalized", "instance_expr_implicit_refs") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("normalized", "display_expr_implicit_refs") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("normalized", "contribution_expr_implicit_refs") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("normalized", "guard_expr_implicit_refs") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_implicit_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.implicit_ref(
                    NodeId(row.node_id),
                    row.implicit_system_id,
                    row.unknown_ordinal,
                )?;
            }
        }
        ("compiled", "math_indexed_equations") => {
            let view = pse_relations::generated::r#compiled::r#math_indexed_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.indexed_equation(
                    row.indexed_equation_id,
                    row.owner_instance_id,
                    row.equation_decl_id,
                    &row.qualified_name,
                    row.product_id,
                    row.filter_node_id.map(NodeId),
                    NodeId(row.body_node_id),
                    enumeration(row.sense.as_str(), pse_mathir::equation::Sense::parse)?,
                    row.lower_node_id.map(NodeId),
                    row.upper_node_id.map(NodeId),
                    row.residual_quantity_type_id.map(QuantityTypeId::from_id),
                    row.law_instance_id,
                    row.derivation_id,
                )?;
            }
        }
        ("inferred", "math_indexed_equations") => {
            let view = pse_relations::generated::r#inferred::r#math_indexed_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.indexed_equation(
                    row.indexed_equation_id,
                    row.owner_instance_id,
                    row.equation_decl_id,
                    &row.qualified_name,
                    row.product_id,
                    row.filter_node_id.map(NodeId),
                    NodeId(row.body_node_id),
                    enumeration(row.sense.as_str(), pse_mathir::equation::Sense::parse)?,
                    row.lower_node_id.map(NodeId),
                    row.upper_node_id.map(NodeId),
                    row.residual_quantity_type_id.map(QuantityTypeId::from_id),
                    row.law_instance_id,
                    row.derivation_id,
                )?;
            }
        }
        ("compiled", "math_int_constants") => {
            let view = pse_relations::generated::r#compiled::r#math_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("inferred", "math_int_constants") => {
            let view = pse_relations::generated::r#inferred::r#math_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("normalized", "template_expr_int_constants") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("normalized", "instance_expr_int_constants") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("normalized", "display_expr_int_constants") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("normalized", "contribution_expr_int_constants") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("normalized", "guard_expr_int_constants") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_int_constants::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.int_constant(NodeId(row.node_id), row.value)?;
            }
        }
        ("compiled", "math_integrals") => {
            let view = pse_relations::generated::r#compiled::r#math_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::Actual(DomainId::from_id(row.r#domain_id)),
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(row.r#filter_node_id, None, None)?,
                )?;
            }
        }
        ("inferred", "math_integrals") => {
            let view = pse_relations::generated::r#inferred::r#math_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::Actual(DomainId::from_id(row.r#domain_id)),
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(row.r#filter_node_id, None, None)?,
                )?;
            }
        }
        ("normalized", "template_expr_integrals") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "instance_expr_integrals") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "display_expr_integrals") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "contribution_expr_integrals") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "guard_expr_integrals") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_integrals::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.integral(
                    NodeId(row.node_id),
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    row.quadrature_policy_id,
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("compiled", "math_kernel_calls") => {
            let view = pse_relations::generated::r#compiled::r#math_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("inferred", "math_kernel_calls") => {
            let view = pse_relations::generated::r#inferred::r#math_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("normalized", "template_expr_kernel_calls") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("normalized", "instance_expr_kernel_calls") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("normalized", "display_expr_kernel_calls") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("normalized", "contribution_expr_kernel_calls") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("normalized", "guard_expr_kernel_calls") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_kernel_calls::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.kernel_call(
                    NodeId(row.node_id),
                    row.kernel_binding_id,
                    row.output_ordinal,
                )?;
            }
        }
        ("compiled", "math_objectives") => {
            let view = pse_relations::generated::r#compiled::r#math_objectives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_objectives") => {
            let view = pse_relations::generated::r#inferred::r#math_objectives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_piecewise_linear") => {
            let view = pse_relations::generated::r#compiled::r#math_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("inferred", "math_piecewise_linear") => {
            let view = pse_relations::generated::r#inferred::r#math_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("normalized", "template_expr_piecewise_linear") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("normalized", "instance_expr_piecewise_linear") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("normalized", "display_expr_piecewise_linear") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("normalized", "contribution_expr_piecewise_linear") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("normalized", "guard_expr_piecewise_linear") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_piecewise_linear::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let points = row
                    .breakpoints
                    .into_iter()
                    .map(|point| (point.x, point.y))
                    .collect::<Vec<_>>();
                sink.piecewise_linear(
                    NodeId(row.node_id),
                    &points,
                    QuantityTypeId::from_id(row.input_quantity_type_id),
                    QuantityTypeId::from_id(row.output_quantity_type_id),
                )?;
            }
        }
        ("compiled", "math_quantity_selections") => {
            let view = pse_relations::generated::r#compiled::r#math_quantity_selections::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let conversions = row
                    .conversions
                    .into_iter()
                    .map(|conversion| (
                        conversion.operand,
                        ConversionId::from_id(conversion.conversion_id),
                    ))
                    .collect::<Vec<_>>();
                sink.quantity_selection(
                    NodeId(row.node_id),
                    row.operation_id.map(OperationId::from_id),
                    row
                        .builtin_rule
                        .map(|rule| enumeration(rule.as_str(), BuiltInRule::parse))
                        .transpose()?,
                    &row.operand_permutation,
                    &conversions,
                    row.deferred_static_check,
                )?;
            }
        }
        ("inferred", "math_quantity_selections") => {
            let view = pse_relations::generated::r#inferred::r#math_quantity_selections::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let conversions = row
                    .conversions
                    .into_iter()
                    .map(|conversion| (
                        conversion.operand,
                        ConversionId::from_id(conversion.conversion_id),
                    ))
                    .collect::<Vec<_>>();
                sink.quantity_selection(
                    NodeId(row.node_id),
                    row.operation_id.map(OperationId::from_id),
                    row
                        .builtin_rule
                        .map(|rule| enumeration(rule.as_str(), BuiltInRule::parse))
                        .transpose()?,
                    &row.operand_permutation,
                    &conversions,
                    row.deferred_static_check,
                )?;
            }
        }
        ("compiled", "math_reductions") => {
            let view = pse_relations::generated::r#compiled::r#math_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::Actual(DomainId::from_id(row.r#domain_id)),
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(row.r#filter_node_id, None, None)?,
                )?;
            }
        }
        ("inferred", "math_reductions") => {
            let view = pse_relations::generated::r#inferred::r#math_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::Actual(DomainId::from_id(row.r#domain_id)),
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(row.r#filter_node_id, None, None)?,
                )?;
            }
        }
        ("normalized", "template_expr_reductions") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "instance_expr_reductions") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "display_expr_reductions") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "contribution_expr_reductions") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("normalized", "guard_expr_reductions") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_reductions::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.reduction(
                    NodeId(row.node_id),
                    enumeration(row.kind.as_str(), ReductionKind::parse)?,
                    DomainRef::from_columns(
                        row.r#domain_id.map(DomainId::from_id),
                        row.template_id,
                        row.domain_name,
                    )?,
                    BoundIndexId::from_id(row.bound_index_id),
                    guard(
                        row.r#filter_node_id,
                        row.r#filter_source_id,
                        row.r#filter_predicate_id,
                    )?,
                )?;
            }
        }
        ("compiled", "math_smooth_ops") => {
            let view = pse_relations::generated::r#compiled::r#math_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.smooth_op(NodeId(row.node_id), row.eps)?;
            }
        }
        ("inferred", "math_smooth_ops") => {
            let view = pse_relations::generated::r#inferred::r#math_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if !row.eps.is_finite() || row.eps <= 0.0 {
                    return Err(malformed("epsilon must be finite and positive"));
                }
                match (row.epsilon_state.as_str(), row.eps_unit_id) {
                    ("coordinate", None) => {
                        sink.smooth_op(NodeId(row.node_id), row.eps)?;
                    }
                    ("pending_unit", Some(unit)) => {
                        sink.pending_smooth_op(
                            NodeId(row.node_id),
                            row.eps,
                            UnitId::from_id(unit),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete smoothing alternatives"),
                        );
                    }
                }
            }
        }
        ("normalized", "template_expr_smooth_ops") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if !row.eps.is_finite() || row.eps <= 0.0 {
                    return Err(malformed("epsilon must be finite and positive"));
                }
                match (row.epsilon_state.as_str(), row.eps_unit_id) {
                    ("coordinate", None) => {
                        sink.smooth_op(NodeId(row.node_id), row.eps)?;
                    }
                    ("pending_unit", Some(unit)) => {
                        sink.pending_smooth_op(
                            NodeId(row.node_id),
                            row.eps,
                            UnitId::from_id(unit),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete smoothing alternatives"),
                        );
                    }
                }
            }
        }
        ("normalized", "instance_expr_smooth_ops") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if !row.eps.is_finite() || row.eps <= 0.0 {
                    return Err(malformed("epsilon must be finite and positive"));
                }
                match (row.epsilon_state.as_str(), row.eps_unit_id) {
                    ("coordinate", None) => {
                        sink.smooth_op(NodeId(row.node_id), row.eps)?;
                    }
                    ("pending_unit", Some(unit)) => {
                        sink.pending_smooth_op(
                            NodeId(row.node_id),
                            row.eps,
                            UnitId::from_id(unit),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete smoothing alternatives"),
                        );
                    }
                }
            }
        }
        ("normalized", "display_expr_smooth_ops") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if !row.eps.is_finite() || row.eps <= 0.0 {
                    return Err(malformed("epsilon must be finite and positive"));
                }
                match (row.epsilon_state.as_str(), row.eps_unit_id) {
                    ("coordinate", None) => {
                        sink.smooth_op(NodeId(row.node_id), row.eps)?;
                    }
                    ("pending_unit", Some(unit)) => {
                        sink.pending_smooth_op(
                            NodeId(row.node_id),
                            row.eps,
                            UnitId::from_id(unit),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete smoothing alternatives"),
                        );
                    }
                }
            }
        }
        ("normalized", "contribution_expr_smooth_ops") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if !row.eps.is_finite() || row.eps <= 0.0 {
                    return Err(malformed("epsilon must be finite and positive"));
                }
                match (row.epsilon_state.as_str(), row.eps_unit_id) {
                    ("coordinate", None) => {
                        sink.smooth_op(NodeId(row.node_id), row.eps)?;
                    }
                    ("pending_unit", Some(unit)) => {
                        sink.pending_smooth_op(
                            NodeId(row.node_id),
                            row.eps,
                            UnitId::from_id(unit),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete smoothing alternatives"),
                        );
                    }
                }
            }
        }
        ("normalized", "guard_expr_smooth_ops") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_smooth_ops::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if !row.eps.is_finite() || row.eps <= 0.0 {
                    return Err(malformed("epsilon must be finite and positive"));
                }
                match (row.epsilon_state.as_str(), row.eps_unit_id) {
                    ("coordinate", None) => {
                        sink.smooth_op(NodeId(row.node_id), row.eps)?;
                    }
                    ("pending_unit", Some(unit)) => {
                        sink.pending_smooth_op(
                            NodeId(row.node_id),
                            row.eps,
                            UnitId::from_id(unit),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete smoothing alternatives"),
                        );
                    }
                }
            }
        }
        ("compiled", "math_symbol_refs") => {
            let view = pse_relations::generated::r#compiled::r#math_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.symbol_ref(
                    NodeId(row.node_id),
                    ValueRef::ActualSymbol(row.symbol_id),
                )?;
            }
        }
        ("inferred", "math_symbol_refs") => {
            let view = pse_relations::generated::r#inferred::r#math_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.symbol_ref(
                    NodeId(row.node_id),
                    ValueRef::ActualSymbol(row.symbol_id),
                )?;
            }
        }
        ("normalized", "template_expr_symbol_refs") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.kind.as_str() == pse_mathir::Payload::PENDING_PATH_KIND {
                    if row.symbol_id.is_some() || row.template_id.is_some()
                        || row.name.is_some() || row.domain_id.is_some()
                        || row.bound_index_id.is_some()
                    {
                        return Err(
                            malformed("path reference overlaps another alternative"),
                        );
                    }
                    let indices = row
                        .path_index_nodes
                        .ok_or_else(|| malformed("path index inventory absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_path(
                        NodeId(row.node_id),
                        row
                            .path_source_id
                            .ok_or_else(|| malformed("path source absent"))?,
                        row.path_id.ok_or_else(|| malformed("path ordinal absent"))?,
                        &indices,
                    )?;
                    continue;
                }
                if row.path_source_id.is_some() || row.path_id.is_some()
                    || row.path_index_nodes.is_some()
                {
                    return Err(malformed("ordinary reference carries path fields"));
                }
                let reference = match (
                    row.kind.as_str(),
                    row.symbol_id,
                    row.template_id,
                    row.name,
                    row.domain_id,
                    row.bound_index_id,
                ) {
                    ("symbol", Some(id), None, None, None, None) => {
                        ValueRef::ActualSymbol(id)
                    }
                    ("index", None, None, None, None, Some(id)) => {
                        ValueRef::Index(BoundIndexId::from_id(id))
                    }
                    ("domain", None, template, name, domain, None) => {
                        ValueRef::Domain(
                            DomainRef::from_columns(
                                domain.map(DomainId::from_id),
                                template,
                                name,
                            )?,
                        )
                    }
                    (kind, None, Some(template_id), Some(name), None, None) => {
                        ValueRef::Template {
                            template_id,
                            name,
                            kind: TemplateValueKind::ALL
                                .into_iter()
                                .find(|value| value.as_str() == kind)
                                .ok_or_else(|| malformed(
                                    "unknown template reference kind",
                                ))?,
                        }
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete reference alternatives"),
                        );
                    }
                };
                sink.symbol_ref(NodeId(row.node_id), reference)?;
            }
        }
        ("normalized", "instance_expr_symbol_refs") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.kind.as_str() == pse_mathir::Payload::PENDING_PATH_KIND {
                    if row.symbol_id.is_some() || row.template_id.is_some()
                        || row.name.is_some() || row.domain_id.is_some()
                        || row.bound_index_id.is_some()
                    {
                        return Err(
                            malformed("path reference overlaps another alternative"),
                        );
                    }
                    let indices = row
                        .path_index_nodes
                        .ok_or_else(|| malformed("path index inventory absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_path(
                        NodeId(row.node_id),
                        row
                            .path_source_id
                            .ok_or_else(|| malformed("path source absent"))?,
                        row.path_id.ok_or_else(|| malformed("path ordinal absent"))?,
                        &indices,
                    )?;
                    continue;
                }
                if row.path_source_id.is_some() || row.path_id.is_some()
                    || row.path_index_nodes.is_some()
                {
                    return Err(malformed("ordinary reference carries path fields"));
                }
                let reference = match (
                    row.kind.as_str(),
                    row.symbol_id,
                    row.template_id,
                    row.name,
                    row.domain_id,
                    row.bound_index_id,
                ) {
                    ("symbol", Some(id), None, None, None, None) => {
                        ValueRef::ActualSymbol(id)
                    }
                    ("index", None, None, None, None, Some(id)) => {
                        ValueRef::Index(BoundIndexId::from_id(id))
                    }
                    ("domain", None, template, name, domain, None) => {
                        ValueRef::Domain(
                            DomainRef::from_columns(
                                domain.map(DomainId::from_id),
                                template,
                                name,
                            )?,
                        )
                    }
                    (kind, None, Some(template_id), Some(name), None, None) => {
                        ValueRef::Template {
                            template_id,
                            name,
                            kind: TemplateValueKind::ALL
                                .into_iter()
                                .find(|value| value.as_str() == kind)
                                .ok_or_else(|| malformed(
                                    "unknown template reference kind",
                                ))?,
                        }
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete reference alternatives"),
                        );
                    }
                };
                sink.symbol_ref(NodeId(row.node_id), reference)?;
            }
        }
        ("normalized", "display_expr_symbol_refs") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.kind.as_str() == pse_mathir::Payload::PENDING_PATH_KIND {
                    if row.symbol_id.is_some() || row.template_id.is_some()
                        || row.name.is_some() || row.domain_id.is_some()
                        || row.bound_index_id.is_some()
                    {
                        return Err(
                            malformed("path reference overlaps another alternative"),
                        );
                    }
                    let indices = row
                        .path_index_nodes
                        .ok_or_else(|| malformed("path index inventory absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_path(
                        NodeId(row.node_id),
                        row
                            .path_source_id
                            .ok_or_else(|| malformed("path source absent"))?,
                        row.path_id.ok_or_else(|| malformed("path ordinal absent"))?,
                        &indices,
                    )?;
                    continue;
                }
                if row.path_source_id.is_some() || row.path_id.is_some()
                    || row.path_index_nodes.is_some()
                {
                    return Err(malformed("ordinary reference carries path fields"));
                }
                let reference = match (
                    row.kind.as_str(),
                    row.symbol_id,
                    row.template_id,
                    row.name,
                    row.domain_id,
                    row.bound_index_id,
                ) {
                    ("symbol", Some(id), None, None, None, None) => {
                        ValueRef::ActualSymbol(id)
                    }
                    ("index", None, None, None, None, Some(id)) => {
                        ValueRef::Index(BoundIndexId::from_id(id))
                    }
                    ("domain", None, template, name, domain, None) => {
                        ValueRef::Domain(
                            DomainRef::from_columns(
                                domain.map(DomainId::from_id),
                                template,
                                name,
                            )?,
                        )
                    }
                    (kind, None, Some(template_id), Some(name), None, None) => {
                        ValueRef::Template {
                            template_id,
                            name,
                            kind: TemplateValueKind::ALL
                                .into_iter()
                                .find(|value| value.as_str() == kind)
                                .ok_or_else(|| malformed(
                                    "unknown template reference kind",
                                ))?,
                        }
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete reference alternatives"),
                        );
                    }
                };
                sink.symbol_ref(NodeId(row.node_id), reference)?;
            }
        }
        ("normalized", "contribution_expr_symbol_refs") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.kind.as_str() == pse_mathir::Payload::PENDING_PATH_KIND {
                    if row.symbol_id.is_some() || row.template_id.is_some()
                        || row.name.is_some() || row.domain_id.is_some()
                        || row.bound_index_id.is_some()
                    {
                        return Err(
                            malformed("path reference overlaps another alternative"),
                        );
                    }
                    let indices = row
                        .path_index_nodes
                        .ok_or_else(|| malformed("path index inventory absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_path(
                        NodeId(row.node_id),
                        row
                            .path_source_id
                            .ok_or_else(|| malformed("path source absent"))?,
                        row.path_id.ok_or_else(|| malformed("path ordinal absent"))?,
                        &indices,
                    )?;
                    continue;
                }
                if row.path_source_id.is_some() || row.path_id.is_some()
                    || row.path_index_nodes.is_some()
                {
                    return Err(malformed("ordinary reference carries path fields"));
                }
                let reference = match (
                    row.kind.as_str(),
                    row.symbol_id,
                    row.template_id,
                    row.name,
                    row.domain_id,
                    row.bound_index_id,
                ) {
                    ("symbol", Some(id), None, None, None, None) => {
                        ValueRef::ActualSymbol(id)
                    }
                    ("index", None, None, None, None, Some(id)) => {
                        ValueRef::Index(BoundIndexId::from_id(id))
                    }
                    ("domain", None, template, name, domain, None) => {
                        ValueRef::Domain(
                            DomainRef::from_columns(
                                domain.map(DomainId::from_id),
                                template,
                                name,
                            )?,
                        )
                    }
                    (kind, None, Some(template_id), Some(name), None, None) => {
                        ValueRef::Template {
                            template_id,
                            name,
                            kind: TemplateValueKind::ALL
                                .into_iter()
                                .find(|value| value.as_str() == kind)
                                .ok_or_else(|| malformed(
                                    "unknown template reference kind",
                                ))?,
                        }
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete reference alternatives"),
                        );
                    }
                };
                sink.symbol_ref(NodeId(row.node_id), reference)?;
            }
        }
        ("normalized", "guard_expr_symbol_refs") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_symbol_refs::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                if row.kind.as_str() == pse_mathir::Payload::PENDING_PATH_KIND {
                    if row.symbol_id.is_some() || row.template_id.is_some()
                        || row.name.is_some() || row.domain_id.is_some()
                        || row.bound_index_id.is_some()
                    {
                        return Err(
                            malformed("path reference overlaps another alternative"),
                        );
                    }
                    let indices = row
                        .path_index_nodes
                        .ok_or_else(|| malformed("path index inventory absent"))?
                        .into_iter()
                        .map(NodeId)
                        .collect::<Vec<_>>();
                    sink.pending_path(
                        NodeId(row.node_id),
                        row
                            .path_source_id
                            .ok_or_else(|| malformed("path source absent"))?,
                        row.path_id.ok_or_else(|| malformed("path ordinal absent"))?,
                        &indices,
                    )?;
                    continue;
                }
                if row.path_source_id.is_some() || row.path_id.is_some()
                    || row.path_index_nodes.is_some()
                {
                    return Err(malformed("ordinary reference carries path fields"));
                }
                let reference = match (
                    row.kind.as_str(),
                    row.symbol_id,
                    row.template_id,
                    row.name,
                    row.domain_id,
                    row.bound_index_id,
                ) {
                    ("symbol", Some(id), None, None, None, None) => {
                        ValueRef::ActualSymbol(id)
                    }
                    ("index", None, None, None, None, Some(id)) => {
                        ValueRef::Index(BoundIndexId::from_id(id))
                    }
                    ("domain", None, template, name, domain, None) => {
                        ValueRef::Domain(
                            DomainRef::from_columns(
                                domain.map(DomainId::from_id),
                                template,
                                name,
                            )?,
                        )
                    }
                    (kind, None, Some(template_id), Some(name), None, None) => {
                        ValueRef::Template {
                            template_id,
                            name,
                            kind: TemplateValueKind::ALL
                                .into_iter()
                                .find(|value| value.as_str() == kind)
                                .ok_or_else(|| malformed(
                                    "unknown template reference kind",
                                ))?,
                        }
                    }
                    _ => {
                        return Err(
                            malformed("overlapping or incomplete reference alternatives"),
                        );
                    }
                };
                sink.symbol_ref(NodeId(row.node_id), reference)?;
            }
        }
        ("compiled", "math_unit_converts") => {
            let view = pse_relations::generated::r#compiled::r#math_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.unit_convert(
                    NodeId(row.node_id),
                    row.scale,
                    row.offset,
                    UnitId::from_id(row.from_unit_id),
                    UnitId::from_id(row.to_unit_id),
                )?;
            }
        }
        ("inferred", "math_unit_converts") => {
            let view = pse_relations::generated::r#inferred::r#math_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                match (
                    row.conversion_state.as_str(),
                    row.scale,
                    row.offset,
                    row.from_unit_id,
                ) {
                    ("pending", None, None, None) => {
                        sink.pending_unit_convert(
                            NodeId(row.node_id),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    ("resolved", Some(scale), Some(offset), Some(from)) => {
                        sink.unit_convert(
                            NodeId(row.node_id),
                            scale,
                            offset,
                            UnitId::from_id(from),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed(
                                "overlapping or incomplete conversion alternatives",
                            ),
                        );
                    }
                }
            }
        }
        ("normalized", "template_expr_unit_converts") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                match (
                    row.conversion_state.as_str(),
                    row.scale,
                    row.offset,
                    row.from_unit_id,
                ) {
                    ("pending", None, None, None) => {
                        sink.pending_unit_convert(
                            NodeId(row.node_id),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    ("resolved", Some(scale), Some(offset), Some(from)) => {
                        sink.unit_convert(
                            NodeId(row.node_id),
                            scale,
                            offset,
                            UnitId::from_id(from),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed(
                                "overlapping or incomplete conversion alternatives",
                            ),
                        );
                    }
                }
            }
        }
        ("normalized", "instance_expr_unit_converts") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                match (
                    row.conversion_state.as_str(),
                    row.scale,
                    row.offset,
                    row.from_unit_id,
                ) {
                    ("pending", None, None, None) => {
                        sink.pending_unit_convert(
                            NodeId(row.node_id),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    ("resolved", Some(scale), Some(offset), Some(from)) => {
                        sink.unit_convert(
                            NodeId(row.node_id),
                            scale,
                            offset,
                            UnitId::from_id(from),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed(
                                "overlapping or incomplete conversion alternatives",
                            ),
                        );
                    }
                }
            }
        }
        ("normalized", "display_expr_unit_converts") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                match (
                    row.conversion_state.as_str(),
                    row.scale,
                    row.offset,
                    row.from_unit_id,
                ) {
                    ("pending", None, None, None) => {
                        sink.pending_unit_convert(
                            NodeId(row.node_id),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    ("resolved", Some(scale), Some(offset), Some(from)) => {
                        sink.unit_convert(
                            NodeId(row.node_id),
                            scale,
                            offset,
                            UnitId::from_id(from),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed(
                                "overlapping or incomplete conversion alternatives",
                            ),
                        );
                    }
                }
            }
        }
        ("normalized", "contribution_expr_unit_converts") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                match (
                    row.conversion_state.as_str(),
                    row.scale,
                    row.offset,
                    row.from_unit_id,
                ) {
                    ("pending", None, None, None) => {
                        sink.pending_unit_convert(
                            NodeId(row.node_id),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    ("resolved", Some(scale), Some(offset), Some(from)) => {
                        sink.unit_convert(
                            NodeId(row.node_id),
                            scale,
                            offset,
                            UnitId::from_id(from),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed(
                                "overlapping or incomplete conversion alternatives",
                            ),
                        );
                    }
                }
            }
        }
        ("normalized", "guard_expr_unit_converts") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_unit_converts::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                match (
                    row.conversion_state.as_str(),
                    row.scale,
                    row.offset,
                    row.from_unit_id,
                ) {
                    ("pending", None, None, None) => {
                        sink.pending_unit_convert(
                            NodeId(row.node_id),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    ("resolved", Some(scale), Some(offset), Some(from)) => {
                        sink.unit_convert(
                            NodeId(row.node_id),
                            scale,
                            offset,
                            UnitId::from_id(from),
                            UnitId::from_id(row.to_unit_id),
                        )?;
                    }
                    _ => {
                        return Err(
                            malformed(
                                "overlapping or incomplete conversion alternatives",
                            ),
                        );
                    }
                }
            }
        }
        ("compiled", "math_weighted_means") => {
            let view = pse_relations::generated::r#compiled::r#math_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        ("inferred", "math_weighted_means") => {
            let view = pse_relations::generated::r#inferred::r#math_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        ("normalized", "template_expr_weighted_means") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        ("normalized", "instance_expr_weighted_means") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        ("normalized", "display_expr_weighted_means") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        ("normalized", "contribution_expr_weighted_means") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        ("normalized", "guard_expr_weighted_means") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_weighted_means::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let pairs = row
                    .pairs
                    .into_iter()
                    .map(|pair| (
                        NodeId(pair.weight_node_id),
                        NodeId(pair.value_node_id),
                    ))
                    .collect::<Vec<_>>();
                sink.weighted_mean(
                    NodeId(row.node_id),
                    &pairs,
                    enumeration(row.normalization.as_str(), WeightNormalization::parse)?,
                    row.unit_sum_invariant_id.map(InvariantId::from_id),
                )?;
            }
        }
        _ => return Err(malformed("unregistered mathematical source family")),
    }
    Ok(())
}
