// SPDX-License-Identifier: MIT OR Apache-2.0
// Copyright (c) 2026 Paul Heyse
// @generated by pse-schema; do not edit

//! Exact generated Arrow views feed the existing mathematical algorithm callbacks.
use pse_mathir::{DomainRef, GuardRef, MathIrError, NodeId, Opcode, Payload, ValueRef};
use pse_schema::math::TemplateValueKind;
use pse_quantity::{
    BoundIndexId, ConversionId, DomainId, InvariantId, OperationId, QuantityTypeId,
    ReductionKind, UnitId, WeightNormalization, infer::BuiltInRule,
};
use crate::mathir_relations::malformed;
#[expect(
    clippy::needless_pass_by_value,
    reason = "map_err consumes the relation error at the mathematical boundary"
)]
fn relation_error(error: pse_relations::RelationError) -> MathIrError {
    malformed(error.to_string())
}
fn node(value: i64) -> Result<NodeId, MathIrError> {
    if value < 0 {
        Err(malformed("negative mathematical node ordinal"))
    } else {
        Ok(NodeId(value))
    }
}
fn narrow(value: i64) -> Result<u16, MathIrError> {
    u16::try_from(value).map_err(|_| malformed("mathematical position exceeds u16"))
}
fn enumeration<T>(value: &str, parse: fn(&str) -> Option<T>) -> Result<T, MathIrError> {
    parse(value).ok_or_else(|| malformed("unknown mathematical enum member"))
}
/// Replay one exact generated family into the domain algorithm, without a Cell model.
pub(crate) fn replay(
    key: pse_schema::model::RelationKey,
    batch: &pse_relations::columnar::FieldCheckedBatch,
    sink: &mut dyn pse_mathir::relations::MathRelationSink,
) -> Result<(), MathIrError> {
    match (key.namespace.as_str(), key.name) {
        ("compiled", "kernel_bindings") => {
            let view = pse_relations::generated::r#compiled::r#kernel_bindings::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let parameters = row
                    .parameter_bindings
                    .into_iter()
                    .map(|parameter| {
                        parameter.binding.selected().map_err(relation_error)?;
                        let (symbol, value, unit) = if let Some(symbol) = parameter
                            .binding
                            .symbol
                        {
                            (Some(symbol.symbol_id), None, None)
                        } else {
                            let literal = parameter
                                .binding
                                .literal
                                .ok_or_else(|| malformed("missing kernel literal"))?;
                            (
                                None,
                                Some(literal.value),
                                Some(UnitId::from_id(literal.unit_id)),
                            )
                        };
                        Ok((parameter.name, symbol, value, unit))
                    })
                    .collect::<Result<Vec<_>, MathIrError>>()?;
                let inputs = row
                    .input_bindings
                    .into_iter()
                    .map(|input| Ok((input.name, node(input.node_id)?)))
                    .collect::<Result<Vec<_>, MathIrError>>()?;
                sink.kernel_binding(
                    row.binding_id,
                    row.kernel_id,
                    row.scope_instance_id,
                    &parameters,
                    &inputs,
                )?;
            }
        }
        ("inferred", "kernel_bindings") => {
            let view = pse_relations::generated::r#inferred::r#kernel_bindings::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let parameters = row
                    .parameter_bindings
                    .into_iter()
                    .map(|parameter| {
                        parameter.binding.selected().map_err(relation_error)?;
                        let (symbol, value, unit) = if let Some(symbol) = parameter
                            .binding
                            .symbol
                        {
                            (Some(symbol.symbol_id), None, None)
                        } else {
                            let literal = parameter
                                .binding
                                .literal
                                .ok_or_else(|| malformed("missing kernel literal"))?;
                            (
                                None,
                                Some(literal.value),
                                Some(UnitId::from_id(literal.unit_id)),
                            )
                        };
                        Ok((parameter.name, symbol, value, unit))
                    })
                    .collect::<Result<Vec<_>, MathIrError>>()?;
                let inputs = row
                    .input_bindings
                    .into_iter()
                    .map(|input| Ok((input.name, node(input.node_id)?)))
                    .collect::<Result<Vec<_>, MathIrError>>()?;
                sink.kernel_binding(
                    row.binding_id,
                    row.kernel_id,
                    row.scope_instance_id,
                    &parameters,
                    &inputs,
                )?;
            }
        }
        ("compiled", "math_complementarity") => {
            let view = pse_relations::generated::r#compiled::r#math_complementarity::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_complementarity") => {
            let view = pse_relations::generated::r#inferred::r#math_complementarity::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_dae_links") => {
            let view = pse_relations::generated::r#compiled::r#math_dae_links::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_dae_links") => {
            let view = pse_relations::generated::r#inferred::r#math_dae_links::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_equations") => {
            let view = pse_relations::generated::r#compiled::r#math_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_equations") => {
            let view = pse_relations::generated::r#inferred::r#math_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_expr_nodes") => {
            let view = pse_relations::generated::r#compiled::r#math_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, false, false)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("inferred", "math_expr_nodes") => {
            let view = pse_relations::generated::r#inferred::r#math_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, false, true)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "template_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#template_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, true, true)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "instance_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#instance_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, true, true)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "display_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#display_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, true, true)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "contribution_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#contribution_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, true, true)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("normalized", "guard_expr_nodes") => {
            let view = pse_relations::generated::r#normalized::r#guard_expr_nodes::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let payload = {
                    macro_rules! required {
                        ($value:expr) => {
                            $value .ok_or_else(||
                            malformed("selected mathematical value arm is absent")) ?
                        };
                    }
                    macro_rules! ordinal {
                        ($value:expr) => {
                            { let value = $value; if value < 0 { return
                            Err(malformed("negative mathematical ordinal")); } value }
                        };
                    }
                    macro_rules! domain {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "actual" =>
                            { DomainRef::Actual(DomainId::from_id(required!(value.actual)
                            .domain_id)) } "template" => { let value = required!(value
                            .template); DomainRef::Template { template_id : value
                            .template_id, domain_name : value.name, } } _ => return
                            Err(malformed("unknown mathematical domain alternative")), }
                            }
                        };
                    }
                    macro_rules! guard {
                        ($value:expr) => {
                            { let value = $value; match value.kind.as_str() { "math" =>
                            GuardRef::Math(NodeId(ordinal!(required!(value.math)
                            .node_id))), "predicate" => { let value = required!(value
                            .predicate); GuardRef::Predicate { source_id : value
                            .source_id, predicate_id : ordinal!(value.predicate_id), } }
                            _ => return
                            Err(malformed("unknown mathematical guard alternative")), } }
                        };
                    }
                    let value = row.payload;
                    match value.kind.as_str() {
                        "none" => Payload::None,
                        "symbol" => {
                            let value = required!(value.symbol).reference;
                            let symbol = match value.kind.as_str() {
                                "symbol" => {
                                    ValueRef::ActualSymbol(required!(value.symbol).symbol_id)
                                }
                                "template" => {
                                    let value = required!(value.template);
                                    ValueRef::Template {
                                        template_id: value.template_id,
                                        kind: TemplateValueKind::ALL
                                            .into_iter()
                                            .find(|kind| kind.as_str() == value.member_kind.as_str())
                                            .ok_or_else(|| malformed("invalid template member kind"))?,
                                        name: value.name,
                                    }
                                }
                                "domain" => {
                                    ValueRef::Domain(domain!(required!(value.domain) .value))
                                }
                                "index" => {
                                    ValueRef::Index(
                                        BoundIndexId::from_id(required!(value.index).bound_index_id),
                                    )
                                }
                                _ => {
                                    return Err(
                                        malformed("unknown mathematical reference alternative"),
                                    );
                                }
                            };
                            Payload::SymbolRef { symbol }
                        }
                        "float" => {
                            let value = required!(value.float);
                            Payload::FloatConst {
                                value: value.value,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "integer" => {
                            Payload::IntConst {
                                value: required!(value.integer).value,
                            }
                        }
                        "affine" => {
                            let value = required!(value.affine);
                            if value.coefficients.len() != row.children.len() {
                                return Err(
                                    malformed("affine coefficient/child cardinality differs"),
                                );
                            }
                            Payload::Affine {
                                constant: value.constant,
                                constant_quantity_type: value
                                    .constant_quantity_type_id
                                    .map(QuantityTypeId::from_id),
                                constant_unit: value.constant_unit_id.map(UnitId::from_id),
                                terms: value
                                    .coefficients
                                    .into_iter()
                                    .zip(&row.children)
                                    .map(|(coefficient, child)| {
                                        Ok(pse_mathir::AffineTerm {
                                            coefficient,
                                            child: NodeId(ordinal!(* child)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "weighted_mean" => {
                            let value = required!(value.weighted_mean);
                            Payload::WeightedMean {
                                pairs: value
                                    .pairs
                                    .into_iter()
                                    .map(|pair| {
                                        Ok(pse_mathir::WeightedPair {
                                            weight: NodeId(ordinal!(pair.weight_node_id)),
                                            value: NodeId(ordinal!(pair.value_node_id)),
                                        })
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                                normalization: enumeration(
                                    value.normalization.as_str(),
                                    WeightNormalization::parse,
                                )?,
                                unit_sum_invariant: value
                                    .unit_sum_invariant_id
                                    .map(InvariantId::from_id),
                            }
                        }
                        "reduction" => {
                            let value = required!(value.reduction);
                            Payload::Reduction {
                                kind: enumeration(
                                    value.reduction_kind.as_str(),
                                    ReductionKind::parse,
                                )?,
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "gather" => {
                            let value = required!(value.gather);
                            Payload::Gather {
                                group: value.group_id,
                                coordinate_map: value
                                    .coordinates
                                    .into_iter()
                                    .map(|value| {
                                        Ok((
                                            BoundIndexId::from_id(value.bound_index_id),
                                            u16::try_from(value.position)
                                                .map_err(|_| malformed("coordinate width"))?,
                                        ))
                                    })
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_gather" => {
                            let value = required!(value.pending_gather);
                            Payload::PendingGather {
                                group: value.group_id,
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "pending_path" => {
                            let value = required!(value.pending_path);
                            Payload::PendingPath {
                                source_id: value.source_id,
                                path_id: ordinal!(value.path_id),
                                indices: value
                                    .indices
                                    .into_iter()
                                    .map(|id| Ok(NodeId(ordinal!(id))))
                                    .collect::<Result<_, MathIrError>>()?,
                            }
                        }
                        "broadcast" => {
                            let value = required!(value.broadcast);
                            Payload::Broadcast {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                            }
                        }
                        "derivative" => {
                            let value = required!(value.derivative);
                            Payload::Derivative {
                                wrt_domain: domain!(value.domain),
                                order: u8::try_from(value.order)
                                    .map_err(|_| malformed("derivative order width"))?,
                            }
                        }
                        "integral" => {
                            let value = required!(value.integral);
                            Payload::Integral {
                                domain: domain!(value.domain),
                                bound_index: BoundIndexId::from_id(value.bound_index_id),
                                quadrature_policy: value.quadrature_policy_id,
                                filter: value
                                    .filter
                                    .map(|value| Ok(guard!(value)))
                                    .transpose()?,
                            }
                        }
                        "smooth" => {
                            Payload::SmoothOp {
                                eps: required!(value.smooth).eps,
                            }
                        }
                        "pending_smooth" => {
                            let value = required!(value.pending_smooth);
                            Payload::PendingSmoothOp {
                                eps: value.eps,
                                unit: UnitId::from_id(value.unit_id),
                            }
                        }
                        "conditional" => {
                            Payload::Conditional {
                                guard: guard!(required!(value.conditional) .guard),
                            }
                        }
                        "kernel_call" => {
                            let value = required!(value.kernel_call);
                            Payload::KernelCall {
                                kernel_binding: value.binding_id,
                                output_ordinal: u16::try_from(value.output_ordinal)
                                    .map_err(|_| malformed("kernel output width"))?,
                            }
                        }
                        "implicit_ref" => {
                            let value = required!(value.implicit_ref);
                            Payload::ImplicitRef {
                                implicit_system: value.system_id,
                                unknown_ordinal: u16::try_from(value.unknown_ordinal)
                                    .map_err(|_| malformed("implicit unknown width"))?,
                            }
                        }
                        "unit_convert" => {
                            let value = required!(value.unit_convert);
                            Payload::UnitConvert(pse_quantity::UnitConvertSpec {
                                scale: value.scale,
                                offset: value.offset,
                                from: UnitId::from_id(value.from_unit_id),
                                to: UnitId::from_id(value.to_unit_id),
                            })
                        }
                        "pending_unit_convert" => {
                            Payload::PendingUnitConvert {
                                to: UnitId::from_id(
                                    required!(value.pending_unit_convert).to_unit_id,
                                ),
                            }
                        }
                        "piecewise_linear" => {
                            let value = required!(value.piecewise_linear);
                            Payload::PiecewiseLinear {
                                breakpoints: value
                                    .breakpoints
                                    .into_iter()
                                    .map(|point| (point.x, point.y))
                                    .collect(),
                                input: QuantityTypeId::from_id(
                                    value.input_quantity_type_id,
                                ),
                                output: QuantityTypeId::from_id(
                                    value.output_quantity_type_id,
                                ),
                            }
                        }
                        _ => {
                            return Err(
                                malformed("unknown mathematical payload alternative"),
                            );
                        }
                    }
                };
                crate::mathir_relations::check_payload_family(&payload, true, true)?;
                let children = row
                    .children
                    .into_iter()
                    .map(node)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.expr_node(
                    node(row.node_id)?,
                    enumeration(row.opcode.as_str(), Opcode::parse)?,
                    &children,
                    &payload,
                    row.quantity_type_id.map(QuantityTypeId::from_id),
                    row.scope_instance_id,
                    row.subtree_hash,
                )?;
            }
        }
        ("compiled", "math_free_indices") => {
            let view = pse_relations::generated::r#compiled::r#math_free_indices::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.free_index(
                    row.indexed_equation_id,
                    BoundIndexId::from_id(row.bound_index_id),
                    DomainId::from_id(row.domain_id),
                    narrow(row.position)?,
                )?;
            }
        }
        ("inferred", "math_free_indices") => {
            let view = pse_relations::generated::r#inferred::r#math_free_indices::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                sink.free_index(
                    row.indexed_equation_id,
                    BoundIndexId::from_id(row.bound_index_id),
                    DomainId::from_id(row.domain_id),
                    narrow(row.position)?,
                )?;
            }
        }
        ("compiled", "math_indexed_equations") => {
            let view = pse_relations::generated::r#compiled::r#math_indexed_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let sense = enumeration(
                    row.constraint.kind.as_str(),
                    pse_schema::math::Sense::parse,
                )?;
                row.constraint
                    .selected()
                    .map_err(|_| malformed("invalid equation constraint alternative"))?;
                let (lower, upper) = if let Some(range) = row.constraint.range {
                    (Some(node(range.lower_node_id)?), Some(node(range.upper_node_id)?))
                } else {
                    let target = node(
                        row
                            .constraint
                            .single
                            .ok_or_else(|| malformed("missing equation target"))?
                            .node_id,
                    )?;
                    if sense == pse_schema::math::Sense::Ge {
                        (Some(target), None)
                    } else {
                        (None, Some(target))
                    }
                };
                sink.indexed_equation(
                    row.indexed_equation_id,
                    row.owner_instance_id,
                    row.equation_decl_id,
                    &row.qualified_name,
                    row.product_id,
                    row.filter_node_id.map(node).transpose()?,
                    node(row.body_node_id)?,
                    sense,
                    lower,
                    upper,
                    row.residual_quantity_type_id.map(QuantityTypeId::from_id),
                    row.law_instance_id,
                    row.derivation_id,
                )?;
            }
        }
        ("inferred", "math_indexed_equations") => {
            let view = pse_relations::generated::r#inferred::r#math_indexed_equations::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let sense = enumeration(
                    row.constraint.kind.as_str(),
                    pse_schema::math::Sense::parse,
                )?;
                row.constraint
                    .selected()
                    .map_err(|_| malformed("invalid equation constraint alternative"))?;
                let (lower, upper) = if let Some(range) = row.constraint.range {
                    (Some(node(range.lower_node_id)?), Some(node(range.upper_node_id)?))
                } else {
                    let target = node(
                        row
                            .constraint
                            .single
                            .ok_or_else(|| malformed("missing equation target"))?
                            .node_id,
                    )?;
                    if sense == pse_schema::math::Sense::Ge {
                        (Some(target), None)
                    } else {
                        (None, Some(target))
                    }
                };
                sink.indexed_equation(
                    row.indexed_equation_id,
                    row.owner_instance_id,
                    row.equation_decl_id,
                    &row.qualified_name,
                    row.product_id,
                    row.filter_node_id.map(node).transpose()?,
                    node(row.body_node_id)?,
                    sense,
                    lower,
                    upper,
                    row.residual_quantity_type_id.map(QuantityTypeId::from_id),
                    row.law_instance_id,
                    row.derivation_id,
                )?;
            }
        }
        ("compiled", "math_objectives") => {
            let view = pse_relations::generated::r#compiled::r#math_objectives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("inferred", "math_objectives") => {
            let view = pse_relations::generated::r#inferred::r#math_objectives::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            if !view.is_empty() {
                return Err(
                    malformed(
                        "grounded mathematical output is not an indexed graph source",
                    ),
                );
            }
        }
        ("compiled", "math_quantity_selections") => {
            let view = pse_relations::generated::r#compiled::r#math_quantity_selections::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let conversions = row
                    .conversions
                    .into_iter()
                    .map(|conversion| Ok((
                        narrow(conversion.operand)?,
                        ConversionId::from_id(conversion.conversion_id),
                    )))
                    .collect::<Result<Vec<_>, MathIrError>>()?;
                let permutation = row
                    .operand_permutation
                    .into_iter()
                    .map(narrow)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.quantity_selection(
                    node(row.node_id)?,
                    row.operation_id.map(OperationId::from_id),
                    row
                        .builtin_rule
                        .map(|rule| enumeration(rule.as_str(), BuiltInRule::parse))
                        .transpose()?,
                    &permutation,
                    &conversions,
                    row.deferred_static_check,
                )?;
            }
        }
        ("inferred", "math_quantity_selections") => {
            let view = pse_relations::generated::r#inferred::r#math_quantity_selections::View::from_checked(
                    batch,
                )
                .map_err(relation_error)?;
            for index in 0..view.len() {
                let row = view.row(index).map_err(relation_error)?;
                let conversions = row
                    .conversions
                    .into_iter()
                    .map(|conversion| Ok((
                        narrow(conversion.operand)?,
                        ConversionId::from_id(conversion.conversion_id),
                    )))
                    .collect::<Result<Vec<_>, MathIrError>>()?;
                let permutation = row
                    .operand_permutation
                    .into_iter()
                    .map(narrow)
                    .collect::<Result<Vec<_>, _>>()?;
                sink.quantity_selection(
                    node(row.node_id)?,
                    row.operation_id.map(OperationId::from_id),
                    row
                        .builtin_rule
                        .map(|rule| enumeration(rule.as_str(), BuiltInRule::parse))
                        .transpose()?,
                    &permutation,
                    &conversions,
                    row.deferred_static_check,
                )?;
            }
        }
        _ => return Err(malformed("unregistered mathematical source family")),
    }
    Ok(())
}
