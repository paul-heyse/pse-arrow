// SPDX-License-Identifier: MIT OR Apache-2.0
// Copyright (c) 2026 Paul Heyse
// @generated by pse-schema; do not edit

/// Registry-generated semantic values; native codecs remain local.
pub use pse_model::generated::r#authored::r#physical_balances::{
    AuthoredPhysicalBalancesFieldTermsItem, AuthoredPhysicalBalancesFieldImpulsesItem,
    AuthoredPhysicalBalancesRow, Row,
};
/// The declared relation identity.
pub const RELATION_ID: pse_ids::SemanticId = pse_ids::SemanticId::from_bytes([
    27u8, 62u8, 133u8, 206u8, 66u8, 207u8, 85u8, 122u8, 113u8, 227u8, 28u8, 62u8, 204u8,
    156u8, 230u8, 179u8,
]);
/// The declared name within its namespace.
pub const NAME: &str = "physical_balances";
/// The declared namespace.
pub const NAMESPACE: pse_schema::model::Namespace = pse_schema::model::Namespace::Authored;
/// The schema generation.
pub const VERSION: u32 = 1u32;
/// Generated interchange fingerprint, not proof of semantic equivalence or row validity.
pub const FINGERPRINT: pse_ids::ContentHash = pse_ids::ContentHash::from_bytes([
    158u8, 190u8, 163u8, 168u8, 182u8, 239u8, 251u8, 244u8, 33u8, 19u8, 67u8, 229u8,
    106u8, 164u8, 4u8, 251u8, 33u8, 15u8, 137u8, 240u8, 122u8, 218u8, 160u8, 130u8, 62u8,
    48u8, 125u8, 77u8, 120u8, 252u8, 109u8, 217u8,
]);
impl crate::columnar::ArrowValue for AuthoredPhysicalBalancesFieldTermsItem {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#source_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#role, children[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#transfer_id,
            children[2usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#mode, children[3usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#instance_id,
            children[4usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#output, children[5usize].as_mut())?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <crate::generated::enums::BalanceRole as crate::columnar::ArrowValue>::append_null(
            children[1usize].as_mut(),
        )?;
        <Option<
            pse_ids::SemanticId,
        > as crate::columnar::ArrowValue>::append_null(children[2usize].as_mut())?;
        <Option<
            i64,
        > as crate::columnar::ArrowValue>::append_null(children[3usize].as_mut())?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[4usize].as_mut(),
        )?;
        <i64 as crate::columnar::ArrowValue>::append_null(children[5usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#source_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#role: <crate::generated::enums::BalanceRole as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#transfer_id: <Option<
                pse_ids::SemanticId,
            > as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
            r#mode: <Option<
                i64,
            > as crate::columnar::ArrowValue>::read(
                input.column(3usize).as_ref(),
                index,
            )?,
            r#instance_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(4usize).as_ref(),
                index,
            )?,
            r#output: <i64 as crate::columnar::ArrowValue>::read(
                input.column(5usize).as_ref(),
                index,
            )?,
        })
    }
}
impl crate::columnar::ArrowValue for AuthoredPhysicalBalancesFieldImpulsesItem {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#event_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#value, children[1usize].as_mut())?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[1usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#event_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#value: <f64 as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
        })
    }
}
impl crate::columnar::ArrowValue for AuthoredPhysicalBalancesRow {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#balance_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#model_id,
            children[1usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#case_id, children[2usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#quantity_id,
            children[3usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#accumulation,
            children[4usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#tolerance,
            children[5usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#integral_tolerance,
            children[6usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#provenance,
            children[7usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#terms, children[8usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#impulses,
            children[9usize].as_mut(),
        )?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[1usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[2usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[3usize].as_mut(),
        )?;
        <Option<
            pse_ids::SemanticId,
        > as crate::columnar::ArrowValue>::append_null(children[4usize].as_mut())?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[5usize].as_mut())?;
        <Option<
            f64,
        > as crate::columnar::ArrowValue>::append_null(children[6usize].as_mut())?;
        <String as crate::columnar::ArrowValue>::append_null(children[7usize].as_mut())?;
        <Vec<
            AuthoredPhysicalBalancesFieldTermsItem,
        > as crate::columnar::ArrowValue>::append_null(children[8usize].as_mut())?;
        <Vec<
            AuthoredPhysicalBalancesFieldImpulsesItem,
        > as crate::columnar::ArrowValue>::append_null(children[9usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#balance_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#model_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#case_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
            r#quantity_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(3usize).as_ref(),
                index,
            )?,
            r#accumulation: <Option<
                pse_ids::SemanticId,
            > as crate::columnar::ArrowValue>::read(
                input.column(4usize).as_ref(),
                index,
            )?,
            r#tolerance: <f64 as crate::columnar::ArrowValue>::read(
                input.column(5usize).as_ref(),
                index,
            )?,
            r#integral_tolerance: <Option<
                f64,
            > as crate::columnar::ArrowValue>::read(
                input.column(6usize).as_ref(),
                index,
            )?,
            r#provenance: <String as crate::columnar::ArrowValue>::read(
                input.column(7usize).as_ref(),
                index,
            )?,
            r#terms: <Vec<
                AuthoredPhysicalBalancesFieldTermsItem,
            > as crate::columnar::ArrowValue>::read(
                input.column(8usize).as_ref(),
                index,
            )?,
            r#impulses: <Vec<
                AuthoredPhysicalBalancesFieldImpulsesItem,
            > as crate::columnar::ArrowValue>::read(
                input.column(9usize).as_ref(),
                index,
            )?,
        })
    }
}
/// The concrete generated relation view.
pub type View<'a> = AuthoredPhysicalBalancesView<'a>;
/// The concrete generated relation builder.
pub type Builder = AuthoredPhysicalBalancesBuilder;
/// Resolves this exact generated contract in a runtime registry.
/// # Errors
/// A missing or incompatible declaration.
pub fn spec(
    reg: &pse_schema::Registry,
) -> Result<&pse_schema::model::RelationSpec, crate::RelationError> {
    let spec = reg
        .relation_by_id(RELATION_ID)
        .ok_or_else(|| crate::RelationError::UnknownRegistry {
            relation: "authored.physical_balances".to_owned(),
        })?;
    check_declaration(reg, spec)?;
    Ok(spec)
}
fn check_declaration(
    reg: &pse_schema::Registry,
    spec: &pse_schema::model::RelationSpec,
) -> Result<(), crate::RelationError> {
    reg.contract(spec)?.require_generated(crate::generated::contracts::expected()?)?;
    Ok(())
}
/// The schema built from the authoritative declaration.
/// # Errors
/// An unavailable registry or incompatible generated contract.
pub fn schema() -> Result<crate::SchemaRef, crate::RelationError> {
    let reg = pse_schema::registry()?;
    Ok(pse_schema::arrow::relation_schema_ref(reg, spec(reg)?)?)
}
/// Checks schema, recursive extension contracts and visible values.
/// # Errors
/// All independently actionable violations.
pub fn validate(batch: &crate::RecordBatch) -> Result<(), Vec<crate::RelationError>> {
    let reg = pse_schema::registry().map_err(|error| vec![error.into()])?;
    crate::validate::validate_batch(reg, spec(reg).map_err(|error| vec![error])?, batch)
}
impl crate::columnar::RelationRow for AuthoredPhysicalBalancesRow {
    type Builder = AuthoredPhysicalBalancesBuilder;
    fn append_columns(
        &self,
        columns: &mut [Box<dyn arrow_array::builder::ArrayBuilder>],
    ) -> Result<(), crate::RelationError> {
        crate::columnar::ArrowValue::append(
            &self.r#balance_id,
            columns[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#model_id, columns[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#case_id, columns[2usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#quantity_id,
            columns[3usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#accumulation,
            columns[4usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#tolerance,
            columns[5usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#integral_tolerance,
            columns[6usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#provenance,
            columns[7usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#terms, columns[8usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#impulses, columns[9usize].as_mut())?;
        Ok(())
    }
    fn relation(
        registry: &pse_schema::Registry,
    ) -> Result<&pse_schema::model::RelationSpec, crate::RelationError> {
        spec(registry)
    }
    fn builder(
        registry: &pse_schema::Registry,
        capacity: usize,
    ) -> Result<Self::Builder, crate::RelationError> {
        AuthoredPhysicalBalancesBuilder::with_registry(registry, capacity)
    }
    fn push(builder: &mut Self::Builder, row: Self) -> Result<(), crate::RelationError> {
        builder.push(row)
    }
    fn finish(
        builder: Self::Builder,
    ) -> Result<crate::columnar::FieldCheckedBatch, crate::RelationError> {
        builder.finish()
    }
    fn rows(
        batch: &crate::columnar::FieldCheckedBatch,
    ) -> Result<Vec<Self>, crate::RelationError> {
        AuthoredPhysicalBalancesView::from_checked(batch)?.rows()
    }
    fn rows_at(
        batch: &crate::columnar::FieldCheckedBatch,
        positions: &[usize],
    ) -> Result<Vec<Self>, crate::RelationError> {
        let view = AuthoredPhysicalBalancesView::from_checked(batch)?;
        positions.iter().map(|&position| view.row(position)).collect()
    }
    fn builder_allocation_size() -> usize {
        46_080_usize + size_of::<Self::Builder>()
    }
    fn minimum_row_allocation_size() -> usize {
        360usize
    }
    fn allocation_size(&self) -> Result<usize, crate::RelationError> {
        let mut bytes = 0usize;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            if (self.r#accumulation).is_some() {
                crate::columnar::allocation_add(
                    1,
                    Ok::<usize, crate::RelationError>(16usize)?,
                )
            } else {
                Ok::<usize, crate::RelationError>(1)
            }?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(8usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            if (self.r#integral_tolerance).is_some() {
                crate::columnar::allocation_add(
                    1,
                    Ok::<usize, crate::RelationError>(8usize)?,
                )
            } else {
                Ok::<usize, crate::RelationError>(1)
            }?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            crate::columnar::allocation_add(8, (self.r#provenance).len())?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            (self.r#terms)
                .iter()
                .try_fold(
                    8usize,
                    |bytes, item| crate::columnar::allocation_add(
                        bytes,
                        {
                            let mut bytes = 1usize;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                crate::columnar::allocation_add(
                                    8,
                                    ((item).r#role).as_str().len(),
                                )?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#transfer_id).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(16usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#mode).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(8usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            Ok::<usize, crate::RelationError>(bytes)
                        }?,
                    ),
                )?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            (self.r#impulses)
                .iter()
                .try_fold(
                    8usize,
                    |bytes, _| crate::columnar::allocation_add(
                        bytes,
                        {
                            let mut bytes = 1usize;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            Ok::<usize, crate::RelationError>(bytes)
                        }?,
                    ),
                )?,
        )?;
        Ok(bytes)
    }
}
/// The complete declared relation key.
pub const RELATION_KEY: pse_schema::model::RelationKey = pse_schema::model::RelationKey {
    namespace: NAMESPACE,
    name: NAME,
    version: VERSION,
};
/// Stable field references projected from the declared column order.
pub const COLUMNS: [crate::columnar::ColumnReference; 10usize] = [
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "balance_id",
        position: 0usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "model_id",
        position: 1usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "case_id",
        position: 2usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "quantity_id",
        position: 3usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "accumulation",
        position: 4usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "tolerance",
        position: 5usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "integral_tolerance",
        position: 6usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "provenance",
        position: 7usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "terms",
        position: 8usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "impulses",
        position: 9usize,
    },
];
/// Named native column references derived from the declared field inventory.
pub mod columns {
    ///balance_id
    pub const BALANCE_ID: crate::columnar::ColumnReference = super::COLUMNS[0usize];
    ///model_id
    pub const MODEL_ID: crate::columnar::ColumnReference = super::COLUMNS[1usize];
    ///case_id
    pub const CASE_ID: crate::columnar::ColumnReference = super::COLUMNS[2usize];
    ///quantity_id
    pub const QUANTITY_ID: crate::columnar::ColumnReference = super::COLUMNS[3usize];
    ///accumulation
    pub const ACCUMULATION: crate::columnar::ColumnReference = super::COLUMNS[4usize];
    ///tolerance
    pub const TOLERANCE: crate::columnar::ColumnReference = super::COLUMNS[5usize];
    ///integral_tolerance
    pub const INTEGRAL_TOLERANCE: crate::columnar::ColumnReference = super::COLUMNS[6usize];
    ///provenance
    pub const PROVENANCE: crate::columnar::ColumnReference = super::COLUMNS[7usize];
    ///terms
    pub const TERMS: crate::columnar::ColumnReference = super::COLUMNS[8usize];
    ///impulses
    pub const IMPULSES: crate::columnar::ColumnReference = super::COLUMNS[9usize];
}
/// Borrowed Arrow columns with checked layout and local values.
/// Keys, references and domain completeness require relational admission.
#[derive(Debug)]
pub struct AuthoredPhysicalBalancesView<'a> {
    batch: &'a crate::RecordBatch,
    balance_id_column: &'a arrow_array::FixedSizeBinaryArray,
    model_id_column: &'a arrow_array::FixedSizeBinaryArray,
    case_id_column: &'a arrow_array::FixedSizeBinaryArray,
    quantity_id_column: &'a arrow_array::FixedSizeBinaryArray,
    accumulation_column: &'a arrow_array::FixedSizeBinaryArray,
    tolerance_column: &'a arrow_array::Float64Array,
    integral_tolerance_column: &'a arrow_array::Float64Array,
    provenance_column: &'a arrow_array::StringArray,
    terms_column: &'a arrow_array::ListArray,
    impulses_column: &'a arrow_array::ListArray,
}
impl<'a> AuthoredPhysicalBalancesView<'a> {
    /// Admits a raw candidate's actual schema and visible local values.
    /// # Errors
    /// A schema, field contract or local value violation.
    pub fn try_from_batch(
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        Self::try_from_batch_with_registry(pse_schema::registry()?, batch)
    }
    /// Admits a raw candidate with an explicitly bound registry.
    /// # Errors
    /// A schema, field contract or local value violation.
    pub fn try_from_batch_with_registry(
        registry: &pse_schema::Registry,
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        crate::validate::validate_batch(registry, spec(registry)?, batch)
            .map_err(|errors| crate::RelationError::Validation {
                errors,
            })?;
        Self::borrow_columns(batch)
    }
    /// Borrows a checked owner without rescanning visible values.
    /// The private owner must carry this exact relation and complete declaration.
    /// # Errors
    /// A different generated declaration or an incompatible Arrow layout.
    pub fn from_checked(
        owner: &'a crate::columnar::FieldCheckedBatch,
    ) -> Result<Self, crate::RelationError> {
        Self::borrow_columns(
            owner.for_generated(RELATION_ID, crate::generated::contracts::expected()?)?,
        )
    }
    fn borrow_columns(
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        Ok(Self {
            batch,
            balance_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(0usize).as_ref())?,
            model_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(1usize).as_ref())?,
            case_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(2usize).as_ref())?,
            quantity_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(3usize).as_ref())?,
            accumulation_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(4usize).as_ref())?,
            tolerance_column: crate::columnar::array::<
                arrow_array::Float64Array,
            >(batch.column(5usize).as_ref())?,
            integral_tolerance_column: crate::columnar::array::<
                arrow_array::Float64Array,
            >(batch.column(6usize).as_ref())?,
            provenance_column: crate::columnar::array::<
                arrow_array::StringArray,
            >(batch.column(7usize).as_ref())?,
            terms_column: crate::columnar::array::<
                arrow_array::ListArray,
            >(batch.column(8usize).as_ref())?,
            impulses_column: crate::columnar::array::<
                arrow_array::ListArray,
            >(batch.column(9usize).as_ref())?,
        })
    }
    /// The immutable batch, preserving its buffer owners and reservations.
    pub const fn batch(&self) -> &'a crate::RecordBatch {
        self.batch
    }
    /// The number of visible relation rows.
    pub fn len(&self) -> usize {
        self.batch.num_rows()
    }
    /// Whether this view has no relation rows.
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "balance_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn balance_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.balance_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "balance_id", "`.")]
    pub fn balance_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[0usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "model_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn model_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.model_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "model_id", "`.")]
    pub fn model_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[1usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "case_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn case_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.case_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "case_id", "`.")]
    pub fn case_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[2usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "quantity_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn quantity_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.quantity_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "quantity_id", "`.")]
    pub fn quantity_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[3usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "accumulation",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn accumulation_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.accumulation_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "accumulation", "`.")]
    pub fn accumulation_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[4usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "tolerance",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn tolerance_column(&self) -> &'a arrow_array::Float64Array {
        self.tolerance_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "tolerance", "`.")]
    pub fn tolerance_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[5usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "integral_tolerance",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn integral_tolerance_column(&self) -> &'a arrow_array::Float64Array {
        self.integral_tolerance_column
    }
    #[doc = concat!(
        "Borrows the exact declared field for `",
        "integral_tolerance",
        "`.",
    )]
    pub fn integral_tolerance_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[6usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "provenance",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn provenance_column(&self) -> &'a arrow_array::StringArray {
        self.provenance_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "provenance", "`.")]
    pub fn provenance_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[7usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "terms",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn terms_column(&self) -> &'a arrow_array::ListArray {
        self.terms_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "terms", "`.")]
    pub fn terms_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[8usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "impulses",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn impulses_column(&self) -> &'a arrow_array::ListArray {
        self.impulses_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "impulses", "`.")]
    pub fn impulses_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[9usize]
    }
    /// Decodes one row for an explicit scalar algorithm boundary.
    /// Columnar consumers should borrow the concrete column accessors.
    /// # Errors
    /// The row is out of range or its physical value cannot be decoded.
    pub fn row(
        &self,
        index: usize,
    ) -> Result<AuthoredPhysicalBalancesRow, crate::RelationError> {
        if index >= self.len() {
            return Err(crate::columnar::mismatch("a row within the generated view"));
        }
        Ok(AuthoredPhysicalBalancesRow {
            r#balance_id: crate::columnar::ArrowValue::read(
                self.balance_id_column,
                index,
            )?,
            r#model_id: crate::columnar::ArrowValue::read(self.model_id_column, index)?,
            r#case_id: crate::columnar::ArrowValue::read(self.case_id_column, index)?,
            r#quantity_id: crate::columnar::ArrowValue::read(
                self.quantity_id_column,
                index,
            )?,
            r#accumulation: crate::columnar::ArrowValue::read(
                self.accumulation_column,
                index,
            )?,
            r#tolerance: crate::columnar::ArrowValue::read(
                self.tolerance_column,
                index,
            )?,
            r#integral_tolerance: crate::columnar::ArrowValue::read(
                self.integral_tolerance_column,
                index,
            )?,
            r#provenance: crate::columnar::ArrowValue::read(
                self.provenance_column,
                index,
            )?,
            r#terms: crate::columnar::ArrowValue::read(self.terms_column, index)?,
            r#impulses: crate::columnar::ArrowValue::read(self.impulses_column, index)?,
        })
    }
    /// Decodes rows directly from Arrow for an explicit scalar algorithm boundary.
    /// This performs no schema/value admission and creates no `serde_json::Value` intermediates.
    /// # Errors
    /// A physical value cannot be decoded.
    pub fn rows(
        &self,
    ) -> Result<Vec<AuthoredPhysicalBalancesRow>, crate::RelationError> {
        (0..self.len()).map(|index| self.row(index)).collect()
    }
}
/// Shared native column builder for this declaration.
pub type AuthoredPhysicalBalancesBuilder = crate::columnar::RowBuilder<
    AuthoredPhysicalBalancesRow,
>;
impl crate::columnar::RelationView for AuthoredPhysicalBalancesView<'_> {
    type Row = AuthoredPhysicalBalancesRow;
    fn batch(&self) -> &crate::RecordBatch {
        self.batch
    }
}
