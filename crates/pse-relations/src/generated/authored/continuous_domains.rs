// SPDX-License-Identifier: MIT OR Apache-2.0
// Copyright (c) 2026 Paul Heyse
// @generated by pse-schema; do not edit

/// Registry-generated semantic values; native codecs remain local.
pub use pse_model::generated::r#authored::r#continuous_domains::{
    AuthoredContinuousDomainsRow, Row,
};
/// The declared relation identity.
pub const RELATION_ID: pse_ids::SemanticId = pse_ids::SemanticId::from_bytes([
    16u8, 173u8, 25u8, 107u8, 13u8, 92u8, 101u8, 28u8, 89u8, 19u8, 108u8, 12u8, 38u8,
    98u8, 220u8, 136u8,
]);
/// The declared name within its namespace.
pub const NAME: &str = "continuous_domains";
/// The declared namespace.
pub const NAMESPACE: pse_schema::model::Namespace = pse_schema::model::Namespace::Authored;
/// The schema generation.
pub const VERSION: u32 = 1u32;
/// Generated interchange fingerprint, not proof of semantic equivalence or row validity.
pub const FINGERPRINT: pse_ids::ContentHash = pse_ids::ContentHash::from_bytes([
    238u8, 14u8, 105u8, 1u8, 184u8, 135u8, 170u8, 225u8, 211u8, 234u8, 72u8, 225u8,
    119u8, 3u8, 15u8, 44u8, 1u8, 38u8, 88u8, 94u8, 158u8, 242u8, 163u8, 173u8, 134u8,
    112u8, 157u8, 210u8, 186u8, 103u8, 74u8, 199u8,
]);
impl crate::columnar::ArrowValue for AuthoredContinuousDomainsRow {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#domain_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#lower, children[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#upper, children[2usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#unit_id, children[3usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#initial_points,
            children[4usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#discretization_policy_id,
            children[5usize].as_mut(),
        )?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[1usize].as_mut())?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[2usize].as_mut())?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[3usize].as_mut(),
        )?;
        <Vec<
            f64,
        > as crate::columnar::ArrowValue>::append_null(children[4usize].as_mut())?;
        <Option<
            pse_ids::SemanticId,
        > as crate::columnar::ArrowValue>::append_null(children[5usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#domain_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#lower: <f64 as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#upper: <f64 as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
            r#unit_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(3usize).as_ref(),
                index,
            )?,
            r#initial_points: <Vec<
                f64,
            > as crate::columnar::ArrowValue>::read(
                input.column(4usize).as_ref(),
                index,
            )?,
            r#discretization_policy_id: <Option<
                pse_ids::SemanticId,
            > as crate::columnar::ArrowValue>::read(
                input.column(5usize).as_ref(),
                index,
            )?,
        })
    }
}
/// The concrete generated relation view.
pub type View<'a> = AuthoredContinuousDomainsView<'a>;
/// The concrete generated relation builder.
pub type Builder = AuthoredContinuousDomainsBuilder;
/// Resolves this exact generated contract in a runtime registry.
/// # Errors
/// A missing or incompatible declaration.
pub fn spec(
    reg: &pse_schema::Registry,
) -> Result<&pse_schema::model::RelationSpec, crate::RelationError> {
    let spec = reg
        .relation_by_id(RELATION_ID)
        .ok_or_else(|| crate::RelationError::UnknownRegistry {
            relation: "authored.continuous_domains".to_owned(),
        })?;
    check_declaration(reg, spec)?;
    Ok(spec)
}
fn check_declaration(
    reg: &pse_schema::Registry,
    spec: &pse_schema::model::RelationSpec,
) -> Result<(), crate::RelationError> {
    reg.contract(spec)?.require_generated(crate::generated::contracts::expected()?)?;
    Ok(())
}
/// The schema built from the authoritative declaration.
/// # Errors
/// An unavailable registry or incompatible generated contract.
pub fn schema() -> Result<crate::SchemaRef, crate::RelationError> {
    let reg = pse_schema::registry()?;
    Ok(pse_schema::arrow::relation_schema_ref(reg, spec(reg)?)?)
}
/// Checks schema, recursive extension contracts and visible values.
/// # Errors
/// All independently actionable violations.
pub fn validate(batch: &crate::RecordBatch) -> Result<(), Vec<crate::RelationError>> {
    let reg = pse_schema::registry().map_err(|error| vec![error.into()])?;
    crate::validate::validate_batch(reg, spec(reg).map_err(|error| vec![error])?, batch)
}
impl crate::columnar::RelationRow for AuthoredContinuousDomainsRow {
    type Builder = AuthoredContinuousDomainsBuilder;
    fn append_columns(
        &self,
        columns: &mut [Box<dyn arrow_array::builder::ArrayBuilder>],
    ) -> Result<(), crate::RelationError> {
        crate::columnar::ArrowValue::append(
            &self.r#domain_id,
            columns[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#lower, columns[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#upper, columns[2usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#unit_id, columns[3usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#initial_points,
            columns[4usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#discretization_policy_id,
            columns[5usize].as_mut(),
        )?;
        Ok(())
    }
    fn relation(
        registry: &pse_schema::Registry,
    ) -> Result<&pse_schema::model::RelationSpec, crate::RelationError> {
        spec(registry)
    }
    fn builder(
        registry: &pse_schema::Registry,
        capacity: usize,
    ) -> Result<Self::Builder, crate::RelationError> {
        AuthoredContinuousDomainsBuilder::with_registry(registry, capacity)
    }
    fn push(builder: &mut Self::Builder, row: Self) -> Result<(), crate::RelationError> {
        builder.push(row)
    }
    fn finish(
        builder: Self::Builder,
    ) -> Result<crate::columnar::FieldCheckedBatch, crate::RelationError> {
        builder.finish()
    }
    fn rows(
        batch: &crate::columnar::FieldCheckedBatch,
    ) -> Result<Vec<Self>, crate::RelationError> {
        AuthoredContinuousDomainsView::from_checked(batch)?.rows()
    }
    fn rows_at(
        batch: &crate::columnar::FieldCheckedBatch,
        positions: &[usize],
    ) -> Result<Vec<Self>, crate::RelationError> {
        let view = AuthoredContinuousDomainsView::from_checked(batch)?;
        positions.iter().map(|&position| view.row(position)).collect()
    }
    fn builder_allocation_size() -> usize {
        16_384_usize + size_of::<Self::Builder>()
    }
    fn minimum_row_allocation_size() -> usize {
        128usize
    }
    fn allocation_size(&self) -> Result<usize, crate::RelationError> {
        let mut bytes = 0usize;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(8usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(8usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            (self.r#initial_points)
                .iter()
                .try_fold(
                    8usize,
                    |bytes, _| crate::columnar::allocation_add(
                        bytes,
                        Ok::<usize, crate::RelationError>(8usize)?,
                    ),
                )?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            if (self.r#discretization_policy_id).is_some() {
                crate::columnar::allocation_add(
                    1,
                    Ok::<usize, crate::RelationError>(16usize)?,
                )
            } else {
                Ok::<usize, crate::RelationError>(1)
            }?,
        )?;
        Ok(bytes)
    }
}
/// The complete declared relation key.
pub const RELATION_KEY: pse_schema::model::RelationKey = pse_schema::model::RelationKey {
    namespace: NAMESPACE,
    name: NAME,
    version: VERSION,
};
/// Stable field references projected from the declared column order.
pub const COLUMNS: [crate::columnar::ColumnReference; 6usize] = [
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "domain_id",
        position: 0usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "lower",
        position: 1usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "upper",
        position: 2usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "unit_id",
        position: 3usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "initial_points",
        position: 4usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "discretization_policy_id",
        position: 5usize,
    },
];
/// Named native column references derived from the declared field inventory.
pub mod columns {
    ///domain_id
    pub const DOMAIN_ID: crate::columnar::ColumnReference = super::COLUMNS[0usize];
    ///lower
    pub const LOWER: crate::columnar::ColumnReference = super::COLUMNS[1usize];
    ///upper
    pub const UPPER: crate::columnar::ColumnReference = super::COLUMNS[2usize];
    ///unit_id
    pub const UNIT_ID: crate::columnar::ColumnReference = super::COLUMNS[3usize];
    ///initial_points
    pub const INITIAL_POINTS: crate::columnar::ColumnReference = super::COLUMNS[4usize];
    ///discretization_policy_id
    pub const DISCRETIZATION_POLICY_ID: crate::columnar::ColumnReference = super::COLUMNS[5usize];
}
/// Borrowed Arrow columns with checked layout and local values.
/// Keys, references and domain completeness require relational admission.
#[derive(Debug)]
pub struct AuthoredContinuousDomainsView<'a> {
    batch: &'a crate::RecordBatch,
    domain_id_column: &'a arrow_array::FixedSizeBinaryArray,
    lower_column: &'a arrow_array::Float64Array,
    upper_column: &'a arrow_array::Float64Array,
    unit_id_column: &'a arrow_array::FixedSizeBinaryArray,
    initial_points_column: &'a arrow_array::ListArray,
    discretization_policy_id_column: &'a arrow_array::FixedSizeBinaryArray,
}
impl<'a> AuthoredContinuousDomainsView<'a> {
    /// Admits a raw candidate's actual schema and visible local values.
    /// # Errors
    /// A schema, field contract or local value violation.
    pub fn try_from_batch(
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        Self::try_from_batch_with_registry(pse_schema::registry()?, batch)
    }
    /// Admits a raw candidate with an explicitly bound registry.
    /// # Errors
    /// A schema, field contract or local value violation.
    pub fn try_from_batch_with_registry(
        registry: &pse_schema::Registry,
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        crate::validate::validate_batch(registry, spec(registry)?, batch)
            .map_err(|errors| crate::RelationError::Validation {
                errors,
            })?;
        Self::borrow_columns(batch)
    }
    /// Borrows a checked owner without rescanning visible values.
    /// The private owner must carry this exact relation and complete declaration.
    /// # Errors
    /// A different generated declaration or an incompatible Arrow layout.
    pub fn from_checked(
        owner: &'a crate::columnar::FieldCheckedBatch,
    ) -> Result<Self, crate::RelationError> {
        Self::borrow_columns(
            owner.for_generated(RELATION_ID, crate::generated::contracts::expected()?)?,
        )
    }
    fn borrow_columns(
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        Ok(Self {
            batch,
            domain_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(0usize).as_ref())?,
            lower_column: crate::columnar::array::<
                arrow_array::Float64Array,
            >(batch.column(1usize).as_ref())?,
            upper_column: crate::columnar::array::<
                arrow_array::Float64Array,
            >(batch.column(2usize).as_ref())?,
            unit_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(3usize).as_ref())?,
            initial_points_column: crate::columnar::array::<
                arrow_array::ListArray,
            >(batch.column(4usize).as_ref())?,
            discretization_policy_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(5usize).as_ref())?,
        })
    }
    /// The immutable batch, preserving its buffer owners and reservations.
    pub const fn batch(&self) -> &'a crate::RecordBatch {
        self.batch
    }
    /// The number of visible relation rows.
    pub fn len(&self) -> usize {
        self.batch.num_rows()
    }
    /// Whether this view has no relation rows.
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "domain_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn domain_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.domain_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "domain_id", "`.")]
    pub fn domain_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[0usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "lower",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn lower_column(&self) -> &'a arrow_array::Float64Array {
        self.lower_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "lower", "`.")]
    pub fn lower_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[1usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "upper",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn upper_column(&self) -> &'a arrow_array::Float64Array {
        self.upper_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "upper", "`.")]
    pub fn upper_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[2usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "unit_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn unit_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.unit_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "unit_id", "`.")]
    pub fn unit_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[3usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "initial_points",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn initial_points_column(&self) -> &'a arrow_array::ListArray {
        self.initial_points_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "initial_points", "`.")]
    pub fn initial_points_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[4usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "discretization_policy_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn discretization_policy_id_column(
        &self,
    ) -> &'a arrow_array::FixedSizeBinaryArray {
        self.discretization_policy_id_column
    }
    #[doc = concat!(
        "Borrows the exact declared field for `",
        "discretization_policy_id",
        "`.",
    )]
    pub fn discretization_policy_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[5usize]
    }
    /// Decodes one row for an explicit scalar algorithm boundary.
    /// Columnar consumers should borrow the concrete column accessors.
    /// # Errors
    /// The row is out of range or its physical value cannot be decoded.
    pub fn row(
        &self,
        index: usize,
    ) -> Result<AuthoredContinuousDomainsRow, crate::RelationError> {
        if index >= self.len() {
            return Err(crate::columnar::mismatch("a row within the generated view"));
        }
        Ok(AuthoredContinuousDomainsRow {
            r#domain_id: crate::columnar::ArrowValue::read(
                self.domain_id_column,
                index,
            )?,
            r#lower: crate::columnar::ArrowValue::read(self.lower_column, index)?,
            r#upper: crate::columnar::ArrowValue::read(self.upper_column, index)?,
            r#unit_id: crate::columnar::ArrowValue::read(self.unit_id_column, index)?,
            r#initial_points: crate::columnar::ArrowValue::read(
                self.initial_points_column,
                index,
            )?,
            r#discretization_policy_id: crate::columnar::ArrowValue::read(
                self.discretization_policy_id_column,
                index,
            )?,
        })
    }
    /// Decodes rows directly from Arrow for an explicit scalar algorithm boundary.
    /// This performs no schema/value admission and creates no `serde_json::Value` intermediates.
    /// # Errors
    /// A physical value cannot be decoded.
    pub fn rows(
        &self,
    ) -> Result<Vec<AuthoredContinuousDomainsRow>, crate::RelationError> {
        (0..self.len()).map(|index| self.row(index)).collect()
    }
}
/// Shared native column builder for this declaration.
pub type AuthoredContinuousDomainsBuilder = crate::columnar::RowBuilder<
    AuthoredContinuousDomainsRow,
>;
impl crate::columnar::RelationView for AuthoredContinuousDomainsView<'_> {
    type Row = AuthoredContinuousDomainsRow;
    fn batch(&self) -> &crate::RecordBatch {
        self.batch
    }
}
