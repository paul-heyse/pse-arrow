// SPDX-License-Identifier: MIT OR Apache-2.0
// Copyright (c) 2026 Paul Heyse
// @generated by pse-schema; do not edit

/// Registry-generated semantic values; native codecs remain local.
pub use pse_model::generated::r#authored::r#fit_cases::{
    AuthoredFitCasesFieldParametersItem, AuthoredFitCasesFieldExperimentsItem,
    AuthoredFitCasesFieldObservationsItem, AuthoredFitCasesRow, Row,
};
/// The declared relation identity.
pub const RELATION_ID: pse_ids::SemanticId = pse_ids::SemanticId::from_bytes([
    219u8, 244u8, 5u8, 89u8, 58u8, 96u8, 190u8, 124u8, 237u8, 233u8, 93u8, 57u8, 234u8,
    223u8, 60u8, 185u8,
]);
/// The declared name within its namespace.
pub const NAME: &str = "fit_cases";
/// The declared namespace.
pub const NAMESPACE: pse_schema::model::Namespace = pse_schema::model::Namespace::Authored;
/// The schema generation.
pub const VERSION: u32 = 2u32;
/// Generated interchange fingerprint, not proof of semantic equivalence or row validity.
pub const FINGERPRINT: pse_ids::ContentHash = pse_ids::ContentHash::from_bytes([
    121u8, 33u8, 186u8, 212u8, 24u8, 110u8, 72u8, 57u8, 90u8, 114u8, 29u8, 43u8, 228u8,
    147u8, 202u8, 123u8, 164u8, 73u8, 235u8, 96u8, 78u8, 4u8, 206u8, 197u8, 143u8, 223u8,
    122u8, 201u8, 75u8, 118u8, 122u8, 123u8,
]);
impl crate::columnar::ArrowValue for AuthoredFitCasesFieldParametersItem {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#symbol_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#fixed, children[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#value, children[2usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#lower, children[3usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#upper, children[4usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#scale, children[5usize].as_mut())?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <bool as crate::columnar::ArrowValue>::append_null(children[1usize].as_mut())?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[2usize].as_mut())?;
        <Option<
            f64,
        > as crate::columnar::ArrowValue>::append_null(children[3usize].as_mut())?;
        <Option<
            f64,
        > as crate::columnar::ArrowValue>::append_null(children[4usize].as_mut())?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[5usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#symbol_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#fixed: <bool as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#value: <f64 as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
            r#lower: <Option<
                f64,
            > as crate::columnar::ArrowValue>::read(
                input.column(3usize).as_ref(),
                index,
            )?,
            r#upper: <Option<
                f64,
            > as crate::columnar::ArrowValue>::read(
                input.column(4usize).as_ref(),
                index,
            )?,
            r#scale: <f64 as crate::columnar::ArrowValue>::read(
                input.column(5usize).as_ref(),
                index,
            )?,
        })
    }
}
impl crate::columnar::ArrowValue for AuthoredFitCasesFieldExperimentsItem {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#experiment_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#case_id, children[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#dynamic_id,
            children[2usize].as_mut(),
        )?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[1usize].as_mut(),
        )?;
        <Option<
            pse_ids::SemanticId,
        > as crate::columnar::ArrowValue>::append_null(children[2usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#experiment_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#case_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#dynamic_id: <Option<
                pse_ids::SemanticId,
            > as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
        })
    }
}
impl crate::columnar::ArrowValue for AuthoredFitCasesFieldObservationsItem {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(
            &self.r#observation_id,
            children[0usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#experiment_id,
            children[1usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#output_id,
            children[2usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(&self.r#time, children[3usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#time_basis,
            children[4usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#time_unit_id,
            children[5usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#included,
            children[6usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#importance,
            children[7usize].as_mut(),
        )?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[1usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[2usize].as_mut(),
        )?;
        <Option<
            f64,
        > as crate::columnar::ArrowValue>::append_null(children[3usize].as_mut())?;
        <Option<
            crate::generated::enums::ObservationTimeBasis,
        > as crate::columnar::ArrowValue>::append_null(children[4usize].as_mut())?;
        <Option<
            pse_ids::SemanticId,
        > as crate::columnar::ArrowValue>::append_null(children[5usize].as_mut())?;
        <bool as crate::columnar::ArrowValue>::append_null(children[6usize].as_mut())?;
        <f64 as crate::columnar::ArrowValue>::append_null(children[7usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#observation_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#experiment_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#output_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
            r#time: <Option<
                f64,
            > as crate::columnar::ArrowValue>::read(
                input.column(3usize).as_ref(),
                index,
            )?,
            r#time_basis: <Option<
                crate::generated::enums::ObservationTimeBasis,
            > as crate::columnar::ArrowValue>::read(
                input.column(4usize).as_ref(),
                index,
            )?,
            r#time_unit_id: <Option<
                pse_ids::SemanticId,
            > as crate::columnar::ArrowValue>::read(
                input.column(5usize).as_ref(),
                index,
            )?,
            r#included: <bool as crate::columnar::ArrowValue>::read(
                input.column(6usize).as_ref(),
                index,
            )?,
            r#importance: <f64 as crate::columnar::ArrowValue>::read(
                input.column(7usize).as_ref(),
                index,
            )?,
        })
    }
}
impl crate::columnar::ArrowValue for AuthoredFitCasesRow {
    fn append(
        &self,
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        crate::columnar::ArrowValue::append(&self.r#fit_id, children[0usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#model_id,
            children[1usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#parameters,
            children[2usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#experiments,
            children[3usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#observations,
            children[4usize].as_mut(),
        )?;
        output.append(true);
        Ok(())
    }
    fn append_null(
        output: &mut dyn arrow_array::builder::ArrayBuilder,
    ) -> Result<(), crate::RelationError> {
        let output = crate::columnar::builder::<
            arrow_array::builder::StructBuilder,
        >(output)?;
        let children = output.field_builders_mut();
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[0usize].as_mut(),
        )?;
        <pse_ids::SemanticId as crate::columnar::ArrowValue>::append_null(
            children[1usize].as_mut(),
        )?;
        <Vec<
            AuthoredFitCasesFieldParametersItem,
        > as crate::columnar::ArrowValue>::append_null(children[2usize].as_mut())?;
        <Vec<
            AuthoredFitCasesFieldExperimentsItem,
        > as crate::columnar::ArrowValue>::append_null(children[3usize].as_mut())?;
        <Vec<
            AuthoredFitCasesFieldObservationsItem,
        > as crate::columnar::ArrowValue>::append_null(children[4usize].as_mut())?;
        output.append(false);
        Ok(())
    }
    fn read(
        input: &dyn arrow_array::Array,
        index: usize,
    ) -> Result<Self, crate::RelationError> {
        crate::columnar::visible(input, index)?;
        let input = crate::columnar::array::<arrow_array::StructArray>(input)?;
        Ok(Self {
            r#fit_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(0usize).as_ref(),
                index,
            )?,
            r#model_id: <pse_ids::SemanticId as crate::columnar::ArrowValue>::read(
                input.column(1usize).as_ref(),
                index,
            )?,
            r#parameters: <Vec<
                AuthoredFitCasesFieldParametersItem,
            > as crate::columnar::ArrowValue>::read(
                input.column(2usize).as_ref(),
                index,
            )?,
            r#experiments: <Vec<
                AuthoredFitCasesFieldExperimentsItem,
            > as crate::columnar::ArrowValue>::read(
                input.column(3usize).as_ref(),
                index,
            )?,
            r#observations: <Vec<
                AuthoredFitCasesFieldObservationsItem,
            > as crate::columnar::ArrowValue>::read(
                input.column(4usize).as_ref(),
                index,
            )?,
        })
    }
}
/// The concrete generated relation view.
pub type View<'a> = AuthoredFitCasesView<'a>;
/// The concrete generated relation builder.
pub type Builder = AuthoredFitCasesBuilder;
/// Resolves this exact generated contract in a runtime registry.
/// # Errors
/// A missing or incompatible declaration.
pub fn spec(
    reg: &pse_schema::Registry,
) -> Result<&pse_schema::model::RelationSpec, crate::RelationError> {
    let spec = reg
        .relation_by_id(RELATION_ID)
        .ok_or_else(|| crate::RelationError::UnknownRegistry {
            relation: "authored.fit_cases".to_owned(),
        })?;
    check_declaration(reg, spec)?;
    Ok(spec)
}
fn check_declaration(
    reg: &pse_schema::Registry,
    spec: &pse_schema::model::RelationSpec,
) -> Result<(), crate::RelationError> {
    reg.contract(spec)?.require_generated(crate::generated::contracts::expected())?;
    Ok(())
}
/// The schema built from the authoritative declaration.
/// # Errors
/// An unavailable registry or incompatible generated contract.
pub fn schema() -> Result<crate::SchemaRef, crate::RelationError> {
    let reg = pse_schema::registry()?;
    Ok(pse_schema::arrow::relation_schema_ref(reg, spec(reg)?)?)
}
/// Checks schema, recursive extension contracts and visible values.
/// # Errors
/// All independently actionable violations.
pub fn validate(batch: &crate::RecordBatch) -> Result<(), Vec<crate::RelationError>> {
    let reg = pse_schema::registry().map_err(|error| vec![error.into()])?;
    crate::validate::validate_batch(reg, spec(reg).map_err(|error| vec![error])?, batch)
}
impl crate::columnar::RelationRow for AuthoredFitCasesRow {
    type Builder = AuthoredFitCasesBuilder;
    fn append_columns(
        &self,
        columns: &mut [Box<dyn arrow_array::builder::ArrayBuilder>],
    ) -> Result<(), crate::RelationError> {
        crate::columnar::ArrowValue::append(&self.r#fit_id, columns[0usize].as_mut())?;
        crate::columnar::ArrowValue::append(&self.r#model_id, columns[1usize].as_mut())?;
        crate::columnar::ArrowValue::append(
            &self.r#parameters,
            columns[2usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#experiments,
            columns[3usize].as_mut(),
        )?;
        crate::columnar::ArrowValue::append(
            &self.r#observations,
            columns[4usize].as_mut(),
        )?;
        Ok(())
    }
    fn relation(
        registry: &pse_schema::Registry,
    ) -> Result<&pse_schema::model::RelationSpec, crate::RelationError> {
        spec(registry)
    }
    fn builder(
        registry: &pse_schema::Registry,
        capacity: usize,
    ) -> Result<Self::Builder, crate::RelationError> {
        AuthoredFitCasesBuilder::with_registry(registry, capacity)
    }
    fn push(builder: &mut Self::Builder, row: Self) -> Result<(), crate::RelationError> {
        builder.push(row)
    }
    fn finish(
        builder: Self::Builder,
    ) -> Result<crate::columnar::FieldCheckedBatch, crate::RelationError> {
        builder.finish()
    }
    fn rows(
        batch: &crate::columnar::FieldCheckedBatch,
    ) -> Result<Vec<Self>, crate::RelationError> {
        AuthoredFitCasesView::from_checked(batch)?.rows()
    }
    fn rows_at(
        batch: &crate::columnar::FieldCheckedBatch,
        positions: &[usize],
    ) -> Result<Vec<Self>, crate::RelationError> {
        let view = AuthoredFitCasesView::from_checked(batch)?;
        positions.iter().map(|&position| view.row(position)).collect()
    }
    fn builder_allocation_size() -> usize {
        55_296_usize + size_of::<Self::Builder>()
    }
    fn minimum_row_allocation_size() -> usize {
        432usize
    }
    fn allocation_size(&self) -> Result<usize, crate::RelationError> {
        let mut bytes = 0usize;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            Ok::<usize, crate::RelationError>(16usize)?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            (self.r#parameters)
                .iter()
                .try_fold(
                    8usize,
                    |bytes, item| crate::columnar::allocation_add(
                        bytes,
                        {
                            let mut bytes = 1usize;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#lower).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(8usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#upper).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(8usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            Ok::<usize, crate::RelationError>(bytes)
                        }?,
                    ),
                )?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            (self.r#experiments)
                .iter()
                .try_fold(
                    8usize,
                    |bytes, item| crate::columnar::allocation_add(
                        bytes,
                        {
                            let mut bytes = 1usize;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#dynamic_id).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(16usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            Ok::<usize, crate::RelationError>(bytes)
                        }?,
                    ),
                )?,
        )?;
        bytes = crate::columnar::allocation_add(
            bytes,
            (self.r#observations)
                .iter()
                .try_fold(
                    8usize,
                    |bytes, item| crate::columnar::allocation_add(
                        bytes,
                        {
                            let mut bytes = 1usize;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(16usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#time).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(8usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if let Some(value) = ((item).r#time_basis).as_ref() {
                                    crate::columnar::allocation_add(
                                        1,
                                        crate::columnar::allocation_add(8, (value).as_str().len())?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                if ((item).r#time_unit_id).is_some() {
                                    crate::columnar::allocation_add(
                                        1,
                                        Ok::<usize, crate::RelationError>(16usize)?,
                                    )
                                } else {
                                    Ok::<usize, crate::RelationError>(1)
                                }?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            bytes = crate::columnar::allocation_add(
                                bytes,
                                Ok::<usize, crate::RelationError>(8usize)?,
                            )?;
                            Ok::<usize, crate::RelationError>(bytes)
                        }?,
                    ),
                )?,
        )?;
        Ok(bytes)
    }
}
/// The complete declared relation key.
pub const RELATION_KEY: pse_schema::model::RelationKey = pse_schema::model::RelationKey {
    namespace: NAMESPACE,
    name: NAME,
    version: VERSION,
};
/// Stable field references projected from the declared column order.
pub const COLUMNS: [crate::columnar::ColumnReference; 5usize] = [
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "fit_id",
        position: 0usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "model_id",
        position: 1usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "parameters",
        position: 2usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "experiments",
        position: 3usize,
    },
    crate::columnar::ColumnReference {
        relation_id: RELATION_ID,
        name: "observations",
        position: 4usize,
    },
];
/// Named native column references derived from the declared field inventory.
pub mod columns {
    ///fit_id
    pub const FIT_ID: crate::columnar::ColumnReference = super::COLUMNS[0usize];
    ///model_id
    pub const MODEL_ID: crate::columnar::ColumnReference = super::COLUMNS[1usize];
    ///parameters
    pub const PARAMETERS: crate::columnar::ColumnReference = super::COLUMNS[2usize];
    ///experiments
    pub const EXPERIMENTS: crate::columnar::ColumnReference = super::COLUMNS[3usize];
    ///observations
    pub const OBSERVATIONS: crate::columnar::ColumnReference = super::COLUMNS[4usize];
}
/// Borrowed Arrow columns with checked layout and local values.
/// Keys, references and domain completeness require relational admission.
#[derive(Debug)]
pub struct AuthoredFitCasesView<'a> {
    batch: &'a crate::RecordBatch,
    fit_id_column: &'a arrow_array::FixedSizeBinaryArray,
    model_id_column: &'a arrow_array::FixedSizeBinaryArray,
    parameters_column: &'a arrow_array::ListArray,
    experiments_column: &'a arrow_array::ListArray,
    observations_column: &'a arrow_array::ListArray,
}
impl<'a> AuthoredFitCasesView<'a> {
    /// Admits a raw candidate's actual schema and visible local values.
    /// # Errors
    /// A schema, field contract or local value violation.
    pub fn try_from_batch(
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        Self::try_from_batch_with_registry(pse_schema::registry()?, batch)
    }
    /// Admits a raw candidate with an explicitly bound registry.
    /// # Errors
    /// A schema, field contract or local value violation.
    pub fn try_from_batch_with_registry(
        registry: &pse_schema::Registry,
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        crate::validate::validate_batch(registry, spec(registry)?, batch)
            .map_err(|errors| crate::RelationError::Validation {
                errors,
            })?;
        Self::borrow_columns(batch)
    }
    /// Borrows a checked owner without rescanning visible values.
    /// The private owner must carry this exact relation and complete declaration.
    /// # Errors
    /// A different generated declaration or an incompatible Arrow layout.
    pub fn from_checked(
        owner: &'a crate::columnar::FieldCheckedBatch,
    ) -> Result<Self, crate::RelationError> {
        Self::borrow_columns(
            owner.for_generated(RELATION_ID, crate::generated::contracts::expected())?,
        )
    }
    fn borrow_columns(
        batch: &'a crate::RecordBatch,
    ) -> Result<Self, crate::RelationError> {
        Ok(Self {
            batch,
            fit_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(0usize).as_ref())?,
            model_id_column: crate::columnar::array::<
                arrow_array::FixedSizeBinaryArray,
            >(batch.column(1usize).as_ref())?,
            parameters_column: crate::columnar::array::<
                arrow_array::ListArray,
            >(batch.column(2usize).as_ref())?,
            experiments_column: crate::columnar::array::<
                arrow_array::ListArray,
            >(batch.column(3usize).as_ref())?,
            observations_column: crate::columnar::array::<
                arrow_array::ListArray,
            >(batch.column(4usize).as_ref())?,
        })
    }
    /// The immutable batch, preserving its buffer owners and reservations.
    pub const fn batch(&self) -> &'a crate::RecordBatch {
        self.batch
    }
    /// The number of visible relation rows.
    pub fn len(&self) -> usize {
        self.batch.num_rows()
    }
    /// Whether this view has no relation rows.
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "fit_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn fit_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.fit_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "fit_id", "`.")]
    pub fn fit_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[0usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "model_id",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn model_id_column(&self) -> &'a arrow_array::FixedSizeBinaryArray {
        self.model_id_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "model_id", "`.")]
    pub fn model_id_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[1usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "parameters",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn parameters_column(&self) -> &'a arrow_array::ListArray {
        self.parameters_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "parameters", "`.")]
    pub fn parameters_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[2usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "experiments",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn experiments_column(&self) -> &'a arrow_array::ListArray {
        self.experiments_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "experiments", "`.")]
    pub fn experiments_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[3usize]
    }
    #[doc = concat!(
        "Borrows the actual Arrow column `",
        "observations",
        "`, including its offsets and validity bitmap.",
    )]
    pub const fn observations_column(&self) -> &'a arrow_array::ListArray {
        self.observations_column
    }
    #[doc = concat!("Borrows the exact declared field for `", "observations", "`.")]
    pub fn observations_field(&self) -> &'a crate::FieldRef {
        &self.batch.schema_ref().fields()[4usize]
    }
    /// Decodes one row for an explicit scalar algorithm boundary.
    /// Columnar consumers should borrow the concrete column accessors.
    /// # Errors
    /// The row is out of range or its physical value cannot be decoded.
    pub fn row(
        &self,
        index: usize,
    ) -> Result<AuthoredFitCasesRow, crate::RelationError> {
        if index >= self.len() {
            return Err(crate::columnar::mismatch("a row within the generated view"));
        }
        Ok(AuthoredFitCasesRow {
            r#fit_id: crate::columnar::ArrowValue::read(self.fit_id_column, index)?,
            r#model_id: crate::columnar::ArrowValue::read(self.model_id_column, index)?,
            r#parameters: crate::columnar::ArrowValue::read(
                self.parameters_column,
                index,
            )?,
            r#experiments: crate::columnar::ArrowValue::read(
                self.experiments_column,
                index,
            )?,
            r#observations: crate::columnar::ArrowValue::read(
                self.observations_column,
                index,
            )?,
        })
    }
    /// Decodes rows directly from Arrow for an explicit scalar algorithm boundary.
    /// This performs no schema/value admission and creates no `serde_json::Value` intermediates.
    /// # Errors
    /// A physical value cannot be decoded.
    pub fn rows(&self) -> Result<Vec<AuthoredFitCasesRow>, crate::RelationError> {
        (0..self.len()).map(|index| self.row(index)).collect()
    }
}
/// Shared native column builder for this declaration.
pub type AuthoredFitCasesBuilder = crate::columnar::RowBuilder<AuthoredFitCasesRow>;
impl crate::columnar::RelationView for AuthoredFitCasesView<'_> {
    type Row = AuthoredFitCasesRow;
    fn batch(&self) -> &crate::RecordBatch {
        self.batch
    }
}
