# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright (c) 2026 Paul Heyse
# Blueprint Appendix B names these relations without a complete column contract.
# R-30 requires an ADR before a later pass consumes them.
# A later producer alone is not a reason to omit an already specified relation.
[deferred]
"authored.template_scaling_defaults" = "Later scaling defaults; column contract remains partial."
"reference.diagnostic_thresholds" = "Later diagnostic policy; column contract remains partial."
"reference.cost_indices" = "Later costing data; column contract remains partial."
"reference.location_factors" = "Later costing data; column contract remains partial."
"runtime.sweep_results" = "Later case-set execution; no complete column contract."
"runtime.profile_results" = "Later scaling profiles; no complete column contract."
"runtime.run_state" = "Later runtime execution; no complete column contract."
"authored.measurement_models" = "Blueprint §6.10 leaves parameters untyped and the ErrorModel dictionary members undefined."
"authored.tags" = "Blueprint §19.2 does not specify authored tag identity, ownership/key columns, or the representation of group defaults."
"inferred.phase_equilibrium_species" = "Blueprint §9.5 does not map schematic pair to the composite phase-equilibrium-pair key, and intersection-only membership conflicts with l_only/v_only roles; both require a contract decision."
"compiled.math_alternative_sets" = "Blueprint §6.9 names the relation without a column contract."
"compiled.math_alternatives" = "Blueprint §6.9 names the relation without a column contract."
"compiled.math_static_analysis" = "Blueprint §6.9 leaves a literal ellipsis in the row contract; §7.5 only supplies parameter_dependence."
"compiled.scaling_plans" = "Blueprint §6.11 leaves options untyped; its representation must be declared before registration."
"runtime.solver_events" = "Blueprint §6.13 supplies the row shape, but no authority declares SolverEvent dictionary members or the ordinal width."
"provenance.closure_report" = "Blueprint §14.5 specifies report category/status but not the open-item payload and association contract; this explicitly defers the original A4 declaration pending that contract."
