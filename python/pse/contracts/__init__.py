# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright (c) 2026 Paul Heyse
# @generated by pse-schema (placeholder: the generator lands in phase 0; do not edit)
"""Generated contract classes for the Python boundary (blueprint §21).

This package is generated from the schema registry by ``pse-schema`` and
diff-checked in CI (``cargo xtask codegen --check``): edit the registry, then
``just codegen``. Nothing here may import anything else from ``pse`` -- an
import-linter contract enforces that -- so the contracts stay the leaf of the
package graph and can be structured without pulling the runtime in.

Phase 0 ships the extension-type registrar only; the attrs relation-row classes,
the msgspec manifest structs and the enumerations follow with the generator.
"""

from pse.contracts.extension_types import EXTENSION_NAMES, register_all

__all__ = ["EXTENSION_NAMES", "register_all"]
